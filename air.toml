# Air R formatter configuration -- https://posit-dev.github.io/air/
[format]
line-width = 80
indent-width = 2
indent-style = "space"
# docs/ holds verbatim review records (R scripts included); they are not
# package code and are never reformatted.
exclude = ["docs/"]
