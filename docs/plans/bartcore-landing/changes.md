# Changes register: main to bartcore

Every user-visible, C-consumer-visible or behavioural change the merge brings, merged and deduplicated from the five code-side slices and, from chg-U66, chg-C27, chg-B09 and chg-I15 on, from the eight arcs that landed after b7266da9: the pure C header, the front-door rename, interfaces and dependencies, engine performance, the memory-footprint audit, per-draw callbacks, the front-door formals consolidation and the setPredictor partition. Base is main at cb290550, tip is bartcore at fb34e15d; there are still no main-only commits, so the diff between them is exactly what a merge brings.

`visibility` is user, C-consumer, build or internal. `breaking` is yes, no, or behavioural, meaning the same call returns a different result. `anchor` names a symbol or a path, never a line. `decision` points at the row in docs/decisions.md that settles the change where one applies.

## 1. User-visible

| id | what changed | visibility | breaking | anchor | evidence | decision |
|---|---|---|---|---|---|---|
| chg-U01 | rbart_vi and its S3 methods removed, nine at the removal commit and six of them registered on main; the grouped random-effects and slice-sampler sources and the rbart man page deleted. rbart_vi and four of the methods stay exported as erroring stubs naming stan4bart (chg-U71); plot.rbart and print.rbart go outright | user | yes | rbart_vi, dbartsTombstones | commit 1e5f80b2 | dec-B105 |
| chg-U02 | dbartsControl loses rngKind and rngNormalKind; rngSeed is renamed seed, as both argument and S4 slot | user | yes | dbartsControl | git diff main..bartcore -- R/A_class.R R/dbarts.R | dec-A04 |
| chg-U03 | dbartsControl gains levelGibbs in fifth position and storage in seventh; n.cuts is promoted from an attribute to an S4 slot | user | yes | dbartsControl | git diff main..bartcore -- R/dbarts.R | dec-A23 |
| chg-U04 | Default proposal probabilities move from birth_death .5, swap .1, change .4 to birth_death .6, swap 0, change .4, perturb 0, rule_gibbs 0 | user | behavioural | defaultProposalProbs | git show main:R/model.R; commits fbff1989, 3ecd7f47, ab49f83a, 7fb166ca | dec-B02 |
| chg-U05 | Binary default node hyperprior moves from chi(1.25, Inf) to chi(1.5, 2.0), and chi()'s own defaults move with it | user | behavioural | resolveNodeHyperprior, chi | git show main:R/model.R | dec-B106 |
| chg-U06 | normal() takes k, sd and scale all NULL by default, with k resolved by family downstream | user | no | normal | git show main:R/model.R | |
| chg-U07 | cgm()'s split.probs defaults to NULL and resolves against the data at fit time | user | no | cgm, splitProbabilitiesSpec | git show main:R/model.R | |
| chg-U08 | bart2 defaults combineChains to TRUE, and combined-chain flattening goes sample-major to chain-major | user | behavioural | bart2 | git show main:R/bart.R; commit 5287837a | dec-A08 |
| chg-U09 | bart and bart2 keep a dots channel, but it carries only the tombstoned spellings; any other name is refused by name rather than dropped; storage and updateState become formals | user | yes | refuseForeignFrontDoorArgs | commit 08db8da1 | dec-B10, dec-B116 |
| chg-U10 | bart2 becomes an alias for bart, its formals copied rather than kept as its own signature (formals(bart2) <- formals(bart)); nets 11 more than main's bart2 (34 to 45): sigdf, sigquant, power, base, split.probs and proposal.probs leave as formals, folded into prior objects and the control slot, while monotone, interactions, blocks, variance, warm.start, n.grow.sweeps, factors, family, na.action, tree.prior, node.prior, resid.prior, storage, updateState, keepFits, callback and control join | user | yes | bart2 | git diff main..bartcore -- R/bart.R | dec-B75, dec-B116 |
| chg-U11 | bart gains 14 formals over main (31 to 45) as it takes on bart2's former front door (dec-B75): tree.prior, node.prior and resid.prior replace the scalar power, base, sigdf and sigquant (dec-B116), and subset, storage, family and control newly appear among others; subset was hard-coded NULL before | user | yes | bart | git show main:R/bart.R | dec-B75, dec-B116 |
| chg-U12 | bart takes family over thirteen tokens, resolved at the bridge into the engine's own list, and reads a counts-carrying data object as multinomial; bartBT has neither a family formal nor dots, so a family call there reaches R's unused-argument error | user | yes | bart, bartBT | git diff main..bartcore -- R/bart.R | dec-B81 |
| chg-U13 | dbarts() gains 12 formals and loses proposal.probs, which becomes a dbartsControl slot instead; control and sigma move later in the argument list | user | yes | dbarts | git diff main..bartcore -- R/dbarts.R | dec-B116 |
| chg-U14 | dbartsData() gains factors, na.action, bases and counts | user | no | dbartsData | git show main:R/data.R | |
| chg-U15 | Factor predictors default to single categorical or ordinal columns rather than indicator expansion on dbarts, dbartsData, bart and xbart; bartBT still expands | user | behavioural | factors | git diff main..bartcore -- R/data.R | dec-B78 |
| chg-U16 | Rows with missing predictors are kept and modelled rather than dropped by complete cases | user | behavioural | na.keepPredictors | git show main:R/data.R | dec-B01, dec-B108 |
| chg-U17 | bart keeps and models incomplete predictors under its na.keepPredictors default, where main's bart dropped the row by complete cases; bartBT keeps main's na.omit rule | user | behavioural | bart, bartBT | git diff main..bartcore -- R/bart.R | dec-B108 |
| chg-U18 | A test and training column-name mismatch is an error naming the missing columns; it was a warning with positional matching | user | yes | validateXTest | git show main:R/data.R | |
| chg-U19 | A named x with an unnamed test warns, naming the assumed mapping; main was silent because the warning was commented out | user | behavioural | dbartsPositionalArgsWarning | git show main:R/data.R | |
| chg-U20 | Weights of length neither n nor 1 error on the x and y interface instead of being recycled | user | yes | dbartsData | git show main:R/data.R | |
| chg-U21 | probit refuses weights, all-ones excepted; ordinal and negative binomial likewise; logistic requires positive integer counts, and the latent scaling by the inverse square root of the weight is gone | user | yes | enforceWeightPolicy, ProbitResponse | git diff main..bartcore -- R/spec.R; git show main:src/dbarts/bartFit.cpp | dec-B13 |
| chg-U22 | xbart keeps control as a formal, now merged with the flat formals under dec-B116's precedence rule rather than passed through; the name is unchanged from main | user | no | xbart | git diff main..bartcore -- R/xbart.R | dec-B77, dec-B116 |
| chg-U23 | xbart's sigma is renamed sigest | user | yes | xbart | git show main:R/xbart.R | dec-B80 |
| chg-U24 | xbart's n.burn default drops its third element, a length past two is refused, and chains are no longer carried between folds or replications | user | yes and behavioural | xbart | git show main:R/xbart.R | dec-B11, dec-B77 |
| chg-U25 | xbart gains 11 formals and loses sigma to the sigest rename, 22 to 32; drop, resid.prior and control shift position | user | yes | xbart | git diff main..bartcore -- R/xbart.R man/xbart.Rd | |
| chg-U26 | xbart's default k follows the front door for the response type: a fixed 2 for a continuous response and chi(1.5, 2) for a binary one, where main's binary arm inherited chi(1.25, Inf) | user | behavioural | xbart, resolveNodeHyperprior | git diff main..bartcore -- R/xbart.R man/xbart.Rd | dec-B102 |
| chg-U27 | xbart's threaded C++ fold loop is replaced by a cluster of parallel-package R workers, so the design matrix serializes per worker and the draw stream differs from main's | user | behavioural | xbart | git diff --stat main..bartcore -- src R/xbart.R | dec-B77 |
| chg-U28 | The sampler's startThreads and stopThreads become no-ops that warn once per session; threads are owned by each run | user | behavioural | dbartsSampler, dbartsTombstones | git show main:R/dbarts.R | dec-B76 |
| chg-U29 | The sampler's run method renames its fourth argument from numThreads to n.threads | user | yes | dbartsSampler run | git show main:R/dbarts.R | |
| chg-U30 | setResponse's formals become y, updateScale, updateState, status; a second unnamed argument warns once per session, so the ported call is warned rather than silent | user | behavioural | dbartsSampler setResponse | commits 504038c2, 9ae41bc1 | dec-B06 |
| chg-U31 | Fourteen mutators store state only on an explicit updateState = TRUE; NA no longer defers to the control slot, though run and the prior-draw methods still do | user | behavioural | dbartsSampler setData, setResponse, setOffset, setWeights | git show main:R/dbarts.R | dec-A14 |
| chg-U32 | 18 new reference-class methods, including forest, calibration, count, active-row and grow-from-root entries | user | no | dbartsSampler | git diff main..bartcore -- R/dbarts.R | |
| chg-U33 | getSigmas and getSumsOfSquaredResiduals refuse a result argument by name, where it was accepted and ignored | user | yes | dbartsSampler getSigmas | git diff main..bartcore -- R/dbarts.R | dec-B43 |
| chg-U34 | The sum of squared residuals is de-scaled by the square of the data range, not by the range | user | behavioural | dbartsSampler getSumsOfSquaredResiduals | git diff main..bartcore -- R/dbarts.R inst/NEWS.Rd | dec-B28 |
| chg-U35 | New reference-class field forestWeights | user | no | dbartsSampler | git diff main..bartcore -- R/dbarts.R | |
| chg-U36 | The data object's x and x.test slots change type from matrix to any, and it gains nine slots for missingness, the na.action, response type and levels, bases, counts and the two category offsets, 11 to 20 | user | yes for code type-checking the slot | dbartsData | git show main:R/A_class.R | dec-B35 |
| chg-U37 | The model object gains prior.scale and family and loses its four proposal-probability slots, which become one named dbartsControl slot; family is new on the model, not moved off the control | user | no for R callers, yes for a consumer reading the removed slots | dbartsModel | git show main:R/A_class.R | dec-B30 |
| chg-U38 | predict.bart's argument order changes, moving type ahead of offset and weights and adding ci.level, forest and bases before n.threads | user | yes | predict.bart | git show main:R/generics.R | dec-B07 |
| chg-U39 | fitted.bart inserts ci.level as its third argument, changing what a third positional argument means | user | behavioural | fitted.bart | git show main:R/generics.R | dec-A15 |
| chg-U40 | extract.bart gains loglik and forest types and forest and contribution formals | user | no | extract.bart | git diff main..bartcore -- R/generics.R | |
| chg-U41 | residuals.bart gains a type formal, defaulting to the expected value, so link-scale and posterior-predictive residuals become reachable | user | no | residuals.bart | git show main:R/generics.R | |
| chg-U42 | Foreign argument names on the fit generics are refused by name; anything else in dots warns once on the predict methods and is dropped on the others | user | yes | refuseUnusedGenericArgs, warnUnusedDots | commits 31cd2473, d48aef8a | dec-B09 |
| chg-U43 | extract with the trees type refuses sample, combineChains, forest and contribution; a numeric sample previously partial-matched sampleNums | user | yes | refuseTreesArguments | commit 8042cc2c | |
| chg-U44 | Fractional doubles are refused rather than truncated across 52 count and index arguments, at 75 call sites | user | yes | coerceOrError | commits d48aef8a, 77cad379, b337a9ca, 549f8589 | dec-B103 |
| chg-U45 | The fit object drops elements whose value is NULL, so absent components no longer appear in its names | user | behavioural | packageBartResults | git show main:R/bart.R | dec-A17 |
| chg-U46 | The fit object gains 16 components, 15 names to 31, the binary arm also returns y, and the chain count is always present | user | no | packageBartResults | git diff main..bartcore -- R/bart.R | |
| chg-U47 | 17 new exports and 55 new S3 methods for the new fit classes, two S3 methods removed, plus exported S4 classes and methods | user | no | NAMESPACE | git diff main..bartcore -- NAMESPACE | dec-A67, dec-B37 |
| chg-U48 | No posterior-package integration ships: no load hook registers methods, summary and draws() being in-package (chg-U82) | user | no | onAttach | git diff main..bartcore -- R/hooks.R | dec-B99 |
| chg-U49 | plot.bart saves and restores graphics parameters and errors when the fit has no training predictions | user | behavioural | plot.bart | commits c5f85804, fcbbc478 | |
| chg-U50 | print.bart prints a fit synopsis after the call rather than the call alone | user | behavioural | print.bart | git show main:R/generics.R | |
| chg-U51 | 22 new man pages and a new vignette on using the sampler as a component | user | no | man, vignettes | git diff --stat main..bartcore -- man vignettes | |
| chg-U52 | Six new response laws: logistic, ordinal, negative binomial and accelerated failure time join the ResponseFamily enum, multinomial softmax is a K-forest combiner and Student-t errors a residual distribution | user | no | ResponseFamily, TResponse | git diff --stat main..bartcore -- src | dec-B15, dec-B20 |
| chg-U53 | Four new leaf models: monotone-constrained constant, linear ridge over designated columns, Gaussian process, and a scale leaf for the variance forest | user | no | LeafModelKind, ConstantVarianceLeaf | git diff --stat main..bartcore -- src | dec-B16, dec-B17, dec-B22 |
| chg-U54 | Multi-forest couplings added: an amplitude combiner for causal and K-forest models and a multinomial softmax combiner | user | no | createAmplitudeSampler, createMultinomialSampler | git diff --stat main..bartcore -- src | dec-B18, dec-B19 |
| chg-U55 | Predictor ingestion widened: compressed-sparse-column predictors, categorical subset splits, missing-value incorporation with a per-rule missing direction, and an ordered-factor midpoint cut grid | user | behavioural for factor designs | PredictorSource, fillCutsAtLevelMidpoints | git diff --stat main..bartcore -- src | dec-B78, dec-A09 |
| chg-U56 | Categorical rules move from a 32-bit direction word to a 64-bit one with the missing direction at the top bit, plus a per-tree mask pool above 63 levels; the level cap rises to 65535 | user | behavioural | Rule | git show main:src/dbarts/node.hpp | dec-B91 |
| chg-U57 | New move kernels ship: perturb, the nog-node rule draw, the level-fibre Gibbs step, and a grow-from-root builder | user, opt-in | no | LevelGibbsMode | commits ab49f83a, 7fb166ca, 127f04ee, 8f9f46b4 | dec-A47, dec-A23, dec-B94 |
| chg-U58 | The change move carries the surviving proposal-density ratio, which main omitted | user | behavioural | changeMove | git show main:src/dbarts/changeRule.cpp | dec-B03 |
| chg-U59 | The initial forest is rejection-sampled until no leaf is empty, capped at 10000 attempts, where main drew from the prior and collapsed empty nodes | user | behavioural | Chain sampleTreesFromPrior, priorTreeDrawMaxAttempts | git show main:src/dbarts/bartFit.cpp | dec-A10 |
| chg-U60 | The residual-variance posterior degrees of freedom count rows with positive weight | user | behavioural | ChiSquaredScalePrior drawSigmaSqFromPosterior | git show main:src/dbarts/parameterPrior.cpp | dec-A11 |
| chg-U61 | The chi hyperprior on k uses shape one half of the sum of the leaf count and the degrees of freedom, where main took twice the degrees of freedom less one, and a hard cap on the draw is added | user | behavioural | ChiKHyperprior draw | git show main:src/dbarts/parameterPrior.cpp | dec-B04, dec-A13 |
| chg-U62 | Threading is rebuilt: chains run on standard threads with signals blocked in workers, within-chain reductions are serial, and results no longer depend on the thread count | user | behavioural | Sampler | git diff main..bartcore -- src/include/misc/thread.h | dec-B115 |
| chg-U63 | The model-matrix builder gains three errors and emits missing-valued indicator cells for missing factor codes, where main wrote zero into the cell and counted the code through an unguarded index | user | behavioural | makeModelMatrixFromDataFrame | git diff main..bartcore -- src/makeModelMatrixFromDataFrame.c | dec-A18 |
| chg-U64 | The core-count probe adds two BSD variants, drops a never-compiling fallback, and returns zero rather than -1 as the could-not-determine sentinel, which the R interface reads as a missing value | user | behavioural | guessNumCores | git diff main..bartcore -- src/guessNumCores.cpp | dec-A18 |
| chg-U65 | A pkgdown workflow is added, deploying the public documentation site on pushes to main and bartcore; main ships no workflow of its own | user | no | pkgdown workflow | git ls-tree bartcore -- .github/workflows | dec-A58 |
| chg-U66 | bart becomes the modern front door, carrying the former bart2 formals, defaults and body; bart2 is a one-release alias forwarding a matched call after a once-per-session warning, removed in 1.1-0 | user | yes | bart, bart2 | commits 417a7a41, 533cba8e; docs/plans/front-door.md landing note S1 | dec-B75 |
| chg-U67 | A new function, bartBT, carries 0.9-34's 31 formals byte-identical in name, order and default, with none of the bartcore-era extras and no family argument; its result is an ordinary bart-class object | user | no | bartBT | commit 417a7a41; docs/plans/front-door.md landing notes S1, S4 | dec-B75 |
| chg-U68 | A bart call naming any of 22 BayesTree spellings forwards the whole call to bartBT after a once-per-session warning; a fourth-or-later positional argument is refused naming both doors; attaching the package prints a startup message. All three go in 1.1-0 | user | behavioural | bart, warnOnce | commit 533cba8e; docs/plans/front-door.md landing note S1 | dec-B75 |
| chg-U69 | A bart call giving two or three positional arguments takes the modern door: a matrix and a vector are read as formula and data in backward-compatibility mode at the modern defaults, where main read them as x.train and y.train at 0.9-34's. The shim matches names, not positions | user | behavioural | bart | commit 417a7a41; man/bart.Rd formula item | dec-B75 |
| chg-U70 | bartBT refuses a factor response with three or more levels, its message naming both remedies; dbarts() and xbart refuse one too, naming bart(family = "multinomial"); bart itself fits it as a multinomial. 0.9-x coded the levels as numbers and fit them | user | yes | refuseLegacyFactorResponse | commits 417a7a41, ecb319aa | dec-B83 |
| chg-U71 | dbartsTombstones registers 38 names and arguments kept reachable for one release past a removal or rename; each either warns once per session or refuses outright, naming its successor where there is one, and man/dbarts-deprecated.Rd lists them. A retired name reaches that message instead of R's own unused-argument error | user | no | dbartsTombstones | commit 533cba8e; man/dbarts-deprecated.Rd | dec-B76 |
| chg-U72 | dbarts() and dbartsSpec() rename sigma to sigest; dbartsData's own sigma slot is unchanged | user | yes | dbarts, dbartsSpec | commit 282d1804 | dec-B80 |
| chg-U73 | Family settings ride family objects: dbartsFamilies names ten constructors, family takes a token or a call, and resid.dist and dispersion leave bart, dbarts and dbartsSpec, breaks and max.rows leave bart and dbarts, and dart leaves bart and xbart, all as tombstones; levelGibbs moves onto cgm() and dart() | user | yes | dbartsFamily, dbartsFamilies | commit c70aa99b; docs/plans/front-door.md landing note S2 | dec-B98, dec-B81 |
| chg-U74 | bart, dbarts and dbartsData gain na.action, defaulting to na.keepPredictors: a missing-response row is dropped and padded back onto fitted, missing predictors are kept and modelled. The missing formal retires with no tombstone; bartBT keeps 0.9-34's na.omit rule | user | yes and behavioural | na.keepPredictors, padOmittedRows | commit c70aa99b; docs/plans/front-door.md landing note S2 | dec-B108 |
| chg-U75 | A fit saved by 0.9-x, recognized by the absent state format field, is refused by name wherever a state is restored; there is no conversion path | user | yes | refuseLegacyState | commits 282d1804, ecb319aa | dec-B109 |
| chg-U76 | xbart distributes (replication, fold) units across workers and seeds each from the call's seed and its own index, so a seeded call reproduces bitwise at any n.threads; the whole dispatch runs under a preserved caller stream, leaving .Random.seed as it found it | user | behavioural | xbart, withPreservedSeed | commits 3fb7c2ac, cb5d4ef2; docs/plans/front-door.md landing note S3 | dec-B77 |
| chg-U77 | xbart's k takes a list mixing fixed values and hyperprior objects, the modelled cell labelled by its constructor call and swept last; an absent k runs one cell at the front door's own default for the response type, so a binary crossvalidation's default cell moves off the fixed 2 | user | behavioural | xbart, kGridLabel | commit c42b72af | dec-B102 |
| chg-U78 | xbart's three-element n.burn becomes a refusing tombstone naming the two-element form that replaced it; control is a formal again under dec-B116 and carries no tombstone | user | yes | dbartsTombstones | commit c42b72af | dec-B77, dec-B116 |
| chg-U79 | The formula interface takes a Surv left-hand side, which family = "auto" dispatches to aft; subset is honoured for both survival families; a hazard fit accepts a test set on either interface and survivalProbabilities reads its stored test draws with no keepTrees; xbart refuses a Surv formula response by name | user | no, and yes for the xbart refusal | dbartsData, hazardSurvivalProbabilities | commits 4526ebbc, df7b554c, fe06cb81; docs/plans/interfaces-and-dependencies.md landing note S2 | dec-B97 |
| chg-U80 | Sparse columns assigned into a data frame - sparseVector, dgCMatrix, sparseFactor - pass through the formula interface with no marker, lifted out around the model frame and re-attached row-subset under subset and na.action; a sparse name wrapped in poly(), ns(), log(), offset() or an interaction is refused | user | no | dbartsData, subsetSparseFactorRows | commit aa3c7b4e; docs/plans/interfaces-and-dependencies.md landing note S3 | dec-B100 |
| chg-U81 | A factors = "indicators" fit stores a factor's dummy expansion sparse past 100 levels, automatically and unmarked; tree structure, split counts and variable counts match the dense expansion but leaf-mean sufficient statistics differ in the last bit, the block landing in the rank-bitmap tier | user | behavioural | sparseIndicatorLevelCutoff | commit e950538d; docs/plans/interfaces-and-dependencies.md landing note S3 | dec-B100 |
| chg-U82 | summary computes rank-normalized split-Rhat and bulk and tail effective sample size in-package and always reports its nine statistics; the ten as_draws methods are deleted and a draws() generic returning the plain (iteration, chain, variable) array replaces them | user | yes | summariseDraws, draws | commits 15538877, 62311f95; docs/plans/interfaces-and-dependencies.md landing note S1 | dec-B99 |
| chg-U83 | dbartsControl's n.threads default becomes min(guessNumCores(), n.chains), and dbarts and bart warn once per fit, naming both counts, when an explicit budget exceeds n.chains | user | behavioural | dbartsControl | commit bcf42cc7 | dec-B115 |
| chg-U84 | Four engine limits become dbartsControl settings - categoricalExhaustiveCap, testFitParallelCutoff, predictParallelCutoff and sparseDensityThreshold - reaching every sampler kind through one bridge seam; setControl refuses a change to any of them on an existing sampler | user | no | dbartsControl, optionsFromParsed | commits 6f92e5c3, 9f841889 | dec-B91 |
| chg-U85 | predictParallelCutoff's default is recalibrated from the uncalibrated 1e7 to 50000 traversals, so a mid-size predict replay threads where it was serial; each worker owns its own output range, so the answer is bit-for-bit identical at any thread count | user | no, timing only | predictParallelCutoff | commit 6f92e5c3; docs/plans/engine-performance.md landing note S5 | dec-B91 |
| chg-U86 | sparseDensityThreshold buys memory and proposes identical splits but is not bitwise: a rank-stored column hands a leaf its rows in a different order, so fits and sigma drift in the last bits, about 1e-16 at draw 2 to 4e-15 at draw 200 | user | behavioural | sparseDensityThreshold | commits b7802bfa, 992da112 | dec-B91 |
| chg-U87 | n.cuts above 65533, the most a predictor grid can represent, is refused by name on the bridge path and in the engine store rather than silently clamped | user | yes | maxNumCutsRepresentable, ColumnStore | commits 89c16990, cc6248dd | dec-B91 |
| chg-U88 | A Gaussian-process fit tallies the leaf evaluations that exceeded max.leaf.size and were scored and drawn as constant leaves; the tally rides the fit as gp.fallback and a run above a quarter share warns once, through xbart and the multinomial, ordinal and negative-binomial doors as well | user | no | warnOnGPFallback | commits 02639c1a, 2626fcfe, cdecec90 | dec-B110 |
| chg-U89 | bart, dbarts and the sampler's run method gain callback, a compiled per-draw observer registered as a function and context external-pointer pair; dbartsControl gains keepFits over every per-observation channel, TRUE by default but FALSE automatically when a callback is supplied and keepFits is not itself named, so such a fit returns no fitted values | user | yes and behavioural | callback, keepFits | commits b91cabb9, 93b8bf36; docs/plans/per-draw-callbacks.md landing notes S1, S3 | dec-B114 |
| chg-U90 | Multinomial, ordinal, negative-binomial and hurdle-lognormal fits refuse an automatic or explicit keepFits = FALSE by name; every generic reading a dropped channel names the channel and the argument that dropped it rather than failing on a NULL | user | no | checkFamilyUnsupportedArgs, refuseDroppedForestChannel | commits 93b8bf36, e98cd224 | dec-B114 |
| chg-U91 | predict(type = "ppd") on a fit without keepTrees refuses by name rather than erroring on an invalid length; extract(type = "ppd") was already unaffected | user | yes | predict.bart | commit 013ab8bb | |
| chg-U92 | Packaging no longer holds a third full-size copy of yhat.train or yhat.test, the starting sigma skips lm's model frame and second design, dbartsData skips its complete-cases copy when no row is dropped, and fitted, predict, pdbart and pd2bart reduce in place rather than through apply. Peak memory falls by a whole prediction array - 1.6 GB at n = 1e5, four chains, 500 draws and 4.0 GB at n = 1e6 - plus 77 MB and 42 MB; every returned value is unchanged | user | no | packageBartResults, channelMeans, residualStandardError | commits 4e76f934, ee03fbcd, 6571eae1, 5a05d799 | |
| chg-U93 | The multi-worker run loop returns when the last chain finishes rather than on a 100ms tick: a 4-chain, 4-thread run(0, 1) drops from about 105ms to about 0.12ms median, draws unaffected | user | no | Sampler run | commit 6c6582bf; docs/plans/engine-performance.md landing note S3 | dec-B88 |
| chg-U94 | Weighted families take the fused residual-and-sum pass that served unweighted gaussian fits alone: per sampler iteration, 26 to 29 percent for weighted gaussian at n = 1e5, 17 for BCF, 7 for logistic. Their draws move in the last bits, the pass summing each leaf's statistic in a different order | user | behavioural | rollAndSetNodeAveragesFused | commit f0236082; docs/plans/engine-performance.md landing note, weighted fused pass | dec-B113 |
| chg-U95 | man/dbarts-package.Rd gains a Memory section giving a fit's peak footprint in data size, tree count, chains and kept draws, with the options that dominate it at large n | user | no | man/dbarts-package.Rd | commit 2b827c06; docs/plans/memory-footprint-audit.md landing note, steps 2-6 | |
| chg-U96 | bart() sheds the seven scalar formals that mirror slots on the prior objects - power, base, split.probs (cgm(), dart()); prior.scale (normal()); sigdf, sigquant (chisq()); and the tree-move mixture, proposal.probs - going from 51 formals to 45; each retired name rides consolidatedArgsFor$bart with a reason naming its object, warned once per session, k and sigest staying. proposal.probs becomes a dbartsControl slot and formal instead, read by the bridge from the control. bart() and xbart() (32 formals) each gain a control = dbarts::dbartsControl() formal, merged by one precedence rule: a flat formal the caller named wins, otherwise the control's slot is taken, and a control carrying a bartcore.* attribute is refused | user | yes | bart, xbart, dbartsControl | commits babe360c, 64278e15, 7fb7c498, 1a6da4d8, ee8a2ae2, 219aa0fe, b1d2ba6b; docs/plans/front-door-formals.md landing notes S1, S2 | dec-B116 |
| chg-U97 | dbartsSampler$setPredictor's re-partition takes the dense root in place instead of rewriting the identity order, so a predictor swap runs faster with sampling untouched; leaf sums reassociate at the last bit for every fit that calls it | user | behavioural | dbartsSampler$setPredictor, repartitionSubtree | commit 80b1c8d4; docs/plans/setpredictor-partition.md landing note | dec-B117 |

## 2. C-consumer-visible

| id | what changed | visibility | breaking | anchor | evidence | decision |
|---|---|---|---|---|---|---|
| chg-C01 | The C++ interface header is deleted; it declared 74 entry points over C++ types, 71 of them registered | C-consumer | yes | inst/include/dbarts/R_C_interface.hpp | git show main:inst/include/dbarts/R_C_interface.hpp; commit 21e43216 | dec-B84 |
| chg-C02 | The other 11 shipped headers are deleted: fit, control, integer types, data, model, random, results, scratch, state and the two generated type headers | C-consumer | yes | inst/include/dbarts | git ls-tree -r --name-only main -- inst/include | dec-B84 |
| chg-C03 | One flat header ships, declaring 25 entry points over an opaque sampler handle | C-consumer | yes | inst/include/dbarts/dbarts.h | commit 2fc833ea; commit d0403979 | dec-B84 |
| chg-C04 | Registered callable names change from unprefixed method names to the full symbol names; the 71 old names and the 25 new ones have zero overlap | C-consumer | yes | C_callMethods, DBARTS_API_REGISTER | git show main:src/R_interface.cpp | dec-C01 |
| chg-C05 | The generated integer-type header is no longer produced by configure; the new header uses only standard C and R headers | C-consumer | no | configure.ac | git show main:configure.ac | |
| chg-C06 | Results changes from a library-allocating C++ class a consumer subclassed into a caller-owned plain struct whose leading size field gates each member; a null member is skipped and a zero size is an error | C-consumer | yes | dbarts_results | git show main:inst/include/dbarts/results.hpp; commits e6786580, f7dd3f0e | dec-B59 |
| chg-C07 | The tree query leaves the C API with creation; it is reachable only as the sampler object's R getTrees method, which returns a data frame the caller protects and takes a forest index, where the C++ entry returned a heap structure the consumer freed field by field | C-consumer | yes | dbartsSampler getTrees | commits 3037ea39, 09d2dd83, 9df0cb50, d0403979 | dec-B29, dec-B84 |
| chg-C08 | setSigma takes one scalar for all chains rather than one per chain, and returns a capability status | C-consumer | yes | dbarts_sampler_setSigma | commit b638e6ba | dec-A32 |
| chg-C09 | setResponse takes an explicit scale-update argument and refuses it on multi-forest and heteroscedastic samplers, where main's always rescaled | C-consumer | behavioural | dbarts_sampler_setResponse | git show main:src/dbarts/bartFit.cpp; commit 504038c2 | dec-B40 |
| chg-C10 | setOffset's scale-update argument changes type and its return becomes a capability status | C-consumer | yes | dbarts_sampler_setOffset | commit 9df0cb50 | dec-A32 |
| chg-C11 | The two predict entries collapse into one taking a predictor-source struct and a per-call thread-count override, returning a capability status | C-consumer | yes | dbarts_sampler_predict | commits c44fcbc5, 09d2dd83, c85d6db6 | dec-B93 |
| chg-C12 | The predictor entries leave the C API with creation; the one that stays, predict, takes a self-describing size-first source struct covering dense column-major and sparse columns with an optional integer code channel, instead of a bare double pointer | C-consumer | yes | dbarts_predictor_source | commits d89994d0, a93c44ea, d0403979 | dec-A35, dec-B84 |
| chg-C13 | The three thread-lifetime entries are removed; setting the thread count, plus predict's per-call override, is the only thread control | C-consumer | yes | dbarts_sampler_setNumThreads | git show main:inst/include/dbarts/R_C_interface.hpp | dec-B76 |
| chg-C14 | The latent-store entry becomes a getter returning a capability status | C-consumer | yes | dbarts_sampler_getLatents | commit 9df0cb50 | dec-B21 |
| chg-C15 | The initial-summary printer is removed; a verbose control prints the summary inside R-side sampler creation | C-consumer | yes | createHolder | docs/design/public-surface.md | |
| chg-C16 | The 25 prior-object entries are removed with no replacement; priors arrive only through the R model object | C-consumer | yes | inst/include/dbarts/model.hpp | git show main:inst/include/dbarts/R_C_interface.hpp | dec-B84 |
| chg-C17 | Main's 16 lifecycle entries collapse to destroy alone; a consumer builds the sampler through dbarts's R interface and the R object owns its life | C-consumer | yes | dbarts_sampler_destroy | commits 2fc833ea, d0403979 | dec-B84 |
| chg-C18 | The generator-installation entry is removed; the C API has no way to install an RNG | C-consumer | yes | inst/include/dbarts/dbarts.h | git show main:inst/include/dbarts/R_C_interface.hpp | dec-A04 |
| chg-C19 | The tree-counting entry is removed with no flat-C replacement; the count column is replayed from the creation specification on the R route and is zero-filled for a sparse specification | C-consumer | yes | bartcore_getTrees | git diff main..bartcore -- src/R_interface_bartcore.hpp | |
| chg-C20 | The two per-observation predictor-update entries are removed from the C API and remain reachable only from R | C-consumer | yes | src/R_interface.cpp | git diff main..bartcore -- src/R_interface.cpp | |
| chg-C21 | Main's five test-side entries leave the C API entirely; the R route keeps a predictor setter and an offset setter, with a pairing rule that refuses a row-count change while an offset is installed | C-consumer | yes | bartcore_setTestPredictor | commits c9443220, d0403979 | dec-B84 |
| chg-C22 | New ABI types: two structs, three enumerations and a callback typedef | C-consumer | yes | dbarts_predictor_source, dbarts_draw, dbarts_family | commits 6446ddce, 51973ad3, 1c4f6936 | dec-B62 |
| chg-C23 | A version and handshake mechanism is added where main had none: a major and minor pair with accessors, a baked ABI hash with an accessor, and same-name caching stubs that enforce the version on first resolution and the hash only under an opt-in macro | C-consumer | yes | DBARTS_C_API_MAJOR, dbarts_apiHash, DBARTS_USE_STUBS | commits ef1936f9, 51973ad3, 67049a60 | dec-B56, dec-B111, dec-B60 |
| chg-C24 | Families and compositions beyond dbarts_family's enumerators are selected by twelve string-named attributes on the R control and model objects; creation left the C API, so only R sets them and the header documents none | C-consumer | yes | dbarts_family | commits 978ec12e, d0403979 | dec-B84 |
| chg-C25 | Four of the 25 entry points are called by neither known consumer and freeze at the release: dbarts_apiHash, dbarts_sampler_family, dbarts_sampler_setDrawCallback and dbarts_sampler_usesDart | C-consumer | no | DBARTS_C_API_LIST | git grep dbarts_ across the consumer checkouts | dec-B86 |
| chg-C26 | The engine's support library changes shape under the consumer boundary: five source files deleted, several public vector and matrix entries removed, gather indices narrowed to 32 bits, and fused and single-precision kernels added; the library is not shipped, so no consumer breaks | C-consumer | yes | src/include/misc | git diff main..bartcore -- src/include/misc src/misc/Makefile | dec-B24 |
| chg-C27 | The header becomes pure C: the four entries taking or returning R objects - create, getTrees, storeState, setState - are removed, the list trimming to 24 and the header declaring no R type and creating no sampler. A consumer builds the sampler in R and reads the handle out of the object's external pointer | C-consumer | yes | DBARTS_C_API_LIST, BartcoreHolder | commit d0403979; docs/plans/pure-c-header.md landing note S1 | dec-B84 |
| chg-C28 | setResponse and setOffset copy into buffers the sampler owns, sized once at creation; the sampler retains no caller pointer and allocates nothing after creation, so a caller's array is free on return | C-consumer | yes | adoptVector, createHolder | commit d0403979; docs/plans/pure-c-header.md landing note S1 | dec-B87 |
| chg-C29 | destroy survives the trim with two-owner semantics: it releases the engine sampler behind the handle and marks the R object's pointer dead, the R object then re-creating from a stored state or refusing, and a second call is a no-op | C-consumer | yes | dbarts_sampler_destroy | commit d0403979 | dec-B112 |
| chg-C30 | dbarts_sampler_setDrawCallback joins the entry list, taking it from 24 to 25, alongside the dbarts_draw struct and the dbarts_draw_callback typedef. The ABI token re-bakes to 0x6380bf095d5cae3f with the version pair held at 1/0, and dbarts_draw, the first ABI struct carrying by-value doubles, folds its scalars by name, position and width rather than by pointer-unit offset | C-consumer | yes | dbarts_draw, dbarts_sampler_setDrawCallback | commits 658f9f25, c5a77936; docs/plans/per-draw-callbacks.md landing note S2 | dec-B114 |
| chg-C31 | The two callback channels are independent: a hook installed through dbarts_sampler_setDrawCallback does not fire on the R run route, and the R callback argument does not reach a C-installed hook. One adapter serves both | C-consumer | no | ShippedDrawHook, fillShippedDraw | commit 658f9f25; docs/plans/per-draw-callbacks.md landing note S2 | dec-B114 |
| chg-C32 | A CI job reads the API hash and the version pair from the working tree and from the newest release tag and fails when the hash moved and the pair did not; with no matching tag it skips. It is the discipline that replaces the opt-in exact-ABI flag, which comes off both C consumers at the coordinated merge and stays off | C-consumer | no | tools/check-api-hash.sh | commits 5218d1a7, 439b6791; docs/plans/pure-c-header.md landing note S3 | dec-B111 |
| chg-C33 | The header, dbarts-embedding.Rd and the plan name the R route a consumer takes from an already-built control, model and data triple - new("dbartsSampler", control, model, data) then getPointer() - which neither dbarts() nor dbartsSpec() provides | C-consumer | no | dbarts-embedding.Rd | commits 205eed20, 56398169 | dec-B84 |

## 3. Build and install

| id | what changed | visibility | breaking | anchor | evidence | decision |
|---|---|---|---|---|---|---|
| chg-B01 | The C++ standard is raised to C++20 in both makevars files, and configure reads R's own C++20 settings and errors when the compiler setting is empty | build | yes | CXX_STD, configure.ac | commit b2083d5d | dec-B70 |
| chg-B02 | DESCRIPTION raises the R dependency from 3.1-0 to 4.2.0 and adds a C++20 system requirement | build | yes | DESCRIPTION | commits 7f965bd1, 72fc8b3e, 91982893 | dec-A48 |
| chg-B03 | Three configure arguments are removed and the cut-point integer width is hard-coded to 16 bits; the removed arguments stop configure with a message naming the removal rather than being ignored (chg-B11) | build | yes when a removed flag is passed | configure.ac | git diff main..bartcore -- configure.ac src/Makevars.in | dec-B107, dec-B91 |
| chg-B04 | Solaris support is removed wholesale, along with the byte-order probe and eight autoconf macro files | build | yes on Solaris | configure.ac | git diff main..bartcore -- configure.ac | dec-A49 |
| chg-B05 | The engine subdirectory, its generated configuration header and the generated integer-type header leave the build; the engine compiles directly into the bridge | build | no | SUBDIRS, AC_CONFIG_HEADERS | git diff main..bartcore -- configure.ac src/Makevars.in | dec-B92 |
| chg-B06 | Windows ARM64 support is added: a NEON flag keyed off the architecture string, hedged across three spellings, plus an archive-flag fallback and an MSVC intrinsic arm | build | no | src/Makevars.win, intrinsic.h | commits a1cf3c60, ce9802ea | dec-A57 |
| chg-B07 | DESCRIPTION suggests Matrix and survival, drops four author credits, and adds the documentation site to its URLs | build | no | DESCRIPTION | git diff main..bartcore -- DESCRIPTION | dec-B99, dec-B46 |
| chg-B08 | The build tarball excludes the documentation, benchmark, C++ test, site and backlog trees and the top-level tools scripts; tools/m4, tools/build-aux and tools/check-api-hash.sh still ship | build | no | .Rbuildignore | git diff main..bartcore -- .Rbuildignore | |
| chg-B09 | An --enable-reference-build configure flag, on Windows the DBARTS_REFERENCE_BUILD environment variable, compiles the scalar fixed-order draw-path kernels; dbarts:::buildInfo() reports build mode, compiled instruction sets and dispatch level | build | no | configure.ac, dbarts_buildInfo | commits 7641bdd5, eb7b137a; docs/plans/engine-performance.md landing note S1 | dec-B90 |
| chg-B10 | CI splits by build mode: the bitwise and seeded-snapshot gates run on a reference install with a mode assertion after each install, while tinytest, the exact-posterior gates, the sanitizers and statistical equivalence run on the shipped build; the four snapshot files exit on a shipped build and snapshot regeneration refuses one | build | no | .github/workflows/cpp-tests.yaml, tools/regenerate-snapshots.R | commits 6b4474b2, 167e2c62 | dec-B90 |
| chg-B11 | DESCRIPTION and the package page drop the BayesTree drop-in claim for "provides a BayesTree-compatible interface"; the three removed configure options become stubs that stop configure with a message naming the removal when passed, for one release, checked by a lint job on every push | build | yes when a removed flag is passed | configure.ac, man/dbarts-package.Rd | commits 045fad8e, a98c4cad; docs/plans/interfaces-and-dependencies.md landing note S4 | dec-B107 |
| chg-B12 | posterior leaves Suggests and both CI install lists; Matrix and survival stay | build | no | DESCRIPTION | commit 62311f95 | dec-B99 |

## 4. Internal but notable

| id | what changed | visibility | breaking | anchor | evidence | decision |
|---|---|---|---|---|---|---|
| chg-I01 | The engine is replaced rather than ported: main's pointer-node engine of 8718 lines is deleted and a header-only templated engine of 23617 lines added | internal, results user-visible | behavioural | src/dbarts to src/bartcore | git diff --stat main..bartcore -- src | dec-B92 |
| chg-I02 | Tree representation changes from pointer nodes with per-node availability and index arrays to a flat node arena with adjacent sibling pairs and one shared index buffer partitioned in place | internal | behavioural | tree.hpp | git show main:src/dbarts/node.hpp | |
| chg-I03 | The .Call table goes from 43 registrations to 63: 36 old names out, 56 new in, seven unchanged; the dynamic-library line is unchanged | internal | no | R_callMethods | git diff main..bartcore -- NAMESPACE src/R_interface.cpp | dec-C03 |
| chg-I04 | The cross-validation and grouped-fit .Call entries are removed; the unload finalizer is gone and the pointer registry and its mutex are replaced by per-pointer finalizers | internal | no | src/R_interface.cpp | commit 21e43216 | dec-A34 |
| chg-I05 | The support library is made host-agnostic: printing goes through function pointers the host installs | internal | no | src/misc/io.c | git ls-tree main -- src/misc/io.c returns nothing | dec-B85 |
| chg-I06 | The tinytest suite goes from 52 to 174 files, measured at 8539 assertions; 13 of main's test files are gone | internal | no | inst/tinytest | git diff --stat main..bartcore -- inst/tinytest; commits 1e5f80b2, edcdf735 | dec-A63 |
| chg-I07 | A C++ component suite is added: 17 translation units, about 274 test functions, 299 reported checks, with its own makefile | internal | no | tests/cpp/main.cpp | git diff --stat main..bartcore -- tests | |
| chg-I08 | A benchmark tree is added: 46 gate and benchmark scripts plus 10 surface scripts, 32 baseline files totalling 19 MB, kernels and results | internal | no | benchmarks | git diff --stat main..bartcore -- benchmarks | dec-A61, dec-A64 |
| chg-I09 | Continuous integration goes from one workflow to 12; five of them are schedule and dispatch only and cannot fire on their schedules from this branch | internal | no | .github/workflows | git diff --stat main..bartcore -- .github | dec-B65 |
| chg-I10 | Five maintenance scripts are added: a documentation-freshness guard, a stale-install detector, a reference-class documentation guard, a Windows configuration drift guard and the API hash check (chg-C32), plus a snapshot regenerator | internal | no | tools | git ls-tree bartcore -- tools | dec-A59, dec-C07 |
| chg-I11 | The shared test helpers grow from 6 to 17, including a parallel R handle layer kept out of the package namespace | internal | no | inst/common/bartcoreHandle.R | git ls-tree main -- inst/common | dec-A52 |
| chg-I12 | The test entry point hard-errors when tinytest is missing under CI instead of printing and passing | internal | behavioural | tests/tinytest.R | git diff main..bartcore -- tests/tinytest.R | |
| chg-I13 | Two research-only compile switches ship in the engine source; no CI job defines either | internal | no | src/bartcore/moves.hpp | grep of the source for the switch names | dec-C06 |
| chg-I14 | dbarts's own build binds the ABI at compile time with a signature-token assertion, a recomputed-hash assertion, 79 offset, size and alignment assertions, and one signature-binding assertion per entry | internal | no | src/C_interface.cpp | commits ef1936f9, 51973ad3 | dec-C02 |
| chg-I15 | The linear leaf's statistics cache is pruned at the draw and a store caps retained capacity at twice the membership, so the cache holds one leaf partition per tree rather than about three. At n = 1e5, 200 trees, one chain: peak resident set 689.0 MB to 518.2 MB, member lists 248.3 MB to 86.1 MB, the fit 1.8 percent slower on that cell; draws bitwise unchanged | internal | no | drawFromPosteriorForNode, storeCrossproduct | commit 3388dc15; docs/plans/memory-footprint-audit.md landing note, leaf-cache prune | |
| chg-I16 | The fused pass warms the tree's index permutation for the move phase that follows it, one prefetch hint per cache line: 6 to 16 percent on unweighted fits, with no value moved | internal | no | rollAndSetNodeAveragesFused | commits b5d4e3c8, 0b3fecac | dec-B113 |
| chg-I17 | The three equivalence baselines were re-recorded on the reference build at f0236082, the gaussian corpus reaching 52 scenarios after a Surv-on-formula aft case at 2085cba2 and a wide-factor auto-sparse case at deb144d2; the MANIFEST marks all three historical, superseded by chg-I21 | internal | no | benchmarks/baselines/MANIFEST | commits 2085cba2, deb144d2, 037fa9b6 | dec-B113 |
| chg-I18 | A memory-footprint benchmark records peak resident set per cell in its own subprocess and scores the design note's model over a 30-cell grid; the grid is re-recorded on a quiet host after the reductions above, at 3.7 percent median absolute relative residual with no cell outside its tolerance | internal | no | benchmarks/R/memory-footprint.R | commits 72430fbe, 9171ff64 | |
| chg-I19 | The tinytest suite reaches 174 files and 8539 assertions at the tip and the C++ component suite 299 checks, including a plain-C consumer that drives the shipped draw callback and a shared helper that compiles it once for two test files | internal | no | inst/common/capiConsumer.R | docs/plans/per-draw-callbacks.md landing note S4 | |
| chg-I20 | Two measurement harnesses join benchmarks: one per fixed engine constant, and callback modes on the sampler benchmark timing no callback, a no-op C callback and the running-mean recipe's own compiled copy | internal | no | benchmarks/R/constant-xint-caps.R, benchmarks/R/bench-sampler.R | commits cd2be9ff, 2f434014 | |
| chg-I21 | The three equivalence baselines are re-recorded at 80b1c8d4, superseding f0236082: the nine, six and six scenarios in each corpus that call setPredictor move at maximum absolute z 0.00 and every other scenario reproduces bitwise | internal | no | benchmarks/baselines/MANIFEST | commits bcf4f5fa, ea23d727; docs/plans/setpredictor-partition.md landing note | dec-B117 |

## 5. Consumer readiness

| package | compat branch | main branch | what main calls that bartcore removed or changed | compat status | pins and markers to remove at release | CRAN version, and whether CRAN's copy breaks |
|---|---|---|---|---|---|---|
| stan4bart | bartcore at dfae236, version 0.0-14; the sampler back end is replaced, so the merge replaces the package, not only its dbarts seam | main at 0.0-13 | 20 unprefixed callables resolved in its package init: the fit, control, data and model lifecycle set, setControl, predict, setResponse, setOffset, setSigma, the state pair, runSamplerWithResults, getTrees, printTrees, printInitialSummary, sampleTreesFromPrior and storeLatents. Includes five deleted C++ headers. Its formals-derived control filter also drops rngSeed silently, and the compat branch keeps that filter unchanged, so the drop is permanent rather than a merge-window symptom | re-ported twice since: at 4ace376 for the pure C header, both creation sites building the sampler in R and taking its handle, the restore path using setState and getTrees the R method; and at ef67969 for sigest. Its suite runs 542 of 542 at the branch tip. It calls 19 of the header's 25 entries and not the one the per-draw callback added, and the version pair is unchanged at 1/0, so its existing compiled binary is admitted against the re-baked hash | the branch lists, temporary dbarts install and reinstall steps in three workflows; the readme note. The exact-ABI flag is already out of both makevars files, dropped at 4ace376, though the release block still lists the step | 0.0-13, published 2026-08-21. Yes, loudly: its dependency admits 1.0-0 and its init resolves the old names, so loading it errors with R's function-not-provided message |
| bartCause | dbarts-1.0 at 3183c63, version 1.0-10; adds a causal fit function and routes grouped effects through stan4bart | main at 1.0-10 | rbart_vi at three sites, one of which evaluates it; a dangling cross-package documentation link; a control argument written into an xbart call object; the chain count read off the training-prediction array's dimension count; a three-positional do.call into predict | ported: the grouped route replaced, the xbart control dropped at 31519d2, the basis alignment fixed at 765a596, five files redirected from bart2 to dbarts::bart at b33ae4e, the chain count read off the fit rather than the prediction array at 13ad8e1, and bcf()'s n.threads default capped at n.chains at bdaae47 under dec-B115. Its suite runs 741 passing with none failing at the branch tip. No CI beyond a manual workflow | none in the sources; its stan4bart suggestion needs a version floor added, not removed | 1.0-10, published 2025-12-11. Yes, mixed: no upper bound on dbarts. Loud on the grouped route and on cross-validation; otherwise it fits and returns, recording one chain, with every draw moved by the default changes |
| treatSens | dbarts-1.0 at aeb4bb0, version 3.0-1, a CRAN resubmission | master at 3.0 | 14 unprefixed callables resolved lazily inside the analysis entry: the fit pair, the RNG-state setter, two run entries, setResponse, and the eight prior initialize and invalidate pairs. Includes nine deleted headers | re-ported at 59dfce0 for the pure C header - the same R creation route at its two sites, the per-file exact-ABI define dropped from three files - 184 of 184 and an unchanged package check; fixed again at aeb4bb0 after front-door S2 retired resid.dist, which its call had been dropping silently. It reaches dbarts:::parsePriors and dbarts:::estimateSigmaFromLinearModel through asNamespace, so an internal rename breaks it with no signature to catch it. No CI in the repository at all | nothing left to remove: the exact-ABI define came out of all three translation units at 59dfce0, though the release block still lists the step. Stale: its Windows makevars still cleans a header 1.0-0 no longer generates | archived on CRAN since 2019-04-07; no CRAN copy to break. A locally installed 3.0 fails at the first analysis call, not at load, because its lookups are lazy |
| bairrtt | none; main is already the compat state | main at 6167423, version 0.1.0 | nothing: every dbarts symbol it names is present on both branches with an unchanged signature | ported by construction; its dbarts floor already reads 1.0-0 | the branch pins in two workflows and the readme | not on CRAN |

## 6. Merge order and the intermediate states

Order is forced. Consumers cannot go first: two compat branches include the new flat header, which dbarts main does not ship, and all four consumer floors read a dbarts version no ref satisfies until the merge. dbarts is therefore first, and the only free variable is the lag before the consumer mains follow.

| step | action | install from GitHub, dbarts | install from GitHub, each consumer main | a CRAN consumer binary against a GitHub dbarts | consumer CI |
|---|---|---|---|---|---|
| 0 | today | fine, serves 0.9-34 | all four fine, given bairrtt's branch pin | fine | stan4bart and bairrtt green through their branch pins; bartCause manual only; treatSens none |
| 1 | pre-merge, no push to any main: re-bake the header, reinstall each C consumer from clean, run all four suites, push compat branches only. The exact-ABI flag is already out of all five consumer files, bartCause's bcf() thread default is capped, and the four sisters have been re-verified against the header at the tip; what the release block still owes here is settling treatSens's two internal reaches | unchanged | unchanged | unchanged | unchanged |
| 2 | merge dbarts bartcore into main and push. The window opens | serves 1.0-0. Scripts break loudly on rbart_vi, a three-element n.burn, a three-or-more-level factor response and fractional counts; the thread methods and the renamed arguments no longer error, the tombstone registry answering each with a once-per-session warning and the successor's behaviour. A 0.9-x xbart(control = ) call no longer errors either, control having returned as a formal under dec-B116. Silently on the proposal mixture, the binary k prior, categorical factors, missing-data incorporation, combined chains, setResponse's second positional argument, and a bart call giving two or three positional arguments, which now takes the modern door at the modern defaults | stan4bart and treatSens fail to compile, loudly, on a missing header. bartCause installs, is loud only on the grouped route and cross-validation, and is otherwise silently wrong on the chain count plus drifted by the default changes. bairrtt installs and every signature it calls is unchanged, but its draws move with the default proposal mixture and the prior-drawn initial forest | stan4bart 0.0-13 fails loudly at load; treatSens 3.0, local only, fails loudly at the first analysis call; bartCause 1.0-10 is mixed as above | still green: every pin names the branch, which still exists. dbarts's five dormant workflows fire on their schedules for the first time; the reverse-dependency matrix still names the compat branches, so it stays green |
| 3 | same session: merge each consumer's compat branch into its own main. The GitHub window closes | as above, the new normal | each consumer has been run green at its branch tip against the header at the tip - stan4bart at dfae236, bartCause at 3183c63, treatSens at aeb4bb0 - and the version pair has not moved off 1/0, so no compiled binary is refused | unchanged from step 2; a CRAN binary is not helped by a GitHub merge | unchanged |
| 4 | flip the refs and keep the install steps: dbarts's reverse-dependency matrix to each repository's main, the consumer workflows off the branch pin, and the branch notes out of both readmes | unchanged | unchanged | unchanged | green against dbarts main. The install step itself must stay while CRAN serves 0.9-34, or the resolver under-installs |
| 5 | submit dbarts 1.0-0 to CRAN; on acceptance submit stan4bart 0.0-14 and bartCause the same day | unchanged | unchanged | the unavoidable window: from CRAN accepting dbarts 1.0-0 until it accepts the two consumers, a package update alone breaks stan4bart 0.0-13 loudly at load and bartCause 1.0-10 partly silently | unchanged |
| 6 | when CRAN carries all of them: delete the temporary install and reinstall steps in stan4bart's three workflows and bairrtt's two; the branch notes went at step 4 | - | - | closed | steps deleted |

Two windows cannot be eliminated, only shortened. On GitHub, in steps 2 and 3, source installs of stan4bart and treatSens from their mains fail to compile; that is loud, and as short as doing all four merges in one sitting makes it, except for bartCause, which installs and runs wrong for the duration. On CRAN, in step 5, every user who upgrades dbarts before upgrading stan4bart or bartCause is broken; neither published DESCRIPTION bounds dbarts above, and CRAN accepts one submission at a time, so the two releases cannot land together.
