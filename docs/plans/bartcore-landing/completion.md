# Completion register: what is finished, what is not

Merged and deduplicated from the five code-side slices, the plans-versus-code sweep and the downstream sweep. Rows are grouped by status in this order: unfinished, abandoned, blocked, deferred with no maintainer evidence, deferred by the maintainer with a quote, done but with a stale gate, and done. `gate evidence` names the test file, C++ test, baseline, workflow or run that proves "done", and reads "none" otherwise. Ids are stable handles, not status codes: a row that changes status keeps the id it was first given. Names that are shorthand are unpacked in the row that uses them.

Branch tip is 992da112, a correction to one NEWS sentence; the last src/ commit is b7802bfa, the last inst/include commit e4063140 and the last R/ commit cdecec90, all dated 2026-09-10. Rows from cmp-U47, cmp-D25, cmp-V08 and cmp-K30 on come from the six arcs that landed after b7266da9.

## 1. Unfinished

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-U01 | Run the memory-check leg over the augmentation-helper tests on the first nightly after this branch reaches main | TODO augmentation-helpers; docs/plans/adoption-slate.md | unfinished | none | New C code behind two exported helpers that no memory-check host has ever seen |
| cmp-U02 | Protect the three sites that hold C++ heap owners across R's error longjmp | TODO capi-unwind-protect | unfinished | none; no unwind-protect call anywhere in the flat C API implementation | The sweep callback in the run entry, the chain RNG vector on an allocation failure, and engine throws from the predict fan-out and the chain header. None is reachable by an ordinary refusal |
| cmp-U03 | Bench an alternative to the adaptive-Simpson quadrature that dominates the monotone branch score | TODO monotone-leaf-quadrature | unfinished | none | 3227 of 3232 profile samples; no alternative benched |
| cmp-U04 | The unmeasured legs of the fused per-sweep pass: single-precision residual, move-phase gathers, vector leaves, causal and multinomial couplings, monotone, grow-from-root, and eight forests | TODO memory-wall-frontier | unfinished | the weighted leg landed at f0236082, measured 26 to 29 percent at n = 1e5 with its own re-record of all three baselines; the rest are still an x86-only throwaway patch | The weighted leg is out of this row. The x86 win on the remainder inverts on arm64, and the backlog demands an A and B comparison on both architectures before any arc lands; a size gate below the crossover is now its own post-release item, cmp-D26 |
| cmp-U05 | Decide whether the perturb move, which keeps a node's split variable and displaces only its cut, is removed from the kernel before release | TODO tree-mixing-proposals; docs/design/perturb-move.md | unfinished | benchmarks/R/perturb-balance.R proves detailed balance | Its pre-registered benefit stage ran and killed it at the registered weight; it ships at default weight zero and the backlog calls the removal an open maintainer decision |
| cmp-U06 | The equal-cost arm for the nog-node exact rule draw, on a quiet machine | TODO tree-mixing-proposals; docs/plans/mixing-program-report.md | unfinished | none | The equal-cost arm is the whole of this item and needs a quiet host. Slice 2's balance script is not owed: it landed at d888c9f3, and the backlog clause still naming it as owed was written an hour later at 5f93be0b and is stale |
| cmp-U07 | An R-readable per-sweep count of vetoed leaves | TODO veto-arc-doors | unfinished | none; no such counter in the engine | An additive channel; no work started |
| cmp-U08 | Three doors from the second whole-branch review: a negative-binomial rootogram panel, ordinal log-difference tail precision, and a negative-binomial burn-in dispersion channel | TODO second-review-followups | unfinished | inst/tinytest/test-plot-generics.R covers existing behaviour only | No decision recorded on any of the three |
| cmp-U09 | Three simulation-based-calibration arms: the monotone follow-on, the heteroscedastic lift through the state setter, and the full ladder re-runs | TODO sbc-family-tiers | unfinished | the calibration workflow carries seven arms, none of these | Monotone has been liftable since eafcb9a0; the heteroscedastic arm is liftable from R and was explicitly deferred |
| cmp-U10 | A calibration arm over an ordered-factor predictor above the cut cap, with an interior level absent from training | TODO sbc-ordered-factor-arm | unfinished | benchmarks/R/categorical-exact.R covers only a four-level single-tree shape | The midpoint cut grid changed the posterior for ordered factors and is calibrated only at a shape a user rarely hits |
| cmp-U11 | Slices 3 and 4 of the accelerated-failure-time arc: a prior draw of the variance forest, the heteroscedastic gaussian calibration arm, and then the heteroscedastic survival arm | docs/design/aft-status-setter.md | unfinished | none | Overlaps the variance-forest rescale item; the second is blocked behind the first. The design doc's own status line is stale: the matrix admission it says is proposed landed at 2c766437 |
| cmp-U12 | Three latent causal-forest equivalence scenarios, probit and logistic, plus the one baseline re-record they need | docs/plans/bcf-latent-evidence.md | unfinished | none | The exact gate, the derivation and the calibration measurement all landed; the scenarios remain proposed. Cheap by the plan's own reckoning, since appended scenarios perturb no stream |
| cmp-U13 | Re-record the causal-forest equivalence baseline on one host at the release-candidate tip | docs/plans/prerc-surface-freeze.md; docs/plans/bcf-cross-host.md | unfinished | the manifest now pins the causal-forest and multinomial recordings made at f0236082, re-recorded on the reference build with the weighted fused pass and reproduced bitwise on both builds | Done twice and superseded twice, at 3c81d6df and again at f0236082. No release candidate has been declared, so the item cannot close |
| cmp-U14 | Bump the DESCRIPTION date at the release candidate | TODO release block | unfinished | none | Mechanical, not done |
| cmp-U15 | Re-verify all four sister packages against the final re-baked header at the freeze | TODO release block | unfinished | none at the freeze | Each has been run green against an intermediate header since: stan4bart 458 of 458 at ef67969, bartCause 781 of 782 at b33ae4e with one unrelated failure, treatSens 184 of 184 at 6b274fb and fixed again at aeb4bb0. The release block adds two named items to the step: capping bartCause's bcf() thread default (cmp-U49) and settling treatSens's two internal reaches (cmp-U50) |
| cmp-U17 | On any post-release header change, bump the C API minor or major alongside the hash re-bake and move stan4bart's floor in the same release | TODO release block | unfinished | none; nothing in the build enforces the bump, only the re-bake | Process commitment, not code |
| cmp-U18 | Close the open GitHub issue at release and point the reporter at the new documentation | TODO release block | unfinished | none | The scale-update switch is recorded as resolving it |
| cmp-U19 | Re-run the full battery at submission: a package check on a clean tarball, the suite, and the three equivalence harnesses | TODO release block; docs/plans/archive/cran-readiness.md | unfinished | none at the branch tip | The last recorded green across every workflow, equivalence and exact-gates included, is at 046c35d7; commits landed after it, the engine's own last at b7802bfa |
| cmp-U20 | Recheck the CI action pins on each push and bump when a runtime is deprecated | TODO repo-modernization | unfinished | none | Standing hygiene, not blocking |
| cmp-U21 | Tighten the documentation-freshness guard so a cited symbol must occur inside the cited construct, and so it reads the man pages and vignettes too | TODO doc-freshness-guard-tightening | unfinished | none | The backlog calls it a door for a maintainer decision; nothing shows it has been put to VD. Tightening will surface pre-existing drift first |
| cmp-U22 | What the post-release adoption of the cut-only rule draw owes: name the restricted kernel on the user surface, run its harm controls, build a plateau-error cell, and flip the default with three baselines regenerated | docs/plans/mixing-program-report.md | unfinished | none | The restricted kernel is a private compile-time build today, selectable by nothing shipped |
| cmp-U23 | Two x86 vector-instruction items: the instruction-set inconsistency in configure and the do-not-bother list | docs/plans/x86-simd-plan.md | unfinished | none | The memo's top two recommendations landed; these two are open and carry no backlog entry. The third, a vectorized indexed sufficient statistic behind a toggle, was measured and declined under dec-B113 (cmp-D24) |
| cmp-U24 | The two-channel sub-decision for sparse ordered factors: which of a declared level count and a declared factor-levels entry wins | docs/plans/column-kind-consolidation.md | unfinished | none | Stated rather than fixed; benign today only because the grid takes the larger of the two |
| cmp-U25 | Mutation-test the shared state-continuation helper | docs/plans/review-2026-08-24/mutation-D-findings.md | unfinished | none | The paired test file has no assertions of its own; all 111 live in the shared helper, which no mutation leg has touched |
| cmp-U26 | Partial-dependence plots on the multinomial, ordinal, negative-binomial and hurdle fit classes | docs/plans/prerc-surface-freeze.md; R/partialDependence.R | unfinished | tinytest coverage of the refusal only | Refused by name; the man page documents only continuous and binary shapes. No backlog entry |
| cmp-U27 | The public linear-algebra entry points in the support library that no in-tree caller reaches, and two unreachable RNG kinds | code, by search across source, tests and benchmarks | unfinished | none | Some entries are reached only from the benchmark kernels; one is reached only from the model-matrix builder. The user-uniform and user-normal RNG kinds have no creator, since the only one always asks for Mersenne-Twister and the flat C API exposes no kind |
| cmp-U28 | The multinomial family is unreachable from bart, from the cross-validation entry, and from a formula | TODO multinomial-doors | unfinished | inst/tinytest/test-multinomial-generics.R covers what exists | Documented as refused by name, not as a gap |
| cmp-U29 | Composition questions left open in the shipped documentation: Student-t residuals with a variance forest, the sparse split prior with a drawn k, and variance-forest weight routing, all refused as unadjudicated rather than by design | man/dbarts.Rd; R/spec.R | unfinished | inst/tinytest/test-model-errors.R | Refusals, not fits |
| cmp-U30 | The mutation battery, full run | benchmarks/R/mutation-battery.R | unfinished | last full run recorded 2026-08-18 at 5939ea24: 23 entries, 20 killed, 3 surviving as documented | The battery now holds 25 entries, two added for the perturb and rule-draw moves and two removed; the current inventory has never been run end to end. Only the anchor check runs per push |
| cmp-U31 | Three documented untested paths in the state setter: a negative current-sample number, a negative skipped-updates count for the sparse split prior, and a warm-start donor forest's k length | benchmarks/R/mutation-battery.R, three entries marked as documented survivors | unfinished | none by design | The named killers are expected to miss them; a wrong-length k is silently truncated rather than refused |
| cmp-U32 | The state-setter and warm-start parsing validation clusters | benchmarks/R/mutation-battery.R comment | unfinished | none | Cited as 128 and 86 never-hit lines from a branch-reach file the same comment calls untracked; it is not in the repository, so the figures cannot be checked here |
| cmp-U33 | The core-count probe has no test | R/guessNumCores.R | unfinished | none | Zero references in the test suite; exercised only as a default argument. Its changed generic-Unix branch is unreachable on macOS, Linux and Windows, so no runner exercises it |
| cmp-U34 | Code-coverage measurement | none | unfinished | none | No coverage workflow and no coverage target; coverage claims come only from the mutation battery and an untracked session file |
| cmp-U35 | A build with the move-census instrumentation is compiled by no CI job | tests/cpp/Makefile extra-flags hook | unfinished | none | A C++ test file references it; a bit-rotted census build would not be caught until someone runs the census script |
| cmp-U36 | stan4bart's and bairrtt's CI still installs dbarts from this branch by name | stan4bart's three workflows; bairrtt's two | unfinished | none | The ref flips at the merge; the whole step dies only when 1.0-0 is on CRAN. The paired reinstall step exists because the cache is keyed by version and the branch never bumps 1.0-0 |
| cmp-U37 | dbarts's reverse-dependency matrix still names the three compat branches | .github/workflows/revdep-smoke.yaml | unfinished | none | The workflow's own comment says the refs flip to each repository's main at the lockstep release |
| cmp-U38 | bartCause's three basis-alignment failures | docs/plans/review-2026-08-24/memos/prerc-lens3-external.md | unfinished | a whole-suite run at bartCause b33ae4e, 781 of 782, the one failure an unrelated snapshot | Fixed at bartCause 765a596, which also rewrote the two assertions the memo said the count missed. The suite is green, but no run names these three, so the fix is proved only by the count |
| cmp-U39 | bartCause's suggestion of stan4bart carries no version floor | bartCause dbarts-1.0 DESCRIPTION | unfinished | none | Its grouped route now goes through stan4bart, and the CRAN stan4bart cannot serve it |
| cmp-U40 | stan4bart writes the cut count through an attribute on a control object whose cut count is now an integer S4 slot | stan4bart bartcore R/stan4bart_fit.R | unfinished | none; no test file mentions the cut count | The generic loop above it already copies the value with coercion; the attribute write then overwrites the slot with the raw value, so a double reaches an integer slot and the assignment that follows refuses it |
| cmp-U41 | treatSens has no CI on any branch | the repository has no workflow directory | unfinished | none | Its only automated coverage is dbarts's monthly reverse-dependency smoke test |
| cmp-U42 | treatSens 3.0-1 is a CRAN resubmission of a package archived in 2019 that has never been submitted | treatSens 97000d2, 78e11f0 | unfinished | a clean package check recorded at its tip | A resubmission is a new review, not an update, and is not in the release procedure |
| cmp-U43 | treatSens's Windows makevars still cleans a header 1.0-0 no longer generates | treatSens dbarts-1.0 src/Makevars.win | unfinished | none | Harmless, since the command tolerates a missing file, but it names a deleted header |
| cmp-U44 | bartCause has no per-push CI | its only workflow is a manual one, on both branches | unfinished | none | The R consumer with the silent failure mode is the one with the least automation |
| cmp-U45 | bartCause on CRAN silently records one chain against dbarts 1.0-0 | bartCause main R/bartc.R | unfinished | none | Only a bartCause release fixes it; there is no dbarts-side mitigation and no version bound that prevents the pairing |
| cmp-U46 | No CI anywhere exercises bartCause's grouped route | .github/workflows/revdep-smoke.yaml | unfinished | none | The smoke test installs hard dependencies only, and stan4bart is a suggestion of bartCause; the workflow's own comment gives the reason |
| cmp-K01 | Memory-check gate, a passing run | .github/workflows/valgrind.yaml | unfinished | none; the only run, 34044550541 of 2026-09-06, failed with 48 bytes definitely lost in the predictor-source columns by way of the test-missingness refusal on the predict entry, and was never re-run | The leak is fixed at fdba3809, but no run proves the gate, in CI or on the record locally, so the merge is its first real run. The workflow's nightly schedule cannot fire from a non-default branch |
| cmp-U47 | Decide whether the four new engine settings stay control-only or gain their own bart() formals | docs/plans/engine-performance.md landing note, S5 | unfinished | none | Recorded at the landing as agent-made and pending VD. The reviewer recommends keeping the three cutoffs control-only and, if bart() reach is wanted for the categorical enumeration cap, putting it on cgm() as levelGibbs already is, rather than adding four formals a later decision removed the licence for |
| cmp-U48 | categoricalExhaustiveCap's validity ceiling of 30 admits a 2^29-candidate exact enumeration | TODO engine constants audit | unfinished | none | The manual's recommended range of 8 to 14 warns against going that high; the validity message, which only refuses above 30, does not |
| cmp-U49 | Cap bartCause's bcf() thread default at the smaller of the core probe and its own chain count | TODO release block | unfinished | none | Its default hands guessNumCores() to a fit carrying its own n.chains, so under dec-B115 every multi-core bcf() fit warns about the excess. Its own branch, not dbarts's |
| cmp-U50 | A public route to the resolved prior quadruple and to the creation-time sigma estimate, so treatSens need not reach two internals through asNamespace | TODO release block | unfinished | none | Recorded post-release. Its resid.dist call broke silently when the family-objects slice retired the formal, fixed on its branch at aeb4bb0; no dbarts-side gate would have caught it, an internal name carrying no signature promise |

## 2. Abandoned

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-X01 | A non-conjugate move strategy for Gaussian-process leaves under non-Gaussian likelihoods, phase 6 of the founding engine design | docs/design/gp-leaves.md; docs/design/core-generalization.md | abandoned | none | Designed, not built, and carries no backlog entry; the Gaussian-process follow-ups entry covers only lengthscales and low-rank kernels |
| cmp-X02 | The causal-forest treatment-scale joint rescale, a likelihood-invariant move on the treatment forest's amplitude and leaf values | docs/plans/archive/bcf-b-ridge.md | abandoned | none; the combiner ships with that forest's ridge flag off | Derivation complete and prototype-checked, never implemented, no backlog entry. The prognostic twin landed at 5109b669. The plan is indexed as a no-go while its own later note recommends landing the move later; neither verdict is tracked |
| cmp-X03 | The post-1.0 additive surface recorded by rule: flat-C readers for the heteroscedastic channel, ordinal cutpoints and a sigma getter; data-handle serialization; variable-selection inference; random-effects breadth | docs/plans/prerc-surface-freeze.md | abandoned | none; the header has a sigma setter and no getter, and no heteroscedastic or cutpoint reader | Recorded once and never given a backlog entry; nothing tracks them. One item is moot since the grouped path was deleted |
| cmp-X04 | Cross-repository sanitizer contract CI | docs/plans/archive/capi-dispatch-table.md | abandoned | none | Landed at 9fe39856 then removed at 99b356d8 the same day; the drop is a recorded decision, dec-A36, so this row alone of the four carries one |

## 3. Blocked

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-L01 | VD's own read of the merge review, then the release-candidate declaration | TODO rc-gate; docs/plans/bartcore-review-tour.md | blocked | none | Standing gate naming VD; the tour is the gate document. The same design doc names two further release prerequisites, cmp-L05 and cmp-L06, which gate the CRAN submission rather than the merge |
| cmp-L02 | Validate the five schedule-only workflows on their first live runs after the merge | TODO release block | blocked | none; the schedule and dispatch triggers cannot fire from a non-default branch | Explicitly held pending VD's own review of the branch |
| cmp-L03 | Registering the schedule-only CI legs on the default branch | docs/plans/release-candidate-review.md | blocked | none | Recorded as VD's commit to make or delegate; the memory-check workflow's own header says to validate the schedule when this branch merges |
| cmp-L04 | Give the nog-node exact rule draw, which draws a split rule at an interior node whose two children are both leaves, a nonzero default share | TODO tree-mixing-proposals; docs/design/nog-gibbs.md | blocked | benchmarks/R/rule-gibbs-balance.R, landed at d888c9f3 | It clears the effective-sample-size bar by 22 against a bar of 8 but regresses 95 percent coverage by 0.024 against a margin of 0.010, which blocks the flip |
| cmp-L05 | stan4bart's random-effect scale mixing bar: lag-one autocorrelation below 0.8 and an effective sample size of at least 100 per 1000 draws | docs/plans/retire-grouped-random-effects.md | blocked | none; sister-repository work | Named a release prerequisite of the grouped-effects removal, so it gates the CRAN submission rather than the merge. Both remedies the design doc names are recorded unavailable as stated; the interweaving remedy prototyped in the tau review is now stan4bart's to build. dec-B105 makes it the condition on the removal itself and names the fallback if it cannot be met: an R-only grouped intercept driving the plain sampler through setOffset each sweep, with no engine block |
| cmp-L06 | A bartCause grouped-data route that does not go through the removed grouped fit function | docs/plans/retire-grouped-random-effects.md | blocked | the reverse-dependency smoke test's bartCause leg is an accepted red | The one known consumer has no working path at this tip; the same design doc names this a release prerequisite, gating the CRAN submission rather than the merge |
| cmp-L07 | Push dbarts 1.0-0 and stan4bart 0.0-14 and submit both to CRAN | TODO release block | blocked | none | VD's action by the standing scope rule. CRAN reviews one submission at a time, so "together" is not available |
| cmp-L08 | Contact lorax's maintainer: its example and tests fit bart() with a three-level factor response, which 0.9-x silently coded as the numbers 0, 1, 2 and 1.0-0 refuses with a pointer to bart2(family = "multinomial") | docs/plans/archive/cran-readiness.md, revdep sweep with Suggests; reproduced at the tip 2026-09-08 | blocked | none; sister-author action | VD 2026-09-08: let it break and ask the author to adjust the example or test. lorax lists dbarts in Suggests, so its CRAN check fails once dbarts 1.0-0 is on CRAN |
| cmp-L09 | insight's dbarts test fits bart2 on the base penguins data, whose response has NAs, and expects the NA rows dropped; 1.0-0 errors "response contains missing values" | docs/plans/archive/cran-readiness.md, revdep sweep with Suggests; reproduced at the tip 2026-09-08 against insight 1.5.4 | deferred | none | The test is skipped on CRAN, so only a local or CI run of insight sees it. The archived sweep left "let it break or soften" to VD; no ruling recorded. Softening would mean bart2 dropping NA-response rows as 0.9-x did, dec-A70 |

## 4. Deferred, no maintainer evidence

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-D01 | Fix the frozen-structure mixing in roughly the extreme 3 percent of the causal-forest prior, where no amount of burn-in helps | TODO bcf-sigma-tail-mixing | deferred | none | Research door, no plan doc; reachable only with a stale build scale swapped against a new response inside an outer sampler |
| cmp-D02 | Allow a single varying-coefficient forest with no prognostic partner | TODO binary-kforest-k1-reachability | deferred | none | The engine already takes it; the R specification layer refuses fewer than two forests. Gated on acceptance evidence a new shipped configuration would owe |
| cmp-D03 | Exact serially-correlated errors around a BART mean | TODO correlated-outcomes | deferred | none | The multivariate half shipped in stan4bart with no dbarts change; the serial half is an engine door that opens on a consumer |
| cmp-D04 | Re-record the two causal-forest and multinomial equivalence harnesses against independent per-scenario seeds | TODO equivalence-harness-seeds-axis | deferred | none | Recorded post-release; the current bar tolerates a shift of about 0.9 posterior standard deviations per cell |
| cmp-D05 | Sampled Gaussian-process lengthscales and low-rank kernels; the decision memo comes first | TODO gp-followups | deferred | none | Memo never written. The plan says it is blocked on the flat state format, which has since landed and moved on a version. Separately, dec-B110's constant-leaf fallback tally and warning landed, so a degenerate GP fit now reports itself whatever the cause |
| cmp-D06 | Use the shared cut scan for exact informed birth and death proposals | TODO grow-from-root | deferred | none | The scan ships and is shared with the root-down builder; the Metropolis birth still draws its rule from the prior |
| cmp-D07 | Run the feature-matrix oracle script once at the release candidate, and grow the calibration workflow past its subset | TODO manual-gate-runs | deferred | none | Both recorded as manual by decision |
| cmp-D08 | Whole-data replacement on a multi-forest sampler, and per-forest row subsetting by physical compaction | TODO multiforest-mutation-gaps | deferred | none | The backlog records a maintainer note to revisit on a second consumer or a release want; the value gate was lifted by a published mixture-cure survival model |
| cmp-D09 | Entry points for the multinomial family: a formula interface, a flat-C creation path, and a family token in bart and in the cross-validation entry | TODO multinomial-doors | deferred | inst/tinytest/test-multinomial-generics.R covers what exists | The backlog stamps it post-release-candidate with a date and VD's initials but no fork, so it is deferred rather than deferred by VD. The cross-validation entry additionally needs a multinomial loss; the enumerator exists and sampler creation refuses it |
| cmp-D10 | A Python binding over the engine | TODO python-bindings | deferred | none | No spike was ever run; the plan's premise, that the engine is R-free below the bridge, is contradicted by the code |
| cmp-D11 | Unscheduled mixing mechanisms: rotation, a generator-only regrow census, the re-specified reallocation census, heated companion chains, and the residue of two brainstorm rounds | TODO tree-mixing-proposals | deferred | none | Recorded, ranked, not scheduled |
| cmp-D12 | Rescale the variance forest's scale leaf when the response scale moves, and add a prior draw of the variance forest | TODO variance-forest-mutation-routing | deferred | none | The leaf is calibrated once at creation, so a response or offset swap with a scale update leaves the prior on the old scale; the rescale factor is recorded as unverified |
| cmp-D13 | Align the prior-draw path's all-zero bare-root branch with the mutate path's prior draw | TODO veto-arc-doors | deferred | none | Recorded as a deliberate disagreement between two laws; no baseline reaches the branch |
| cmp-D14 | Drop the once-per-session warning on a positionally-supplied second argument to the response setter | TODO setresponse-positional-arg-warning | deferred | none | Scheduled for 1.2-0 by a date-stamped backlog line naming VD with no fork, so it is deferred rather than deferred by VD |
| cmp-D15 | Notify the tmle package maintainers that weighted binomial analyses lose the dbarts learners by design | TODO release block | deferred | none | Recorded as an optional courtesy |
| cmp-D16 | Active-row-mask evidence for the latent causal-forest arms, a bitwise pair rather than a posterior gate | docs/plans/bcf-latent-evidence.md; docs/design/active-rows-mask.md | deferred | none | Explicitly scoped out; it is what remains of the feature matrix's gap row for the latent sub-families |
| cmp-D17 | Doors held open from the R and C++ adoption slate: a fit-shift accessor matching the fit-scale one, promoting the combined-fits accessor to public, raw random-number primitives on the header, and a grouped-swap equivalence scenario | docs/plans/adoption-slate.md | deferred | none | Recorded, not scheduled; the two grouped items are moot since that path was deleted |
| cmp-D18 | The three tier-5 performance findings from the July package review: leaf-fits scatter elimination, dropping a redundant sufficient statistic, and the multinomial margin cost | docs/plans/archive/package-review-remediation.md | deferred | a later follow-up plan landed a subset | The plan says they were recorded as backlog items; none of the three appears in the current backlog |
| cmp-D19 | The second whole-branch review's deferred bin of 11 items: reading-cost duplication clusters, unpinned strings, equivalent mutants and reach gaps | docs/plans/review-2026-08-24/consolidated-report.md | deferred | none | Several have since been fixed independently; the bin itself was never re-swept |
| cmp-D20 | Calibration waivers: the negative-binomial dispersion and aggregate functionals are waived by flag | .github/workflows/sbc.yaml | deferred | that arm passes with the waiver | Adjudicated as an identifiability ridge rather than a defect; the waiver is per functional, with no VD record |
| cmp-D21 | Calibration arms not in the matrix: four causal-forest variants, discrete-time hazard, hurdle, heteroscedastic and monotone | .github/workflows/sbc.yaml header | deferred | none | The causal arms are recorded slow-mixing findings; the other four are stated ill-posed or not liftable |
| cmp-D22 | Research and benchmark scripts with no CI gate: the sampler benchmark, the composition matrix, the Geweke check, the move census and the nine survey scripts | benchmarks/R | deferred | none | The sampler benchmark is maintainer-run by policy; the Geweke check is cited as an oracle in a baseline manifest row but runs in no workflow |
| cmp-D23 | Additive escalations recorded but not built for the C API handshake: a load-time lookup canary and one other | docs/plans/archive/capi-dispatch-table.md | deferred | none | Recorded as adopt-later with no freeze cost |
| cmp-D25 | Make the sparse layout bitwise: the sparse root path would write the identity order before partitioning, about three engine lines | TODO engine constants audit | deferred | the CI diagnosis behind b7802bfa: tree shapes, split counts, split variables and variable counts bit-for-bit equal over 200 draws, the fits drifting from about 1e-16 to 4e-15 | Recorded post-release as a decision rather than a defect. Taking it moves the rank-tier draws and forces a re-record of the gaussian corpus's wide-factor auto-sparse scenario |
| cmp-D26 | A size gate on the fused roll and sufficient-statistic pass, two thresholds rather than one: fuse weighted at n >= 1e4, unweighted at n >= 2e4 | TODO engine constants audit; docs/plans/engine-performance.md | deferred | the crossover grid recorded with the weighted pass | Below the crossover the fusion loses up to 8 percent and above it wins up to 24, and one threshold cannot serve both families. It must arrive WITH equivalence scenarios sized above the threshold: every fit in all three corpora is under 2000 rows, so a gate today would revert all 75 to the stock association and leave the fused pass with no equivalence coverage at all |
| cmp-D27 | The last full-size copy of a prediction channel, which needs the bridge to allocate it draw-major and the engine to write each draw strided | TODO memory footprint at large n | deferred | none | The R route reached the two-array floor at the audit's step 7; going below it is a draw-path engine change, outside that arc's own no-engine-change constraint, and the backlog holds it for an entry of its own |

## 5. Deferred by the maintainer

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-V01 | Adopt the cut-only exact rule draw at a mixture weight of 0.16, taken out of the change move's share, after the first release | docs/plans/mixing-program-report.md; docs/design/nog-gibbs.md | deferred-by-VD | benchmark arms recorded at 5f93be0b | Recorded as a maintainer choice among four measured arms, control, cut-only, the full draw and the equal-cost arm: "Ruled 2026-09-08: the maintainer adopts the cut-only kernel at `d` = 0.16 to land after the first release". Settles decision row dec-B72, which dec-B96 then supersedes: the 0.16 is recorded as an agent's suggestion adopted without a rationale, the kernel stays at weight zero through the merge, and the mixing research that resumes after it sets the weight |
| cmp-V02 | Real-valued negative-binomial dispersion, which needs a non-integer-shape Polya-Gamma draw | TODO negbin-real-dispersion; docs/design/negative-binomial.md | deferred-by-VD | inst/tinytest/test-nbinom.R pins the integer envelope | "Approximate Polya-Gamma DECLINED for 1.0"; the seam is in place in the model header. Settles decision row dec-B14 |
| cmp-V03 | Arbitrary real observation weights on the binary families | TODO weighted-binary | deferred-by-VD | none | Shares the declined approximate-augmentation unlock with the row above; probit refuses weights outright. Settles decision row dec-B14 |
| cmp-V04 | Sparse-column extensions: a linear leaf over sparse designs, a streaming range kernel, per-column narrow widths, whole-data replacement on a sparse store, and per-observation sparse mutation | TODO sparse-extensions | deferred-by-VD | inst/tinytest/test-data-sparse.R, test-data-mixed-mutation.R | "Settled deferrals (VD 2026-08-24): group.by on bart2/dbarts; survival entry=; sparse-extensions' rbart_vi and linear-leaf", recorded as a window rather than a lock-in |
| cmp-V05 | Survival follow-ups: delayed entry, competing risks, long-format ingestion for time-varying covariates, and a complementary log-log link | TODO survival-followups; docs/design/survival.md | deferred-by-VD | none | The delayed-entry half is named in the same 2026-08-24 walkthrough as deferred post-1.0, window not lock-in. Competing risks moves with reopening the multinomial counts-mutation door |
| cmp-V06 | The fit-time test-basis channel | docs/plans/release-candidate-review.md | deferred-by-VD | none | Deferred post-1.0 in the same walkthrough, on the ground that bartCause's counterfactual arms are already served at fit time |
| cmp-V07 | Formal heredity, an ensemble lattice prior, as a fourth interaction formal; soft path-dependent penalties stay gated | TODO interaction-constraints; docs/plans/pre-review-cleanup.md | deferred-by-VD | none | "Scheduled as the first post-1.0 arc (VD 2026-08-26), not pre-RC. Soft path-dependent penalties: gate STANDS", and fork 14 of the pre-review rulings. Settles decision row dec-B71, narrowed by dec-B95: heredity happens, with no position in the queue, and the manual states the two-stage workaround meanwhile |
| cmp-V08 | Restore xbart's warm starts across folds, opt-in, if the timing against 0.9-34 proves bad | dec-B77 | deferred-by-VD | the S3 landing's wall time: 2.53x over serial at four threads for a 10-fold, one-replication call, results bitwise identical between thread counts | Conditional in the ruling's own words, with the least invasive shape named. No timing against 0.9-34 is recorded, so the condition has never been tested |
| cmp-V09 | Confirm or change the binary node hyperprior default on additional evidence: a checked-in harness, the degrees of freedom varied, and a much wider range of use cases than four simulated data-generating processes, real datasets included | dec-B106 | deferred-by-VD | none; no such harness is in the tree | Scheduled before 1.0-0 if time allows. The July study rejects the infinite scale clearly, prefers scale 2 over 5 narrowly, and says nothing about the degrees of freedom |

## 6. Done, with a stale gate

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-S01 | Protect-balance check | .github/workflows/rchk.yaml | done-but-stale-gate | run 34044550523, 2026-09-06, failed with eight unprotected-pointer findings in the model-matrix builder and one suspicious call in the bridge | Fixed at 916271f3, whose body records a local re-run at zero findings over 15534 functions; no CI re-run, and the schedule cannot fire from this branch |
| cmp-S02 | Gaussian statistical equivalence over 50 scenarios | .github/workflows/equivalence.yaml | done-but-stale-gate | CI green on 046c35d7 against the re-recorded equivalence-f0236082.rds, 52 scenarios, the corpus having gained a Surv-on-formula aft case and a wide-factor auto-sparse case | The gate re-ran and the corpus grew. Engine commits landed after it, the last at b7802bfa, so the row is stale again by three |
| cmp-S03 | Causal-forest cross-host equivalence, and the multinomial equivalence twin | .github/workflows/exact-gates.yaml | done-but-stale-gate | CI green on 046c35d7 against bcf-equivalence-f0236082.rds and multinomial-equivalence-f0236082.rds, 12 compared and 11 compared, 0 skipped, both moved at maximum absolute z 0.00 by the weighted fused pass and re-recorded on the reference build | Both baselines were re-recorded, so the fbff1989 pins are demoted. Engine commits landed after the run, the last at b7802bfa |
| cmp-S04 | Gaussian bitwise equivalence at the tip | commit bodies of 3ecd7f47, ab49f83a, 7fb166ca, cbe80534, 127f04ee | done-but-stale-gate | those commit bodies: "the canonical baselines replay bitwise" | A maintainer-run local claim only; no CI log or artifact. Three other engine commits make no such claim |
| cmp-S05 | tinytest suite, package check on five platforms, C++ component tests, and both sanitizers | inst/tinytest, .github/workflows | done-but-stale-gate | the arcs' own landing runs: tinytest 8403 of 8403 at the engine-constants slice and 8351 of 8351 at the callback slice, tests/cpp 299 checks, address, undefined-behaviour and thread sanitizers clean, and R CMD check --as-cran OK from a clean tarball at each | Every landing since b7266da9 records its own run, so no engine change is unexercised; what is not recorded is a run at the branch tip itself |
| cmp-S06 | The 26 exact-posterior and detailed-balance gate scripts | .github/workflows/exact-gates.yaml | done-but-stale-gate | run 34231841167 at 7ffe9032, 2026-09-08, all 26 passing in quick mode | The full mode is dispatch-only and has no recorded post-tip run |
| cmp-S07 | The simulation-based-calibration matrix, seven arms | .github/workflows/sbc.yaml | done-but-stale-gate | run 34145202758 at 7c1241eb: six arms passing, the discrete self-check cancelled on its ten-minute timeout | The same arm finished in under three minutes in an earlier run; the timeout is marginal and unchanged at the tip, and the run is six engine commits stale |
| cmp-S08 | Reverse-dependency smoke test over stan4bart, bartCause and treatSens | .github/workflows/revdep-smoke.yaml | done-but-stale-gate | run 34046193643, 2026-09-06, success | Pinned to the three compat branches, not to CRAN; the refs must flip at the lockstep release |
| cmp-S09 | CRAN reverse-dependency sweep: every package declaring dbarts checked against a 1.0-0 build | docs/plans/archive/cran-readiness.md, revdep sweep with Suggests | done-but-stale-gate | 22 of 24 OK on 2026-07-25 at an engine well before the tip; lorax and insight the two breaks | No sweep script is in the tree. CRAN now lists 27 packages declaring dbarts, so three have appeared since the sweep and have never been checked. The CI smoke test covers only the three sister packages |

## 7. Done

| id | item | source | status | gate evidence | note |
|---|---|---|---|---|---|
| cmp-K02 | Windows ARM64 vector-instruction leg | .github/workflows/check-standard.yaml | done, narrow scope | the same five-platform run; the job asserts the architecture string and runs the vector-instruction test file only | Not a full package check on that platform |
| cmp-K03 | Speed baseline at the engine tip | benchmarks/baselines/bench-sampler-127f04ee.csv | done | the baseline manifest row: recorded 2026-09-08 at the engine tip on a quiet-machine grant, median of three runs, two same-window comparisons within 2 percent | The only baseline whose commit is the engine tip; it records a 3.2 percent regression on the embedded-offset step, under the gate |
| cmp-K04 | Lint, formatting, Windows configuration drift, reference-class documentation against live formals, and mutation-anchor drift | .github/workflows/lint.yaml | done | run 34193247355 at d0bde553, success | Four separate scripted checks |
| cmp-K05 | Design and plan documentation freshness | .github/workflows/doc-freshness.yaml | done | run 34245026502 at b7266da9, success, and every arc that landed since records check-doc-freshness.R exit 0 in its own landing note | The only workflow with no path filter |
| cmp-K06 | Deterministic per-push gates: package check, C++ tests, exact gates, sanitizers, lint and documentation freshness | .github/workflows | done | the workflows themselves, on push and pull request for main and this branch | Already gating every push on this branch |
| cmp-K07 | All 48 C entry points exercised by a real compiled consumer | inst/tinytest/test-capi.R | done | that file, 2318 lines, plus a 1482-line consumer source in which every one of the 48 names appears | The test compiles the consumer against the installed headers, uses the generated stubs, keeps one raw-lookup canary, skips locally when no toolchain is present and errors under CI. Only the header-internal check function is uncalled |
| cmp-K08 | The version and ABI mismatch handshake is itself tested | inst/tinytest/test-capi.R | done | variants compiled with a wrong hash, with and without the opt-in macro | Covers the documented gap: a wrong hash alone passes without the opt-in |
| cmp-K09 | Downstream consumers ported | docs/design/public-surface.md | done | stan4bart's bartcore branch and treatSens's compat branch build against the new header; the monthly smoke test builds three consumers | The smoke test's schedule and dispatch triggers are bound to the default branch, so on this branch it fires only on a push touching the workflow file |
| cmp-K10 | The model-matrix builder's new refusals | inst/tinytest/test-makeModelMatrix.R | done | that file matches the level-code and drop-pattern messages | The missing-valued indicator output has no matching assertion found by name |
| cmp-K11 | R-surface argument and refusal coverage | eight dedicated tinytest files | done | those files | 161 test files on this branch |
| cmp-K12 | Every export carries an alias and every registered S3 method has a definition | NAMESPACE, man, R | done | checked mechanically over the namespace against the man pages and sources | No missing alias, no missing definition |
| cmp-K13 | Tests that skip silently | exit calls in the suite | done | seven files exit on a missing Matrix package; the vector-instruction test exits on a scalar-only host; the C API test exits only outside CI | No test is commented out, marked as a to-do, or wrapped in an expected failure anywhere in the suite |
| cmp-K14 | The two compile-time-only research paths shipped in the source | code guards; tests/cpp/Makefile | done, by design | the C++ test makefile's extra-flags hook | Both stated in-code as private-build-only; no shipped build defines either |
| cmp-K15 | Fixture defaults in the amplitude specification that no consumer reaches | code comment in the combiner header | done, by design | inst/tinytest causal-forest tests | Stated in-code as a fixture default; the bridge fills the forest list unconditionally |
| cmp-K16 | The GPU survey | TODO gpu-bart; docs/design/gpu-bart.md | done | none needed | Survey memo complete 2026-07-07, recorded as a no-go with the conditions that would reopen it |
| cmp-K17 | The six-review retrospective program: correctness, interface, gate blind spots, calibration, roadmap, and architecture with numerics | docs/plans/archive/retrospective-reviews.md | done | status log: all six complete, 2026-07-08 to 2026-07-10; every filed fix landed | Totals recorded: four engine defects, one calibration defect, roadmap re-ranked |
| cmp-K18 | The three high-severity numerical findings | docs/plans/architecture-numerical-review.md | done | three archived plans, one of them at 93c06415 | All three landed; the accepted-risk and documentation-note items are documented |
| cmp-K19 | Grouped random effects: the exposure memo and the scale-sampler review | docs/plans/group-by-exposure.md, docs/plans/tau-slice-review.md | done | 1e5f80b2 deleted the path | Both retired 2026-09-06; the exact replacement the review recommended landed first and was then deleted with the feature |
| cmp-K20 | The block-fusion program | docs/design/block-fusion.md | done | stage C measured the fused path four to six times slower; machinery excised at 44e19d47 and kept on a branch | Closed as will-not-do with a measured record |
| cmp-K21 | Within-chain threading and blocked-Jacobi tree parallelism | docs/design/within-chain-threading.md | done | no-go on x86 and Apple Silicon, 2026-07-13 and 2026-07-21, and again on the current engine: best 1.03x at four workers and n = 1e6, a loss of about 5 percent at two workers and 15 at eight at n = 1e5 | Both closed on measurement, and the re-measurement closed the opt-in dec-B89 had asked for. n.threads keeps its own meaning, the control default caps at n.chains and an excess budget warns once per fit. Settles decision rows dec-A37 and dec-B115 |
| cmp-K22 | The composition-mixing probe | docs/plans/archive/composition-mixing-probe.md | done | verdict yellow, 2026-08-10; the registered harm clause fired and killed the recommendation | Its rank in the mixing survey was withdrawn |
| cmp-K23 | The grow-from-root warm start defaulting on | docs/design/grow-from-root-default.md | done | killed on measurement in both size strata, each confirmed by a mandated fresh-seed re-run | The option stays opt-in. Settles decision row dec-C05 |
| cmp-K24 | The level-fibre step's default flip, that is, adding a constant to every occupied leaf with the constants summing to zero across the forest | docs/design/level-fibre.md | done | benchmark arms recorded at ca92c11f: primary readings of -0.9 and -2.5 against a bar of +8 | Killed on measurement; the step instead became automatic under a frozen mixture at 127f04ee |
| cmp-K25 | The level-fibre step ships as a control slot no default configuration reaches | code: the control class and the chain header | done, by design | tinytest coverage of the flag itself | The automatic mode fires only where a forest's structural mixture is frozen, and no shipped default freezes one. Settles decision row dec-A23 |
| cmp-K26 | The perturb move's nonzero-default slice | docs/design/perturb-move.md | done | d73fb4e0: killed at the pre-registered weight and share | Closed by a recorded kill; what remains open is only whether to delete the opt-in |
| cmp-K27 | Two deferred-bin items verified fixed since the review: the silent null from the node-scale resolver, and the ensemble-scale logistic reference comparison not gating | docs/plans/review-2026-08-24/consolidated-report.md | done | the model source now stops by family; the logistic reference script folds its second part into the failure flag | Recorded because the review document still lists them open |
| cmp-K28 | The two tickets drafted out of the R and C++ adoption slate: guards on the four placeholder-shell readers, and a per-family latent weight channel | docs/plans/adoption-slate.md | done | the specification and handle sources at the tip | Both closed with recorded reasons before the backlog trim at 39fa379a: the shell case was obviated, the weight channel discharged at d0701a6a with four families declined by identification |
| cmp-K29 | Local-only developer tools: the stale-install detector and the snapshot regenerator | tools | done, as developer tools | none; neither is invoked from any workflow | By design, per the scripts' own headers |
| cmp-U16 | Drop the exact-ABI pin from stan4bart and treatSens at the coordinated merge | TODO release block | done ahead of the step | the pure C header's consumer ports: stan4bart's two makevars files at 4ace376, treatSens's three translation units at 6b274fb | All five files are already clear, so the version pair is the only runtime guard from here; the release block still lists the step. dec-B111's CI assertion, that a moved API hash carries a version bump, is the discipline replacing it. The pin was a runtime token comparison at the first stub resolution over the entry list's signatures, the ABI enumerators, the callback typedef and the ABI structs' layouts, not byte-identity of the header |
| cmp-D24 | The vectorized sufficient-statistic and cut-scan kernels | docs/plans/simd-survey.md; docs/plans/engine-performance.md | done, closed without shipping | measured under one percent of a weighted fit on arm64 and within noise on x86 with AVX2, the hot kernel being gather-bound | Settles decision row dec-B113. The code is archived on its own ref, the reference-build flag stays inert with both builds compiling identically, and the weighted fused pass landed in its place |
| cmp-K30 | The front-door rename arc, four slices | docs/plans/front-door.md | done | landing notes S1 ecb319aa, S2 44b3fa6d, S3 cb5d4ef2, S4 28431413, each recording tinytest, the equivalence trio, a clean-tarball package check and the documentation gates, with a mutation probe per slice | bart is the modern door and bartBT the legacy one at 0.9-34's formals; 31 tombstones; family objects; na.action; xbart's per-unit seeds and list-valued k grid |
| cmp-K31 | The pure C header arc | docs/plans/pure-c-header.md | done | S1 2d0b6d90, the two consumer ports, S3 56398169; the header holds no SEXP, and the shipped test consumer creates through R and keeps its five handshake arms | The four R-object entries are gone, the setters copy into buffers owned from creation, and a CI job asserts a moved hash carries a version bump |
| cmp-K32 | The interfaces and dependencies arc, four slices | docs/plans/interfaces-and-dependencies.md | done | S1 01634227, S2 c38f02d0, S3 7165c352, S4 a98c4cad | posterior removed, its ten methods replaced by draws() and an in-package Rhat and effective-sample-size estimator matching posterior 1.7.0 to 1e-14; Surv on the formula interface; sparse formula columns; the DESCRIPTION claim and three configure stubs |
| cmp-K33 | The per-draw callback arc, five slices | docs/plans/per-draw-callbacks.md | done | S1 711942ad, S2 e4063140, S3 db3bd084, S4 4cf56b69, S5 1e24bd55; tests/cpp pins the firing count and order at one, two and four chains, and a plain-C consumer drives the shipped setter | The R and C channels are independent by construction; the design note's threading and memory sections are measured; stan4bart is recorded as the second named consumer with nothing to port |
| cmp-K34 | The memory-footprint audit | docs/plans/memory-footprint-audit.md | done | the note validated against measured peak resident set over a 30-cell grid and re-recorded at 9171ff64 after the reductions: 3.7 percent median absolute relative residual, no cell outside max(10 percent, 20 MB) | Two packaging copies, the starting-sigma model frame, the unconditional complete-cases copy and the leaf cache's stale slots are all out; the last prediction copy is cmp-D27 |
| cmp-K35 | The engine-performance arc | docs/plans/engine-performance.md | done for S1, S3 and S5; S2 and S4 closed without shipping | S1 167e2c62, S3 70305575, S5 1456e999 with the CI follow-up b7802bfa, and the weighted fused pass 046c35d7 in S2's place | S2's vector kernels were measured and archived under dec-B113; S4's within-chain threading was re-measured and closed under dec-B115 (cmp-K21) |
