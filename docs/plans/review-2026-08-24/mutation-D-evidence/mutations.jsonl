{"id": "af1", "file": "R/bart.R", "find": "    predict(\n      object,\n      newdata,\n      group.by = as.factor(group.by),\n      type = \"ev\",\n      combineChains = FALSE\n    )", "repl": "    predict(\n      object,\n      newdata,\n      group.by = as.factor(group.by),\n      type = \"bart\",\n      combineChains = FALSE\n    )", "note": "survivalProbabilities.rbart: the drawn group intercepts are dropped at newdata"}
{"id": "af2", "file": "R/bart.R", "find": "      result[,, j, ] <- pnorm(\n        (log(times[j]) - as.vector(linearPredictor)) / scale,\n        lower.tail = FALSE\n      )", "repl": "      result[,, j, ] <- pnorm(\n        (log(times[j]) - as.vector(linearPredictor)) / scale,\n        lower.tail = TRUE\n      )", "note": "survivalProbabilities: multi-chain branch reports the CDF, not the survival tail"}
{"id": "af3", "file": "R/bart.R", "find": "  if (combineChains && length(drawDims) > 1L) {\n    # merge the chain and sample margins as combineChains() does, samples\n    # varying fastest within each chain\n    result <- aperm(result, c(2L, 1L, 3L, 4L))", "repl": "  if (combineChains && length(drawDims) > 1L) {\n    result <- aperm(result, c(1L, 2L, 3L, 4L))", "note": "survivalProbabilities: combined chains stacked chain-minor"}
{"id": "dc1", "file": "src/R_interface_bartcore.cpp", "find": "      if (numCuts < 1)", "repl": "      if (numCuts < 0)", "note": "setCutPoints: an empty cut vector is accepted"}
{"id": "dc2", "file": "src/R_interface_bartcore.cpp", "find": "      for (R_xlen_t i = 1; i < numCuts; ++i)\n        if (cuts[i] <= cuts[i - 1])", "repl": "      for (R_xlen_t i = 1; i < numCuts; ++i)\n        if (false)", "note": "setCutPoints: the strictly-increasing check is dropped"}
{"id": "dc3", "file": "src/bartcore/data.hpp", "find": "      grid.inducedNumCuts = 1;", "repl": "      grid.inducedNumCuts = 0;", "note": "a constant column induces no cut at all instead of one degenerate cut"}
{"id": "e01", "file": "src/bartcore/moves.hpp", "find": "  *currentLogLikelihood =\n    current.rank > proposal.rank ? -HUGE_VAL : current.logLikelihood;\n  *proposalLogLikelihood =\n    proposal.rank > current.rank ? -HUGE_VAL : proposal.logLikelihood;", "repl": "  *currentLogLikelihood =\n    current.rank > proposal.rank ? -1.0e7 : current.logLikelihood;\n  *proposalLogLikelihood =\n    proposal.rank > current.rank ? -1.0e7 : proposal.logLikelihood;", "note": "empty-leaf veto priced at a finite -1e7 instead of -HUGE_VAL"}
{"id": "jo1", "file": "R/updatePredictorPerObservationJointly.R", "find": "      x[installed]\n    )", "repl": "      x\n    )", "note": "joint per-observation update: the rolled-back values are installed anyway"}
{"id": "jo2", "file": "R/updatePredictorPerObservationJointly.R", "find": "    columnName <- columnNames[column]", "repl": "    columnName <- columnNames[1L]", "note": "joint update: a numeric column resolves to the first column's name"}
{"id": "jo3", "file": "R/updatePredictorPerObservationJointly.R", "find": "  for (sampler in samplers) {\n    if (\n      (is.na(updateState) && sampler$control@updateState == TRUE) ||\n        identical(updateState, TRUE)\n    ) {\n      sampler$storeState()\n    }\n  }", "repl": "  NULL", "note": "joint update: updateState never stores"}
{"id": "jt1", "file": "src/bartcore/facade.hpp", "find": "    bool valid = true;\n    for (std::size_t k = 0; k < numSamplers && valid; ++k)\n      valid = sessions[k]->observationWouldRemainValid(j);", "repl": "    bool valid = sessions[0]->observationWouldRemainValid(j);", "note": "joint update consults only the first sampler, not every one"}
{"id": "jt2", "file": "src/bartcore/facade.hpp", "find": "    if (valid)\n      for (std::size_t k = 0; k < numSamplers; ++k)\n        sessions[k]->commitObservation(j);", "repl": "    if (valid) sessions[0]->commitObservation(j);", "note": "joint update commits only into the first sampler"}
{"id": "md1", "file": "R/model.R", "find": "    if (startsWith(k, \"chi\")) {", "repl": "    if (FALSE) {", "note": "normal(k = \"chi(...)\") string form no longer parses as a hyperprior"}
{"id": "md2", "file": "R/model.R", "find": "  newValidated(\"dbartsChiSqPrior\", df = df, quantile = quant)", "repl": "  newValidated(\"dbartsChiSqPrior\", df = quant, quantile = df)", "note": "chisq(df, quant) arguments transposed"}
{"id": "md3", "file": "R/model.R", "find": "cgm <- function(power = 2, base = 0.95, split.probs = NULL) {\n  result <- newValidated(\n    \"dbartsCGMPrior\",\n    power = power,\n    base = base,", "repl": "cgm <- function(power = 2, base = 0.95, split.probs = NULL) {\n  result <- newValidated(\n    \"dbartsCGMPrior\",\n    power = base,\n    base = power,", "note": "cgm(power, base) arguments transposed"}
{"id": "o01", "file": "R/dbarts.R", "find": "    n.threads = coerceOrError(n.threads, \"integer\"),\n    n.thin = coerceOrError(n.thin, \"integer\"),", "repl": "    n.threads = 1L,\n    n.thin = coerceOrError(n.thin, \"integer\"),", "note": "dbartsControl: n.threads silently forced to 1"}
{"id": "of1", "file": "R/bartcore.R", "find": "      offset <- rep_len(offset, length(sampler$data@y))", "repl": "      offset <- offset", "note": "setOffset: a scalar offset is not expanded to one value per observation"}
{"id": "of2", "file": "R/bartcore.R", "find": "      if (identical(sampler$data@testUsesRegularOffset, TRUE)) {\n        offset.test <- if (!is.null(sampler$data@x.test)) {", "repl": "      if (TRUE) {\n        offset.test <- if (!is.null(sampler$data@x.test)) {", "note": "setOffset: a separately-set test offset is overwritten by the training offset"}
{"id": "of3", "file": "R/bartcore.R", "find": "            length(offset) == nrow(sampler$data@x.test)\n        ) {\n          offset\n        } else {\n          NULL\n        }", "repl": "            TRUE\n        ) {\n          offset\n        } else {\n          NULL\n        }", "note": "setOffset: a length-mismatched vector offset is still pushed to the test rows"}
{"id": "p02", "file": "R/utility.R", "find": "        match(oldLevels[x.test$sparse@x[entries] + 1], trainingLevels) - 1L", "repl": "        match(oldLevels[x.test$sparse@x[entries] + 1], trainingLevels)", "note": "sparse test column recode: category codes off by one"}
{"id": "p03", "file": "R/utility.R", "find": "      x.test$sparseReference[rank] <- as.integer(reference)", "repl": "      NULL", "note": "sparse test column recode: the reference level is left on the training coding"}
{"id": "p04", "file": "R/utility.R", "find": "      if (anyNA(codes) || is.na(reference)) {", "repl": "      if (FALSE) {", "note": "sparse test column: unseen-level refusal dropped"}
{"id": "pi1", "file": "src/bartcore/chain.hpp", "find": "          if (tree.bottomNodesHaveWeight(forestWeights)) break;", "repl": "          break;", "note": "sampleTreesFromPrior: the empty-leaf rejection loop never rejects"}
{"id": "pi2", "file": "src/bartcore/chain.hpp", "find": "          if (tree.bottomNodesHaveWeight(forestWeights)) break;", "repl": "          if (tree.bottomNodesAreOccupied()) break;", "note": "sampleTreesFromPrior: conditions on membership rather than positive weight"}
{"id": "pm1", "file": "src/bartcore/chain.hpp", "find": "          misc_setVectorToConstant(forest.treeFits.data() + t * n, n, 0.0);\n        }\n      }\n      misc_setVectorToConstant(forest.totalFits.data(), n, 0.0);", "repl": "          misc_setVectorToConstant(forest.treeFits.data() + t * n, n, 0.0);\n        }\n      }", "note": "sampleTreesFromPrior: the rolled totalFits are not reset with the new forest"}
{"id": "po1", "file": "src/bartcore/sampler.hpp", "find": "      bool valid = session.observationWouldRemainValid(j);", "repl": "      bool valid = true;", "note": "per-observation setPredictor installs every value without the empty-leaf check"}
{"id": "po2", "file": "src/bartcore/sampler.hpp", "find": "    std::vector<size_t> scanOrder(n);\n    ext_rng_drawPermutation(chains_[0]->rng(), scanOrder.data(), n);", "repl": "    std::vector<size_t> scanOrder(n);\n    for (size_t i = 0; i < n; ++i) scanOrder[i] = i;", "note": "per-observation setPredictor scans in index order, not randomized"}
{"id": "pp1", "file": "R/dbarts.R", "find": "  newControl@keepTrees <- FALSE", "repl": "  newControl@keepTrees <- TRUE", "note": "samplePriorPredictive: keepTrees is no longer forced off on the private sampler"}
{"id": "pp2", "file": "R/dbarts.R", "find": "  draw <- dbartsSampler$new(newControl, sampler$model, sampler$data)", "repl": "  draw <- sampler$copy()", "note": "samplePriorPredictive: reuses a copy of the caller's sampler (frozen RNG) instead of a fresh one"}
{"id": "pp3", "file": "R/dbarts.R", "find": "      sigmaDraws <- sqrt(df * sigest^2 * rawScale / rchisq(n.samples, df))", "repl": "      sigmaDraws <- rep_len(sigest, n.samples)", "note": "samplePriorPredictive: sigma is held at sigest rather than drawn from its prior"}
{"id": "pr1", "file": "src/bartcore/model.hpp", "find": "  double drawFromPrior(ext_rng* rng, double k) const {\n    return (scale / k) * ext_rng_simulateStandardNormal(rng);\n  }", "repl": "  double drawFromPrior(ext_rng* rng, double k) const {\n    return (scale / (2.0 * k)) * ext_rng_simulateStandardNormal(rng);\n  }", "note": "constant-leaf prior draw uses half the prior scale"}
{"id": "ps1", "file": "src/R_interface_bartcore.cpp", "find": "  if (numTestObservations == 0) Rf_error(\"bartcore_predict: requires rows\");", "repl": "  NULL;", "note": "predict: a zero-row test set is not refused"}
{"id": "pv4", "file": "R/dbarts.R", "find": "    draw$sampleNodeParametersFromPrior(updateState = FALSE)", "repl": "    NULL", "note": "samplePriorPredictive: leaf parameters are never drawn from their prior"}
{"id": "pv5", "file": "R/dbarts.R", "find": "    fit <- draw$predict(xt, offset.test, n.threads)", "repl": "    fit <- draw$predict(xt, NULL, n.threads)", "note": "samplePriorPredictive: the offset.test argument is discarded"}
{"id": "r01", "file": "R/rbart.R", "find": "  group.by <- droplevels(as.factor(group.by))", "repl": "  group.by <- as.factor(rep_len('g', length(group.by)))", "note": "rbart: every observation collapsed into one group"}
{"id": "r02", "file": "R/rbart.R", "find": "  if (length(group.by) != length(data@y)) {", "repl": "  if (FALSE) {", "note": "rbart: group.by length check dropped"}
{"id": "r03", "file": "R/rbart.R", "find": "      yhat.train = if (rbartArgs$keepTrainingFits) {\n        chainMatrix(samples$train, i)\n      },", "repl": "      yhat.train = chainMatrix(samples$train, i),", "note": "rbart in-core: keepTrainingFits = FALSE still stores training fits"}
{"id": "r04", "file": "R/rbart.R", "find": "  result$ranef.mean <- apply(result$ranef, length(dim(result$ranef)), mean)", "repl": "  result$ranef.mean <- apply(result$ranef, 1L, mean)", "note": "rbart: ranef.mean margin taken over chains rather than groups"}
{"id": "r05", "file": "R/rbart.R", "find": "    result$fit <- lapply(chainResults, function(x) x$sampler)", "repl": "    result$fit <- lapply(chainResults, function(x) x$sampler)[1L]", "note": "rbart R-loop: only the first chain's sampler is kept"}
{"id": "rb1", "file": "R/spec.R", "find": "  residDf <- residDistDf(priors$resid.dist)\n  if (!is.null(residDf)) {\n    attr(model, \"resid.df\") <- residDf\n  }", "repl": "  residDf <- residDistDf(priors$resid.dist)", "note": "student residual df never reaches the model's resid.df attribute"}
{"id": "rb2", "file": "R/model.R", "find": "        df <= 0.0\n    ) {", "repl": "        FALSE\n    ) {", "note": "student(): non-positive df is accepted"}
{"id": "rb3", "file": "R/model.R", "find": "  if (is.na(resid.dist@df)) 0.0 else resid.dist@df", "repl": "  resid.dist@df", "note": "student(estimate) reports NA rather than the 0 sentinel the bridge reads"}
{"id": "rm1", "file": "R/bartcore.R", "find": "  if (ncol(newData@x) != ncol(sampler$data@x)) {", "repl": "  if (FALSE) {", "note": "setData: the same-predictor-count refusal is dropped"}
{"id": "rn1", "file": "R/dbarts.R", "find": "  seed <- coerceOrError(seed, \"integer\")\n  if (!is.na(seed)) {\n    control@seed <- seed\n  }", "repl": "  seed <- coerceOrError(seed, \"integer\")", "note": "dbarts(): the seed argument never reaches the control"}
{"id": "ro1", "file": "R/rbart.R", "find": "  factors <- match.arg(factors)", "repl": "  factors <- \"categorical\"", "note": "rbart_vi: the factors argument never reaches the data (indicators ignored)"}
{"id": "ro2", "file": "R/rbart.R", "find": "    if (!is.null(chainResults[[1L]]$varprobs)) {\n      result$varprobs <- convertSamplesFromDbartsToBart(", "repl": "    if (FALSE) {\n      result$varprobs <- convertSamplesFromDbartsToBart(", "note": "rbart_vi: DART split probabilities are dropped from the result"}
{"id": "ro3", "file": "R/rbart.R", "find": "    nodeK = if (!is.null(matchedCall[[\"k\"]])) k else NULL,", "repl": "    nodeK = 2,", "note": "rbart_vi: an unsupplied k resolves to the fixed 2 rather than the family default hyperprior"}
{"id": "ro4", "file": "R/rbart.R", "find": "    if (NROW(chainResults[[1L]]$yhat.train) <= 0L) {\n      result$yhat.train <- NULL", "repl": "    if (FALSE) {\n      result$yhat.train <- NULL", "note": "rbart_vi: keepTrainingFits = FALSE still reports a yhat.train"}
{"id": "ro5", "file": "R/rbart.R", "find": "    result$varcount <- chainResults[[1L]]$varcount\n    if (!is.null(chainResults[[1L]]$varprobs)) {\n      result$varprobs <- chainResults[[1L]]$varprobs\n    }", "repl": "    result$varcount <- chainResults[[1L]]$varcount", "note": "rbart_vi single chain: DART split probabilities dropped from the result"}
{"id": "ro6", "file": "R/rbart.R", "find": "      yhat.train = if (rbartArgs$keepTrainingFits) {\n        chainMatrix(samples$train, i)\n      },", "repl": "      yhat.train = chainMatrix(samples$train, i),", "note": "rbart in-core: keepTrainingFits = FALSE still fills the chain's training fits"}
{"id": "rr1", "file": "R/rbart.R", "find": "    if (is.na(seed)) expr else withFixedSeed(seed, expr)", "repl": "    expr", "note": "rbart_vi in-core path: a supplied seed no longer fixes the draw stream"}
{"id": "s01", "file": "R/sliceSample.R", "find": "  shrinkInterval <- function(x, x.p, int) {\n    if (x.p > x) {\n      int[2L] <- x.p\n    } else {\n      int[1L] <- x.p\n    }\n    int\n  }", "repl": "  shrinkInterval <- function(x, x.p, int) {\n    if (x.p > x) {\n      int[1L] <- x.p\n    } else {\n      int[2L] <- x.p\n    }\n    int\n  }", "note": "sliceSample: the shrink step keeps the wrong half of the interval"}
{"id": "s02", "file": "R/sliceSample.R", "find": "      if (x.r > boundary[2L]) x.r <- boundary[2L]", "repl": "      NULL", "note": "sliceSample: the upper boundary no longer clamps the stepping-out interval"}
{"id": "s03", "file": "R/sliceSample.R", "find": "    if (!accepted) {\n      stop(\"slice sampler failed: maxIter reached\")\n    }", "repl": "    if (j == maxIter) {\n      stop(\"slice sampler failed: maxIter reached\")\n    }", "note": "sliceSample: exhaustion tested on the loop counter rather than acceptance"}
{"id": "s04", "file": "R/sliceSample.R", "find": "    while (TRUE) {\n      u <- -rexp(1)\n      x <- rgenerator()\n      numIters <- numIters + 1\n      if (x <= boundary[1] || x >= boundary[2]) {\n        next\n      }", "repl": "    while (TRUE) {\n      u <- -rexp(1)\n      x <- rgenerator()\n      numIters <- numIters + 1\n", "note": "rejectionSample (log): out-of-boundary proposals are returned rather than discarded"}
{"id": "sc1", "file": "R/spec.R", "find": "  if (!is.na(seed)) {\n    control@seed <- seed\n  }", "repl": "  NULL", "note": "dbartsSpec: the seed argument never reaches the control"}
{"id": "sc2", "file": "R/spec.R", "find": "  if (!missing(sigma)) {\n    data@sigma <- coerceOrError(sigma, \"numeric\")\n  }", "repl": "  data@sigma <- coerceOrError(sigma, \"numeric\")", "note": "dbartsSpec: sigma always overrides the data's, even unsupplied"}
{"id": "sc3", "file": "R/spec.R", "find": "    if (anyNA(survival) || any(survival != 0.0 & survival != 1.0)) {", "repl": "    if (FALSE) {", "note": "dbartsSpec: survival status values are not checked to be 0/1"}
{"id": "sd1", "file": "R/bartcore.R", "find": "  newData@n.cuts <- sampler$data@n.cuts", "repl": "  newData@n.cuts <- rep_len(1L, ncol(newData@x))", "note": "setData: the replacement data is quantized onto a single cut per column"}
{"id": "sm1", "file": "src/misc/linearAlgebra_neon.c", "find": "  for ( ; i < prefix; ++i)\n    y[i] += x[i];\n  \n  if (y_offset == x_offset) {", "repl": "  for ( ; i < prefix; ++i)\n    y[i] += x[i] * 1.0000000001;\n  \n  if (y_offset == x_offset) {", "note": "NEON addVectorsInPlace prefix loop diverges from the scalar kernel"}
{"id": "sp1", "file": "R/model.R", "find": "    split.probs <- split.probs / sum(split.probs)", "repl": "    split.probs <- split.probs", "note": "split probabilities are never normalized to a simplex"}
{"id": "sp2", "file": "R/model.R", "find": "    if (all(split.probs == split.probs[1L])) {\n      split.probs <- numeric()\n    }", "repl": "    if (FALSE) {\n      split.probs <- numeric()\n    }", "note": "uniform split probabilities are no longer canonicalized to the empty vector"}
{"id": "sp3", "file": "R/model.R", "find": "      factorMatch <- which(startsWith(\n        names(result),\n        paste0(split.names[i], \".\")\n      ))", "repl": "      factorMatch <- integer()", "note": "named split probabilities never reach a factor's expanded indicator columns"}
{"id": "sr1", "file": "src/bartcore/chain.hpp", "find": "    double result = misc_computeSumOfSquaredResiduals(\n      response_->workingResponse(), data_.numObservations, combinedFits());", "repl": "    double result = misc_computeSumOfSquaredResiduals(\n      response_->workingResponse(), data_.numObservations,\n      forests_[0].totalFits.data());", "note": "getSumsOfSquaredResiduals reports forest 0's own total, not the combined location"}
{"id": "st1", "file": "src/R_interface_bartcore.cpp", "find": "static constexpr double sumToOneTolerance = 0x1p-26;", "repl": "static constexpr double sumToOneTolerance = 1.0e-10;", "note": "bridge sum-to-one tolerance back at the old 1e-10"}
{"id": "st2", "file": "R/A_class.R", "find": "  if (abs(sum(proposalProbs) - 1.0) >= sqrt(.Machine$double.eps)) {", "repl": "  if (abs(sum(proposalProbs) - 1.0) >= 1.0e-10) {", "note": "dbartsModel validity: proposal-probability tolerance back at 1e-10"}
{"id": "sv1", "file": "R/dbarts.R", "find": "            \"samplers cannot be re-created without a stored state; call \",\n            \"storeState() before serializing (see the Saving section of \",\n            \"?`dbartsSampler-class`)\"", "repl": "            \"no state\"", "note": "the re-creation refusal no longer names storeState()"}
{"id": "sv2", "file": "R/dbarts.R", "find": "        .Call(\n          C_dbarts_bartcore_setState,\n          ptr,\n          state,\n          rawPredictorMatrix(data@x)\n        )", "repl": "        NULL", "note": "a re-created sampler is not given its stored state"}
{"id": "u01", "file": "R/bart.R", "find": "      t(matrix(samples, length(samples) %/% n.chains, n.chains))", "repl": "      matrix(samples, n.chains, length(samples) %/% n.chains)", "note": "uncombineChains vector branch: sample-major instead of chain-major"}
{"id": "u02", "file": "R/bart.R", "find": "    as.vector(t(samples)),", "repl": "    as.vector(samples),", "note": "combineChains 2-D branch: flattened without transposing"}
{"id": "u03", "file": "R/bart.R", "find": "            colnames(res) <- dimnames(samples)[[1L]]", "repl": "            colnames(res) <- NULL", "note": "convertSamplesFromDbartsToBart: combined result loses its parameter names"}
{"id": "u04", "file": "R/bart.R", "find": "        aperm(samples, c(3L, 2L, 1L))", "repl": "        aperm(samples, c(3L, 1L, 2L))", "note": "convertSamplesFromDbartsToBart: uncombined permutation swaps sample and chain"}
{"id": "us1", "file": "R/dbarts.R", "find": "    setCutPoints = function(cuts, column, updateState = NA) {\n      \"Changes the cut points for the predictors in column, or the entire set itself if the column argument is missing. Forces the change by pruning any leaves that end up empty. updateState is opt-in; see setData.\"\n\n      bartcoreSamplerSetCutPoints(\n        .self,\n        cuts,\n        column = if (missing(column)) NULL else column\n      )\n      if (identical(updateState, TRUE)) {", "repl": "    setCutPoints = function(cuts, column, updateState = NA) {\n      \"Changes the cut points for the predictors in column, or the entire set itself if the column argument is missing. Forces the change by pruning any leaves that end up empty. updateState is opt-in; see setData.\"\n\n      bartcoreSamplerSetCutPoints(\n        .self,\n        cuts,\n        column = if (missing(column)) NULL else column\n      )\n      if (!identical(updateState, FALSE)) {", "note": "setCutPoints: updateState defaults to storing rather than opt-in"}
{"id": "v01", "file": "R/validateComposition.R", "find": "  tag0 <- runif(1L)\n  below + sum(runif(ties) < tag0)", "repl": "  below", "note": "SBC rank: the tie-break for atom-valued functionals is dropped"}
{"id": "v02", "file": "R/validateComposition.R", "find": "  on.exit({\n    if (hasSeed) {\n      assign(\".Random.seed\", oldSeed, envir = globalenv())\n    } else if (exists(\".Random.seed\", envir = globalenv(), inherits = FALSE)) {\n      rm(\".Random.seed\", envir = globalenv())\n    }\n  })", "repl": "  NULL", "note": "withFixedSeed: the caller's RNG stream is not restored"}
{"id": "v03", "file": "R/validateComposition.R", "find": "    band <- as.numeric(quantile(nullMax, 1 - alpha))", "repl": "    band <- as.numeric(quantile(nullMax, 1 - 0.05))", "note": "SBC band: alpha ignored, so the Bonferroni correction never reaches it"}
{"id": "v04", "file": "R/validateComposition.R", "find": "  if (ties == 0L) {\n    return(below)\n  }", "repl": "  if (TRUE) {\n    return(below)\n  }", "note": "SBC rank: tie-break unreachable (duplicate of the atom path, guard inverted)"}
{"id": "wl1", "file": "src/R_interface_bartcore.cpp", "find": "      if (!(weights[i] > 0.0) || weights[i] != std::floor(weights[i]))", "repl": "      if (false)", "note": "logistic weights: the positive-integer count check is dropped"}
{"id": "wl2", "file": "R/spec.R", "find": "    if (anyNA(w) || any(w <= 0) || any(w != round(w))) {", "repl": "    if (FALSE) {", "note": "R-side logistic count-weight validation dropped"}
{"id": "wl3", "file": "R/spec.R", "find": "        \"probit models do not support weights; fit integer count weights \",\n        \"with family = \\\"logistic\\\", or model continuous weights' latents \",\n        \"directly\"", "repl": "        \"no weights here\"", "note": "the probit weights refusal text is replaced"}
{"id": "ws1", "file": "src/R_interface_bartcore.cpp", "find": "    if (numForests == 0) {", "repl": "    if (false) {", "note": "warm start: a donor chain declaring no forests is not refused by name"}
{"id": "ws2", "file": "R/dbarts.R", "find": "      .Call(C_dbarts_bartcore_installForests, ptr, donorState, samples)", "repl": "      .Call(C_dbarts_bartcore_installForests, ptr, donorState, NULL)", "note": "installTrees: the samples argument is discarded"}
{"id": "wsr", "file": "R/bart.R", "find": "    sampler$installTrees(warm.start)", "repl": "    NULL", "note": "bart2(warm.start =): the donor forest is never installed"}
{"id": "x01", "file": "R/xbart.R", "find": "  numChunks <- max(1L, min(n.threads, n.reps))", "repl": "  numChunks <- 1L", "note": "xbart: n.threads never splits work into chunks"}
{"id": "x02", "file": "R/xbart.R", "find": "  varNames <- c(\"n.trees\", \"k\", \"power\", \"base\")", "repl": "  varNames <- c(\"k\", \"n.trees\", \"power\", \"base\")", "note": "xbart: result dimnames axis labels transposed"}
{"id": "x03", "file": "R/xbart.R", "find": "      lossValues[cell, ] <- as.numeric(lossValue)", "repl": "      lossValues[cell, ] <- 0.0", "note": "xbart: the loss function's value is discarded"}
{"id": "x04", "file": "R/xbart.R", "find": "      results[[i]] <- repLoss / length(spec$foldSizes)", "repl": "      results[[i]] <- repLoss", "note": "xbart: k-fold loss summed over folds, not averaged"}
{"id": "x05", "file": "R/xbart.R", "find": "    foldSizes <- rep.int(numObservations %/% n.test, n.test) +\n      rep.int(\n        c(1L, 0L),\n        c(numObservations %% n.test, n.test - numObservations %% n.test)\n      )", "repl": "    foldSizes <- rep.int(numObservations %/% n.test, n.test)", "note": "xbart k-fold: the remainder rows are never held out"}
{"id": "x06", "file": "R/xbart.R", "find": "      min(numObservations - 1L, as.integer(round(n.test * numObservations)))", "repl": "      min(numObservations - 1L, 1L)", "note": "xbart random subsample: holds out a single row regardless of n.test"}
{"id": "x07", "file": "R/xbart.R", "find": "        rowMeans(probFromLatent(testSamples)) > 0.5", "repl": "        rowMeans(probFromLatent(testSamples)) > 0.4", "note": "xbart mcr: classification threshold moved off 0.5"}
{"id": "x08", "file": "R/xbart.R", "find": "      logLikelihood <- ifelse(y.test > 0, log(p.test), log1p(-p.test))", "repl": "      logLikelihood <- log(p.test)", "note": "xbart log loss: the y == 0 arm scores the wrong tail"}
{"id": "x09", "file": "R/xbart.R", "find": "  probFromLatent <- if (identical(family, \"logistic\")) plogis else pnorm", "repl": "  probFromLatent <- if (identical(family, \"logistic\")) pnorm else plogis", "note": "xbart binary losses: link swapped between families"}
{"id": "x10", "file": "R/xbart.R", "find": "    withFixedSeed(seed, sample.int(.Machine$integer.max, numChunks))", "repl": "    sample.int(.Machine$integer.max, numChunks)", "note": "xbart: a supplied seed no longer fixes the chunk seeds"}
{"id": "x11", "file": "R/xbart.R", "find": "  kIsGrid <- is.numeric(k)", "repl": "  kIsGrid <- FALSE", "note": "xbart: the k axis collapses even when k is a numeric grid"}
{"id": "x12", "file": "R/xbart.R", "find": "  if (anyNA(n.trees) || any(n.trees <= 0L)) {", "repl": "  if (FALSE) {", "note": "xbart: n.trees validation dropped"}
{"id": "x13", "file": "R/xbart.R", "find": "    stop(\"loss must be in 'rmse', 'log', 'mcr', or a function\")", "repl": "    stop(\"bad loss\")", "note": "xbart: loss refusal message text unpinned"}
{"id": "x14", "file": "R/xbart.R", "find": "        numBurnIn <- spec$n.burn[2L]", "repl": "        numBurnIn <- spec$n.burn[1L]", "note": "xbart: warm cells reuse the fresh-sampler burn-in"}
{"id": "x15", "file": "R/xbart.R", "find": "      foldOffset <- foldOffset + spec$foldSizes[fold]", "repl": "      foldOffset <- foldOffset", "note": "xbart k-fold: every fold holds out the same leading rows"}
{"id": "xm4", "file": "R/xbart.R", "find": "      permutation <- sample.int(numObservations)", "repl": "      permutation <- seq_len(numObservations)", "note": "xbart k-fold: the fold partition is never randomized across replications"}
{"id": "zw1", "file": "src/bartcore/model.hpp", "find": "      degreesOfFreedom + static_cast<double>(numPositiveWeights);", "repl": "      degreesOfFreedom + static_cast<double>(numObservations);", "note": "sigma posterior df counts zero-weight rows"}
{"id": "zw2", "file": "src/bartcore/model.hpp", "find": "    double sumOfSquaredResiduals = weights == nullptr\n      ? misc_computeSumOfSquaredResiduals(y, numObservations, totalFits)\n      : misc_computeWeightedSumOfSquaredResiduals(y, numObservations, weights,\n                                                  totalFits);", "repl": "    double sumOfSquaredResiduals =\n      misc_computeSumOfSquaredResiduals(y, numObservations, totalFits);", "note": "sigma posterior uses the unweighted residual sum of squares"}
