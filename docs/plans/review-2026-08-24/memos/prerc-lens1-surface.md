# Pre-RC audit, lens 1: public surface freeze

`/Users/vdorie/Repositories/dbarts`, branch `bartcore`, HEAD `7a8c7286` (verified; tree clean apart from an untracked `src/*.o.tmp`). Read-only.
Surface counted: 28 `export()` + 2 `exportClasses` + `exportMethods(show, length)`; 47 documented `dbartsSampler` methods; 74 `S3method`s; 47 flat-C entries, 3 ABI structs, 2 enums, 1 callback.
**BREAKING** = a rename/reorder/removal/retype a released 1.0-0 consumer would feel. *additive* = the fix only appends, so post-release costs the same. Cost = dense lines. The top C findings and both top doc findings I re-verified against the header/Rd myself; where that softened a subagent's framing, the softer reading is what is written below.

## A. Flat C API (`inst/include/dbarts/dbarts.h`)

| # | What | Evidence | Class | Fix | Cost | Rec |
|---|---|---|---|---|---|---|
| A1 | **The stub path makes lockstep the DEFAULT and compatibility the opt-out, inverting the header's stated model.** The header promises `apiMajorVersion() == MAJOR && apiMinorVersion() >= MINOR`, "the interface only ever grows... additions arrive under new names (a minor bump)", and offers the hash as an extra "for a consumer that wants lockstep rather than compatibility". But every `DBARTS_USE_STUBS` stub calls `dbarts_stub_checkApiHash()` first and that check is *equality* - so an additive MINOR release hard-errors every stub consumer until rebuilt. The escape hatch the header names is real ("a consumer that wants to negotiate... resolves those two entries by hand"), so negotiation is reachable - just not through the macro path every consumer will use. | `[[inst/include/dbarts/dbarts.h:562-576@7a8c7286]]` (equality), `[[inst/include/dbarts/dbarts.h:603@7a8c7286]]` (every stub), `[[inst/include/dbarts/dbarts.h:20-26@7a8c7286]]` (promise), `[[inst/include/dbarts/dbarts.h:559-560@7a8c7286]]` (hatch) | **BREAKING** - the freeze locks in what the constants MEAN | Stubs check major-equality + minor-floor; reserve hash equality for an opt-in `DBARTS_REQUIRE_EXACT_ABI`. Header-only, no ABI change. | ~15 | **pre-RC** |
| A2 | **`printTrees` lacks `useLiveTrees`** though `getTrees` has it and printTrees' own doc claims it is "on the same index contract as `dbarts_sampler_getTrees`". Live trees cannot be printed at all. Adding a parameter after the freeze is a MAJOR bump. | `[[inst/include/dbarts/dbarts.h:456-470@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:900-906@7a8c7286]]` | **BREAKING** | Add the parameter. | ~8 | **pre-RC** |
| A3 | **`forest` position splits by signature shape**: 2nd arg in 8 simple accessors (`numTrees`, `numForestAmplitudes`, `forestFits`, `forestAmplitudes`, `setForestBasis`, `setForestWeights`, `forestCalibration`, `setForestPriorScale`), LAST in the two index-list entries. Verified not arbitrary - those two are `(sampler, <6 index args>, [useLiveTrees,] forest)`, so `forest` trails a block rather than a handle. | `[[inst/include/dbarts/dbarts.h:456-470@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:905-906@7a8c7286]]` | **BREAKING** | Move to position 2 for one rule, or leave and state the shape rule in the header. | ~10 | **pre-RC (decide)** |
| A4 | **Capability refusal splits between two conventions.** `setSigma`/`setWeights`/`setResponse`/`setOffset`/`setTestPredictors` return `void` and `Rf_error` on a capability refusal; `dispersion`/`getLatents`/`setForest*` return 0 for the same class. Only `void`->`int` fixes it. Sharpened by A6: the header tells callers to "test the family before calling" and gives them no way to. | `[[inst/include/dbarts/dbarts.h:761-764@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:788@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:768@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:989@7a8c7286]]` | **BREAKING** | Move at least `setSigma` to `int`. | ~12 | **pre-RC** |
| A5 | **`setActiveRows` conflates the two channels** - returns 0 both for "family implements no mask" and for "an element is neither 0 nor 1", against the doctrine `setForestBasis`/`setForestWeights` state verbatim ("a CAPABILITY answer is the return value... a MALFORMED BASIS raises"). Verified deliberate and documented, with a rationale (fractional = weighted likelihood, use `setWeights`) - so this is a documented inconsistency in the refusal doctrine, not a bug. | `[[inst/include/dbarts/dbarts.h:1102-1106@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:987-990@7a8c7286]]` | **BREAKING** | Raise on non-0/1, keep 0 for the capability answer. | ~4 | **pre-RC** |
| A6 | **No family accessor exists**, yet `[[inst/include/dbarts/dbarts.h:763@7a8c7286]]` instructs callers to test the family first. Families are stringly-typed (`create`, `drawLatents`, `workingResponse`) while column type and leaf model ARE enums, so adding a `dbarts_family` enum later is awkward. | `[[inst/include/dbarts/dbarts.h:763@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:243@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:320@7a8c7286]]` | **BREAKING** (the typing decision) | Decide the typing now; the accessor is then additive. | ~15 | **pre-RC** |
| A7 | **`columnTypes` + `categoryCounts` are validated then ignored** - the struct annotates them "declared, not read". Two dead fields a consumer must still fill correctly, above the struct's own `/* 1.0-0 field boundary */` marker. | `[[inst/include/dbarts/dbarts.h:286-289@7a8c7286]]` | **BREAKING** to remove later | Drop, or keep with the rationale stated. | ~6 | **pre-RC (decide)** |
| A8 | **Same-width type spelling drift the hash cannot catch**: `cscColumnPointers`/`cscRowIndices` are `const int*` while `columnSources`/`columnTypes`/`referenceCodes` are `const int32_t*` **in the same struct**; `forest_calibration.leafModel` is `int*` where the sibling enum array is `int32_t*`. The header admits same-width retypes are hash-invisible. | `[[inst/include/dbarts/dbarts.h:283-290@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:342@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:121-124@7a8c7286]]` | **BREAKING** | Spell one way. Free now, invisible-to-the-hash later. | ~4 | **pre-RC** |
| A9 | **`printEvery` is `uint32_t`** - the only count in the API that is not `size_t`. | `[[inst/include/dbarts/dbarts.h:478@7a8c7286]]` | **BREAKING** | Retype. | ~2 | **pre-RC** |
| A10 | **`get` prefix on exactly 2 of 5 identically-shaped readers**: `getLatents`, `getTrees` vs `forestFits`, `forestAmplitudes`, `dispersion`. Also `setPredictor` (singular, whole matrix) vs `setTestPredictors` (plural); `drawLatents` (verb) vs `workingResponse` (noun). | `[[inst/include/dbarts/dbarts.h:779@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:890@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:1002@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:1044@7a8c7286]]`, `[[inst/include/dbarts/dbarts.h:789@7a8c7286]]` | **BREAKING** | Pick one convention. | ~20 | **pre-RC** |
| A11 | **`setForestBasis` takes a ROW-major basis** against the global "Matrices are column-major". Verified milder than it reads: the entry states the exception loudly *and* gives its reason ("the contraction with the forest's amplitude vector is the only read the engine makes of it... which is why the contract is stated"). Residual risk is the consumer who skims `const double* basis` against the global rule. | `[[inst/include/dbarts/dbarts.h:976-982@7a8c7286]]` vs `[[inst/include/dbarts/dbarts.h:67@7a8c7286]]` | **BREAKING** if flipped later | Rename the param `basisRowMajor` so the exception shows at the call site; flipping throws away the stated reason. | 0-5 | rename pre-RC, else **decline** |
| A12 | **`dbarts.h` clobbers a consumer's `USE_FC_LEN_T`**: defined unguarded at `[[inst/include/dbarts/dbarts.h:81-83@7a8c7286]]`, `#undef`ed unconditionally at `[[inst/include/dbarts/dbarts.h:93@7a8c7286]]`, unlike the correctly guarded `R_NO_REMAP` dance beside it. Confirmed by compiling. Inert on R >= 4.3, live on R 4.0-4.2, which `DESCRIPTION:24` still supports. Not ABI. | `[[inst/include/dbarts/dbarts.h:81-93@7a8c7286]]` | cosmetic-but-live | Guard it like `R_NO_REMAP`. | ~3 | **pre-RC** |
| A13 | **Flat-creatable families with unreadable outputs.** *Heteroscedastic*: no `variance`/`varianceTest` in `dbarts_results` at all (R has both), `setSigma` refuses, and the variance forest is excluded from `numForests` - a creatable family with no readable variance, the worst gap found. *Ordinal*: no `cutpoints` field, so `getLatents`' z cannot be mapped to probabilities. `groupEffects` is documented `numGroups x ...` with **no entry returning `numGroups`**. And `setSigma` is write-only with no getter, though `dispersion` exists precisely so a host can read a scalar mid-sweep. | `[[src/C_interface.cpp:636-638@7a8c7286]]`; `[[src/C_interface.cpp:761-764@7a8c7286]]`, `[[src/C_interface.cpp:971-973@7a8c7286]]`, `[[src/C_interface.cpp:200@7a8c7286]]`, `[[src/C_interface.cpp:784-788@7a8c7286]]`; R side `[[src/R_interface_bartcore.cpp:4379@7a8c7286]]`, `[[src/R_interface_bartcore.cpp:4392-4396@7a8c7286]]` | *additive* | Append the four channels. | ~43 | **pre-RC anyway** - appends are free before the freeze |
| A14 | Callback puts `userData` before the handle - the only break in the handle-first rule. | `[[src/R_interface_bartcore.cpp:385-388@7a8c7286]]` | judgment | `userData`-first is standard C. Record the rationale so it doesn't read as an oversight. | ~2 | **decline the change, add the note** |
| A15 | Two gate weaknesses: the only real-consumer compile gate `exit_file`s unless `CI` is set; the three `*_INIT` macros are checked by nothing in dbarts's own build (a short one is legal C and silently zero-fills; counts currently correct, 12/12, 12/12, 14/14). | `[[inst/tinytest/test-capi.R:47-56@7a8c7286]]`; `src/C_interface.cpp` | process | Verify CI sets `CI`; `static_assert` each initializer. | ~6 | **pre-RC** |
| A16 | `setActiveRows` takes a 0/1 mask as `const double*` where every other flag is `int`; `drawLatents`/`workingResponse` mix count conventions in one signature (`numObservations` precedes its arrays, `numCutpoints` follows `cutpoints`). | `[[inst/tinytest/test-capi.R:1101-1102@7a8c7286]]`, `[[inst/tinytest/test-capi.R:525-528@7a8c7286]]` | cosmetic | Deliberate R-nativeness on the first. | ~6 | defer/decline, record which |

**Nothing to report, C side.** The hash is sound: it folds field names, offsets and struct sizes; `DBARTS_ALIGN_ASSERT` static_asserts every field offset is pointer-width so the "pointer units" division cannot alias two layouts; it folds enumerator names *and* values; and it stringizes return and parameter types, so `int`->`size_t` on a same-name parameter does move it (`[[src/C_interface.cpp:403-421@7a8c7286]]`, `[[src/C_interface.cpp:441@7a8c7286]]`; `[[dbarts.h:540@7a8c7286]]`). Registration cannot drift - the table comes from the same X-macro, a list entry with no implementation is a link error, and a per-entry full-type `static_assert` binds the Doxygen prototypes to the list: 47 entries == 47 definitions == 47 prototypes, no dead declarations, no unregistered exports (`[[src/R_interface.cpp:272-292@7a8c7286]]`; `[[src/C_interface.cpp:1169-1172@7a8c7286]]`). Out-params are LAST wherever they exist; the handle is first in all 44 `dbarts_sampler_*`; `(ptr, count)` order is uniform; flags are uniformly `int`; no struct crosses by value; no bitfields; no enum is ever a field or parameter type. Header compiles clean as C99/C11/C++ under `-pedantic -Wall -Wextra`, with and without stubs.
**The TODO's "dbarts.h reshape" is LANDED** (`[[docs/plans/archive/dbarts-h-reshape.md:1-7@7a8c7286]]`); `[[TODO:300-305@7a8c7286]]` keeps only freeze-time re-verification of the four sister packages. Everything still on the header's books is additive-append and explicitly post-RC (`[[docs/plans/archive/c-api-growth.md:645-722@7a8c7286]]`).

## B. R API

| # | What | Evidence | Class | Fix | Cost | Rec |
|---|---|---|---|---|---|---|
| B1 | **`predict()` spells one concept three ways, and the wrong spelling is silently swallowed.** `offset` is pos-3 before `type` on `bart`, pos-4 before `type` on `rbart`, pos-4 *after* `type` on `bartMultinomial`, named **`offset.test`** on `bartNegbin`, absent on `bartOrdinal`/`bartHurdle`. `refuseUnusedGenericArgs` refuses only names in the per-class `reasons` lists, which hold `forest`/`contribution`/`sample`/`vars` and never an offset name; `predict.bart`/`predict.rbart` don't call it at all. So `predict(gaussFit, nd, offset.test = o)` and `predict(negbinFit, nd, offset = o)` both return an answer with the offset dropped. `weights` is likewise bart/rbart-only and swallowed elsewhere. Compounded by C1: none of these signatures is rendered in any `\usage`. | `[[R/generics.R:252@7a8c7286]]`, `[[R/generics.R:2145@7a8c7286]]`, `[[R/generics.R:1176@7a8c7286]]`, `[[R/generics.R:1706@7a8c7286]]`, `[[R/generics.R:1464-1471@7a8c7286]]`, `[[R/generics.R:2098-2105@7a8c7286]]`; helper `[[R/generics.R:1859-1874@7a8c7286]]`; lists `[[R/generics.R:955@7a8c7286]]`, `[[R/generics.R:1292@7a8c7286]]`, `[[R/generics.R:1571@7a8c7286]]`, `[[R/generics.R:1996@7a8c7286]]` | **BREAKING** | Rename negbin's `offset.test` -> `offset` (3 of 4 siblings already agree); add `offset`/`offset.test`/`weights` to every family's `reasons` list so a mismatch is refused by name. | ~25 | **pre-RC** |
| B2 | **The third positional argument of `predict()` means a different thing per family**: `type` on multinomial/ordinal/negbin/hurdle, `offset` on bart, `group.by` on rbart. `predict(fit, nd, "ppd")` is valid and differently wrong depending on class. | `[[R/generics.R:249-260@7a8c7286]]`, `[[R/generics.R:1172-1180@7a8c7286]]`, `[[R/generics.R:2141-2152@7a8c7286]]` | **BREAKING** | Move `offset`/`weights`/`group.by` after `type` so every method reads `(object, newdata, type, ...)`. | ~10 | **pre-RC** |
| B3 | **Deprecation scaffolding ships into 1.0-0, on one method only.** `predict.rbart` warns `"argument 'value' has been deprecated; use 'type' instead"` and `"type of 'post-mean' for predict deprecated; use 'ev' instead"`. Neither shim exists on `predict.bart`, the older surface. | `[[R/generics.R:2158-2169@7a8c7286]]` | **BREAKING** - removing after release needs its own deprecation cycle | Delete both shims and their `dotsList` plumbing. | ~12 removed | **pre-RC** |
| B4 | **`getSigmas(result)` and `getSumsOfSquaredResiduals(result)` declare a formal they never read** - only `getLatents` uses it. The Rd concedes it ("Accepted but not used by..."), so a caller preallocating a buffer has it silently discarded. | `[[R/dbarts.R:1675-1680@7a8c7286]]`, `[[R/dbarts.R:1686-1690@7a8c7286]]` (bodies `.Call(..., ptr)` only) vs `[[R/dbarts.R:1663-1674@7a8c7286]]`; `[[man/dbartsSampler-class.Rd:290-291@7a8c7286]]` | **BREAKING** | Drop from both, or honour it. | ~4 | **pre-RC** |
| B5 | **The `forest` argument carries three conventions across four sibling accessors**: no default on `getForestFits`/`getForestVariableCounts`, `NULL` (= all, stacked) on `getForestAmplitudes`, `1L` on `getCalibration`/`setCalibration`. On an ordinary single-forest sampler `getCalibration()` works and `getForestFits()` is a missing-argument error. | `[[R/dbarts.R:1691@7a8c7286]]`, `[[R/dbarts.R:1716@7a8c7286]]`, `[[R/dbarts.R:1707@7a8c7286]]`, `[[R/dbarts.R:1730@7a8c7286]]`, `[[R/dbarts.R:1739@7a8c7286]]` | **BREAKING** (adding a default is additive; changing `NULL` semantics later is not) | Default all four to `1L`; keep `NULL` = all only where a stacked read is defined. | ~6 | **pre-RC** |
| B6 | `fitted()`'s `type` vocabulary diverges from `extract()`/`predict()` on the same fit: `fitted.bartNegbin` offers `c("ev","bart")` with no `"ppd"` though both siblings have it; `"class"` exists on `fitted.bartMultinomial`/`fitted.bartOrdinal` but on no `predict`, so a predicted class at `newdata` is unreachable. | `[[R/generics.R:1657@7a8c7286]]` vs `[[R/generics.R:1579@7a8c7286]]`, `[[R/generics.R:1705@7a8c7286]]`; `[[R/generics.R:1085@7a8c7286]]`, `[[R/generics.R:1394@7a8c7286]]` vs `[[R/generics.R:1175@7a8c7286]]`, `[[R/generics.R:1467@7a8c7286]]` | *additive* | Add `"ppd"` to negbin's `fitted`, `"class"` to the two `predict`s. | ~20 | defer |
| B7 | `setCalibration`'s `prior.mean` exists only to `stop()` and holds positional slot 3 ahead of `forest`/`updateState`. Deliberate and documented. | `[[R/dbarts.R:1735-1752@7a8c7286]]`; `[[man/dbartsSampler-class.Rd:209-210@7a8c7286]]` | *additive* | Leave, or move last. | ~3 | defer - the discoverable refusal earns the slot |
| B8 | `fitted.bartHurdle`'s `sample = "train"` is a bare string where every sibling uses the `c("train","test")` idiom, and it reaches `extract` unvalidated. | `[[R/generics.R:2067@7a8c7286]]`, `[[R/generics.R:2073@7a8c7286]]` vs `[[R/generics.R:853@7a8c7286]]`, `[[R/generics.R:969@7a8c7286]]`, `[[R/generics.R:1300@7a8c7286]]`, `[[R/generics.R:1579@7a8c7286]]`, `[[R/generics.R:2004@7a8c7286]]` | cosmetic | Use the choice vector + `validateSample`. | ~2 | pre-RC (rides with any generics edit) |
| B9 | 17-18 `stop()` messages still name arguments bare against error-style R1; several miss R12's `"must be one of"` shape, and several sit on user-facing `predict`/`extract` paths. Plus 7 C-side retired-adjective messages. Slice L is explicitly still pending. | `[[R/generics.R:1201@7a8c7286]], [[R/generics.R:1491@7a8c7286]], [[R/generics.R:1730@7a8c7286]], [[R/generics.R:1836@7a8c7286]], [[R/generics.R:1846@7a8c7286]], [[R/generics.R:1887@7a8c7286]], [[R/generics.R:2535@7a8c7286]]`; `[[R/dbarts.R:1090@7a8c7286]], [[R/dbarts.R:1157@7a8c7286]]`; `[[R/xbart.R:594@7a8c7286]]`; `[[R/data.R:1232@7a8c7286]]`; `[[R/utility.R:330@7a8c7286]], [[R/utility.R:411@7a8c7286]]`; `[[R/guessNumCores.R:5@7a8c7286]], [[R/guessNumCores.R:8@7a8c7286]]`; `[[R/rbart.R:325@7a8c7286]]`; `[[R/bart.R:1333@7a8c7286]]`; `[[src/R_interface_bartcore.cpp:1407@7a8c7286]], [[src/R_interface_bartcore.cpp:1431@7a8c7286]], [[src/R_interface_bartcore.cpp:1587@7a8c7286]], [[src/R_interface_bartcore.cpp:1941@7a8c7286]], [[src/R_interface_bartcore.cpp:3214@7a8c7286]], [[src/R_interface_bartcore.cpp:3707@7a8c7286]], [[src/R_interface_bartcore.cpp:6227@7a8c7286]]` | cosmetic (text is not API) | Run slice L. | ~31 | **pre-RC** - bounded, mechanical, and wording is what nobody wants to re-touch after a release |
| B10 | `dbartsSampler` mixes the C header's camelCase and R's dotted names in one signature: `run(numBurnIn, numSamples, updateState, n.threads)`. The camelCase half deliberately mirrors `dbarts_sampler_run` and the Rd states the "engine's own terms" rule, but `n.threads`/`n.sweeps`/`x.test`/`offset.test` on the same class don't follow it. Also `growFromRoot(n.sweeps)` vs `bart2(n.grow.sweeps)`. | `[[R/dbarts.R:959-963@7a8c7286]]`, `[[R/dbarts.R:1013@7a8c7286]]`, `[[R/dbarts.R:1084@7a8c7286]]`; `[[dbarts.h:411@7a8c7286]]`; rule `[[man/dbartsSampler-class.Rd:370@7a8c7286]]` | cosmetic | Leave - a real convention, pinned by 201 in-repo uses and the C header. | 0 | **decline** |
| B11 | `forest`, `blocks`, `interactions` are newly exported at 1.0-0 under very generic top-level names, while the prior vocabulary (`cgm`/`normal`/`chi`/`gaussian`/`student`) stays unexported behind a shadow env, shipping only the `dbartsPriors` container - two mechanisms for two argument vocabularies. | `NAMESPACE:22-25`; `[[R/model.R:1670@7a8c7286]]`, `[[R/model.R:1693@7a8c7286]]`, `[[R/model.R:1720@7a8c7286]]`, `[[R/model.R:1761@7a8c7286]]`, `[[R/model.R:107-118@7a8c7286]]`, `[[R/model.R:1796@7a8c7286]]`; eager use `[[R/dbarts.R:426@7a8c7286]]` | *additive* (un-exporting later is breaking) | Keep: the shadow-env route cannot serve `forests = list(forest())` or a `forest()` formula term, and the names read correctly at the call site. | 0 | decline |

## C. Documentation of the surface

`tools::codoc()` returns zero mismatches, and all 46 reference-class method usages match their formals exactly (names, order, defaults), including the multi-line `run`/`predictForests`/`setPredictor`/`setCalibration`/`getTrees`.

| # | What | Evidence | Class | Cost | Rec |
|---|---|---|---|---|---|
| C1 | **15 documented S3 methods have `\alias` but NO `\usage`**: `extract`/`fitted`/`predict`/`print`/`residuals` for `bartOrdinal`, `bartNegbin`, `bartHurdle`. Verified - the usage block covers only the `bartMultinomial` set plus all four `plot`/`summary`. So no signature or default is visible for exactly the divergences B1/B6 name: `predict.bartNegbin(offset.test = NULL)`, `fitted.bartHurdle(sample = "train")`, and the per-generic `type` vocabularies that genuinely differ. | aliases `[[man/bart2.Rd:10-28@7a8c7286]]`; usage `[[man/bart2.Rd:36-108@7a8c7286]]`; code `[[R/generics.R:1297@7a8c7286]], [[R/generics.R:1392@7a8c7286]], [[R/generics.R:1440@7a8c7286]], [[R/generics.R:1464@7a8c7286]], [[R/generics.R:1544@7a8c7286]], [[R/generics.R:1576@7a8c7286]], [[R/generics.R:1656@7a8c7286]], [[R/generics.R:1682@7a8c7286]], [[R/generics.R:1702@7a8c7286]], [[R/generics.R:1775@7a8c7286]], [[R/generics.R:2001@7a8c7286]], [[R/generics.R:2064@7a8c7286]], [[R/generics.R:2080@7a8c7286]], [[R/generics.R:2098@7a8c7286]], [[R/generics.R:2133@7a8c7286]]` | *additive* as docs, but it hides a **BREAKING** divergence | ~25 | **pre-RC** |
| C2 | **`xbart`'s `\value` understates the returned array**: it omits a 6th `"loss"` margin whenever the loss function returns >1 value, omits that the `k` axis drops unconditionally under a hyperprior *regardless of* `drop`, and omits that the leading margin is named `"rep"`. Users index this array. | `[[man/xbart.Rd:130@7a8c7286]]` vs `[[R/xbart.R:515@7a8c7286]], [[R/xbart.R:529@7a8c7286]], [[R/xbart.R:548-554@7a8c7286]], [[R/xbart.R:557@7a8c7286]], [[R/xbart.R:566@7a8c7286]], [[R/xbart.R:568@7a8c7286]]` | *additive* | ~4 | **pre-RC** |
| C3 | **`print.bart` and `print.rbart` are undocumented** - registered and defined but with no `\alias` anywhere, so `?print.bart` fails and there is no `\value`. (The four `bart2` own-class `print` methods are aliased.) Verified. | `NAMESPACE:38,42`; `[[R/generics.R:2975@7a8c7286]], [[R/generics.R:2981@7a8c7286]]` | *additive* | ~6 | **pre-RC** - `print` is the first thing a user calls |
| C4 | **`dbartsSampler$predict`'s return shape is documented nowhere**, though `$predictForests`, `$getFitsWithoutOffset` and `$getForestFits` all get full shapes. This is the main read for the embedding surface the package exists for. | `[[man/dbartsSampler-class.Rd:414@7a8c7286]]` vs `[[man/dbartsSampler-class.Rd:434@7a8c7286]]`; `[[R/dbarts.R:1145@7a8c7286]]` | *additive* | ~2 | **pre-RC** |
| C5 | **`rbart`'s `$fit` shape and `$n.chains` presence contradict the shared-elements claim.** `[[man/rbart.Rd:138@7a8c7286]]` says an `rbart` object "contains all of the same elements of an object of class `bart`" and `[[man/bart.Rd:317@7a8c7286]]` documents `fit` as one sampler, but `rbart_vi`'s `$fit` is a *list*. Separately `[[man/bart.Rd:155@7a8c7286]]`/`[[man/bart2.Rd:179@7a8c7286]]` both assert `n.chains` "is tracked regardless on the returned object", but `[[R/rbart.R:1292-1296@7a8c7286]]` sets it only in the `else` branch, so an `rbart_vi` fit on the R-loop path (custom `prior`, or a `callback`) has no `$n.chains` at all. | `[[R/rbart.R:430@7a8c7286]]`, `[[R/rbart.R:1292-1296@7a8c7286]]`; `[[R/diagnostics.R:10@7a8c7286]], [[R/diagnostics.R:16@7a8c7286]]` | *additive* | ~5 | **pre-RC** - a `\value` claim users code against |
| C6 | Undocumented components on returned fits: `status` (aft censoring), `weights`/`weights.test`, and for `rbart` also `seed`, `group.by`/`group.by.test`. | `[[R/bart.R:352@7a8c7286]]`, `[[R/bart.R:2880-2887@7a8c7286]]`; `[[R/rbart.R:1060@7a8c7286]], [[R/rbart.R:1068@7a8c7286]], [[R/rbart.R:1070-1076@7a8c7286]], [[R/rbart.R:1298@7a8c7286]]` | *additive* | ~6 | defer (fold into C5's edit) |
| C7 | Three small contradictions: `n.samples` is "A positive integer" in `[[man/dbarts.Rd:54@7a8c7286]]` but "non-negative... `0` is accepted" in `[[man/dbartsControl.Rd:33@7a8c7286]]`, and the code agrees with the latter (`[[R/dbarts.R:297@7a8c7286]]`); `[[man/xbart.Rd:126@7a8c7286]]` says `loss` takes 2 arguments while `[[man/xbart.Rd:58@7a8c7286]]`, its own example and `[[R/xbart.R:582@7a8c7286]]` all use 3; `[[man/bart.Rd:22@7a8c7286]]` writes `sigest = NA` where the formal is `NA_real_` (the sibling pages spell it correctly). | as cited | cosmetic | ~3 | **pre-RC** (C7a, C7b), defer C7c |
| C8 | `n.threads` shown with no default in three `predict` `\usage` entries; `print`/`format.dbartsVarianceForest` aliased but absent from `\usage` and `\value`. Both rendering gaps, not false claims. | `[[man/bart.Rd:52@7a8c7286]]`, `[[man/bart2.Rd:86@7a8c7286]]`, `[[man/rbart.Rd:61@7a8c7286]]`; `[[man/varianceForest.Rd:3-9@7a8c7286]]` | cosmetic | ~6 | defer |

## D. Recorded surface decisions, and categories with NOTHING to report

**Decisions.** Every substantive decision in `public-surface.md`/`consumer-spec-surface.md` is implemented as decided - argument names, defaults, return shapes, export boundary, entry-point signatures, error-shape rules. Six doc-drift items, code right in every case: §6's "C++ headers ship for `LinkingTo` use without ABI promises" is **not implemented** and should be struck rather than implemented (stan4bart ported entirely onto the flat API; shipping 30k lines of internals invites the coupling the flat API exists to end); §1's `startThreads`/`stopThreads` no-ops and §8's `dbartsData(treatment =)` describe absent code; §3's `dbartsControl@family` slot and rbart's probit-only claim are superseded; §8's `setTreatment`/`bcfGlue` "to re-sign" are already gone; §6's "callback deferred" landed anyway (`[[dbarts.h:717@7a8c7286]]`). The one Open marker, `[[public-surface.md:220@7a8c7286]]` ("whether `family` also belongs on `xbart`"), is **resolved in code and not in the doc** - `[[R/xbart.R:26@7a8c7286]]` carries the full four-token family. ~12 lines; **pre-RC**, since that file is what a reviewer reads to certify the freeze and four of its claims describe code that does not exist. Also stale: `[[man/dbarts-package.Rd:43@7a8c7286]]` and `[[inst/NEWS.Rd:564-567@7a8c7286]]` still call the API hash an "exact signature check" and omit that it moves on additive releases; `[[ordinal.md:505@7a8c7286]]`, `[[negative-binomial.md:664@7a8c7286]]`, `[[heteroscedastic.md:562@7a8c7286]]` each claim "no dbarts.h exposure in v1", contradicted by `[[src/C_interface.cpp:636-638@7a8c7286]]`.

**Nothing to report.** *`...` on entry points*: no exported top-level function takes `...`, so a misspelled argument is refused by R itself; only the four generics and the two `pdbart` pass-throughs carry it, all legitimately. *S3 grid*: complete - all six fit classes carry `extract`/`fitted`/`plot`/`plotTree`/`predict`/`print`/`residuals`/`summary`/`survivalProbabilities`; `as_draws_array`/`as_draws_df` cover all six via the `.onLoad` hook (`[[R/hooks.R:6-33@7a8c7286]]`); every `summary.*` returns `"summary.bart"` or `"summary.bartHurdle"` and both have print methods; no `logLik`, consistently - it is a `type = "loglik"` channel on `extract`. *Removed exports*: nothing exported by the installed 0.9.34 is gone except the `.__C__dbartsSampler` artefact; 13 names are new at 1.0-0 (`blocks`, `dbartsDrawLatents`, `dbartsPriors`, `dbartsSpec`, `dbartsValidateComposition`, `dbartsWorkingResponse`, `forest`, `interactions`, `plotTree`, `samplePriorPredictive`, `sparseFactor`, `survivalProbabilities`, `varianceForest`). *`combineChains`*: uniform chain-major collapse across every field and family (`[[R/bart.R:6-10@7a8c7286]]`, `[[R/bart.R:47-70@7a8c7286]]`; multinomial's own reshape at `[[R/diagnostics.R:352-357@7a8c7286]]` agrees). *`bart()`'s all-lowercase names*: documented as the BayesTree-compatible surface (`[[man/bart.Rd:16@7a8c7286]]`, `[[man/bart.Rd:147@7a8c7286]]`). *`dbartsSpec`'s narrower 8-token `family` and its `parentEnv` formal*: documented as deliberate (`[[man/dbartsSpec.Rd:28@7a8c7286]]`, `[[man/dbartsSpec.Rd:33@7a8c7286]]`, `[[man/dbartsSpec.Rd:48@7a8c7286]]`). *Sibling defaults that differ* (`n.samples` 500/1500/200, `n.burn` 500/1500/`c(200,150)`, `n.thin` 1/5, `keepTrees` FALSE/TRUE, `verbose` TRUE/FALSE, `k` NULL vs 2.0, `n.threads` `min(guessNumCores(), n.chains)` vs `guessNumCores()`): every one is documented at its own `\item` (`[[man/bart2.Rd:203@7a8c7286]]`, `[[man/rbart.Rd:86@7a8c7286]]`, `[[man/bart.Rd:147@7a8c7286]]`) - no undocumented divergence found. *Deliberate, documented C-API coverage gaps* (no action): multinomial and hurdle creation, per-observation predictor mutation, `setCutPoints`/`setData`, per-forest varcount, xbart, CSC mutation, the monotone direction vector; undeclared but purely additive: `$setControl`/`$setModel`, `$getSigmas`/`$getFitsWithoutOffset`/`$getSumsOfSquaredResiduals`, `$growFromRoot`, `$installTrees`, an atomic `setTestPredictorAndOffset`.
