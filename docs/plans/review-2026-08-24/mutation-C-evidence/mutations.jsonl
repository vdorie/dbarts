{"id": "cv1", "file": "R/generics.R", "find": "    apply(result, length(dim(result)), mean)\n  } else {\n    mean(result)", "repl": "    apply(result, length(dim(result)), median)\n  } else {\n    median(result)", "note": "fitted.bart returns posterior median not mean", "files": ["test-generics-correctValues.R"]}
{"id": "cv2", "file": "R/generics.R", "find": "  object$y - fitted.bart(object, type = type, sample = \"train\", ...)", "repl": "  object$y - fitted.bart(object, type = \"ev\", sample = \"train\", ...)", "note": "residuals.bart ignores type, always ev", "files": ["test-generics-correctValues.R"]}
{"id": "cv3", "file": "R/bart.R", "find": "      matrix(aperm(samples, c(2L, 1L, 3L)), prod(x[1L:2L]), x[3L])", "repl": "      matrix(aperm(samples, c(1L, 2L, 3L)), prod(x[1L:2L]), x[3L])", "note": "combineChains flattens sample-major instead of chain-major", "files": ["test-generics-correctValues.R"]}
{"id": "ge1", "file": "R/generics.R", "find": "      stop(\"predict requires bart to be called with 'keeptrees' == TRUE\")", "repl": "      stop(\"predict requires the fit to retain its sampler\")", "note": "predict keeptrees refusal text unpinned", "files": ["test-generics-errors.R"]}
{"id": "ge2", "file": "R/generics.R", "find": "          \"extracting trees requires bart2 to be called with 'keepTrees' == TRUE\"", "repl": "          \"extracting trees requires a saved tree store\"", "note": "extract type=trees keepTrees refusal text", "files": ["test-generics-errors.R"]}
{"id": "ge3", "file": "R/generics.R", "find": "        \"cannot extract train sample predictions; bart2 must be called with 'keepTrainingFits' == TRUE\"", "repl": "        \"cannot extract train sample predictions; no training fits retained\"", "note": "extract keepTrainingFits refusal text", "files": ["test-generics-errors.R"]}
{"id": "gi1", "file": "R/generics.R", "find": "    est <- apply(draws, obsMargin, mean)", "repl": "    est <- apply(draws, obsMargin, median)", "note": "posteriorInterval est column is median not mean", "files": ["test-generics-intervals.R"]}
{"id": "gi2", "file": "R/generics.R", "find": "    result <- cbind(est, bounds[1L, ], bounds[2L, ])", "repl": "    result <- cbind(est, bounds[2L, ], bounds[1L, ])", "note": "posteriorInterval band bounds reversed", "files": ["test-generics-intervals.R"]}
{"id": "gi3", "file": "R/generics.R", "find": "  probs <- c((1 - ci.level) / 2, 1 - (1 - ci.level) / 2)", "repl": "  probs <- c(0.025, 0.975)", "note": "posteriorInterval ignores ci.level, always 95pct", "files": ["test-generics-intervals.R"]}
{"id": "gi4", "file": "R/generics.R", "find": "      length(ci.level) != 1L ||", "repl": "      FALSE ||", "note": "posteriorInterval drops length-1 validation of ci.level", "files": ["test-generics-intervals.R"]}
{"id": "gm1", "file": "R/generics.R", "find": "  n.threads <- as.integer(n.threads)[1L]", "repl": "  n.threads <- -99L", "note": "predict.bart passes a nonsense n.threads down", "files": ["test-generics-multithreaded.R"]}
{"id": "gm2", "file": "R/dbarts.R", "find": "    predict = function(x.test, offset.test, n.threads = control@n.threads) {", "repl": "    predict = function(x.test, offset.test, n.threads = 1L) {", "note": "sampler$predict default n.threads pinned to 1", "files": ["test-generics-multithreaded.R"]}
{"id": "pd1", "file": "R/partialDependence.R", "find": "      offset <- offset + numObservations * numLevels[j]", "repl": "      offset <- offset + numObservations", "note": "pdbart non-keepTrees block offset advances by one block only", "files": ["test-pdbart.R"]}
{"id": "pd2", "file": "R/partialDependence.R", "find": "  if (!is.null(bartCall[[samplerOnlyName]])) {", "repl": "  if (FALSE) {", "note": "pdbart sampleronly override guard removed", "files": ["test-pdbart.R"]}
{"id": "pd3", "file": "R/partialDependence.R", "find": "        x.test[, xind[j]] <- levs[[j]][i]\n\n        pred <-\n          if (sampler$control@n.chains > 1L) {\n            as.vector(apply(sampler$predict(x.test), c(2L, 3L), mean))\n          } else {\n            apply(sampler$predict(x.test), 2L, mean)\n          }", "repl": "        x.test[, xind[j]] <- levs[[j]][i]\n\n        pred <-\n          if (sampler$control@n.chains > 1L) {\n            as.vector(apply(sampler$predict(x.test), c(2L, 3L), median))\n          } else {\n            apply(sampler$predict(x.test), 2L, median)\n          }", "note": "pdbart keepTrees arm averages with median", "files": ["test-pdbart.R"]}
{"id": "pd4", "file": "R/plot.R", "find": "  rgy <- range(x$fd)\n  for (i in xind) {", "repl": "  rgy <- range(x$fd)\n  for (i in xind[1L]) {", "note": "plot.pdbart renders only the first requested predictor", "files": ["test-pdbart.R"]}
{"id":"mc4","file":"R/bartcore.R","find":"  invisible(.Call(C_dbarts_bartcore_setCounts, bcSampler$ptr, counts))","repl":"  invisible(bcSampler$ptr)","note":"bartcoreSetCounts never reaches the engine","files":["test-multinomial-counts-mutation.R"]}
{"id":"mc5","file":"R/bartcore.R","find":"  if (any(rowSums(counts) < 1)) {\n    stop(\"every multinomial count row must have at least one trial (n_i >= 1)\")\n  }","repl":"  if (FALSE) {\n    stop(\"every multinomial count row must have at least one trial (n_i >= 1)\")\n  }","note":"bartcoreSetCounts drops the at-least-one-trial refusal","files":["test-multinomial-counts-mutation.R"]}
{"id":"mts3","file":"R/dbarts.R","find":"    storeState = function() {","repl":"    storeState = function() { # nolint\n      NULL","note":"storeState becomes a no-op","files":["test-mutate-then-serialize.R","test-data-categorical-wide.R"]}
{"id":"mc5","file":"R/bartcore.R","find":"  counts <- asCountMatrix(counts)\n  # counts is column-major (category-major), the layout the combiner reads","repl":"  counts <- asCountMatrix(counts)[, c(2L, 1L)]\n  # counts is column-major (category-major), the layout the combiner reads","note":"bartcoreSetCounts installs the first two categories swapped","files":["test-multinomial-counts-mutation.R"]}
{"id":"mts3","file":"R/dbarts.R","find":"    storeState = function(ptr = getPointer()) {","repl":"    storeState = function(ptr = getPointer()) {\n      return(invisible(NULL))","note":"storeState becomes a no-op","files":["test-mutate-then-serialize.R","test-data-categorical-wide.R"]}
{"id":"se1","file":"R/dbarts.R","find":"      \"Draws tree structure from prior\"\n      ptr <- getPointer()\n      .Call(C_dbarts_bartcore_sampleTreesFromPrior, ptr)","repl":"      \"Draws tree structure from prior\"\n      ptr <- getPointer()","note":"sampleTreesFromPrior never reaches the engine","files":["test-generics-sequentialExecution.R","test-calibration-prior-draws.R"]}
{"id":"me4","file":"R/model.R","find":"    stop(\"'tree.prior' must be a tree prior specification; see ?dbartsPriors\")","repl":"    stop(\"'tree.prior' is not a prior; see ?dbartsPriors\")","note":"tree.prior refusal text unpinned","files":["test-model-errors.R"]}
{"id":"hm1","file":"R/model.R","find":"    list(vars = vars, n.trees = n.trees, base = base, power = power),","repl":"    list(vars = vars, n.trees = 40L, base = base, power = power),","note":"varianceForest ignores the requested n.trees","files":["test-heteroscedastic-mutation.R"]}
{"id": "a1", "file": "R/bart.R", "find": "        (log(times[j]) - linearPredictor) / scale,\n        lower.tail = FALSE", "repl": "        (log(times[j]) - linearPredictor) / scale,\n        lower.tail = TRUE", "note": "survivalProbabilities reports the lower tail (1 - S)", "files": ["test-aft.R"]}
{"id": "a2", "file": "R/dbarts.R", "find": "    stop(\"survival times must be finite and positive\")", "repl": "    invisible(NULL)", "note": "aft ingestion drops the positive-time refusal", "files": ["test-aft.R"]}
{"id": "a3", "file": "src/bartcore/model.hpp", "find": "      double draw =\n        ext_rng_simulateLowerTruncatedNormal(rng, mean, sd, censorBound_[k]);\n      logT_[i] = !std::isnan(draw) ? draw : censorBound_[k];", "repl": "      logT_[i] = censorBound_[k];", "note": "AFT never imputes a censored latent (pins it at the bound)", "files": ["test-aft.R"]}
{"id": "ai1", "file": "src/R_interface.cpp", "find": "    if (indices[i] < 1 || indices[i] > dims[i])", "repl": "    if (false)", "note": "assignInPlace bounds check removed", "files": ["test-assignInPlace-bounds.R"]}
{"id": "ai2", "file": "src/R_interface.cpp", "find": "    if (XLENGTH(indexExpr) != numDims - 1)\n      Rf_error(\"all but the first array dimension must be specified\");", "repl": "    if (false)\n      Rf_error(\"all but the first array dimension must be specified\");", "note": "assignInPlace index-arity check removed", "files": ["test-assignInPlace-bounds.R"]}
{"id": "ai3", "file": "src/R_interface.cpp", "find": "  if (XLENGTH(indexExpr) < 1)\n    Rf_error(\"assignInPlace: 'index' must be non-empty\");", "repl": "  if (false)\n    Rf_error(\"assignInPlace: 'index' must be non-empty\");", "note": "assignInPlace empty-index check removed", "files": ["test-assignInPlace-bounds.R"]}
{"id": "ag1", "file": "src/R_interface_bartcore.cpp", "find": "      double sign = 2.0 * in.y[i] - 1.0;", "repl": "      double sign = 1.0;", "note": "probit augmentation always truncates below (ignores y)", "files": ["test-augmentation.R"]}
{"id": "ag2", "file": "src/R_interface_bartcore.cpp", "find": "    result[i] = value - (in.offset != NULL ? in.offset[i] : 0.0);", "repl": "    result[i] = value;", "note": "working response does not remove the offset", "files": ["test-augmentation.R"]}
{"id": "ag3", "file": "src/R_interface_bartcore.cpp", "find": "      long reps = in.weights != NULL ? std::lround(in.weights[i]) : 1L;", "repl": "      long reps = 1L;", "note": "logistic augmentation ignores the case weight (PG shape 1)", "files": ["test-augmentation.R"]}
{"id": "bw1", "file": "R/bart.R", "find": "  # needed to extract ppd; mirrors bart2's packageBartResults\n  if (!is.null(sampler$data@weights) && length(sampler$data@weights) > 0L) {\n    result$weights <- sampler$data@weights", "repl": "  # needed to extract ppd; mirrors bart2's packageBartResults\n  if (FALSE) {\n    result$weights <- sampler$data@weights", "note": "bart() tail no longer attaches $weights", "files": ["test-bart-weights-parity.R"]}
{"id": "bw2", "file": "R/generics.R", "find": "      sd <- sd / rep(sqrt(weights), each = n.draws)", "repl": "      sd <- sd", "note": "pointwise loglik ignores case weights", "files": ["test-bart-weights-parity.R", "test-pointwise-loglik.R"]}
{"id": "bw3", "file": "R/bart.R", "find": "  if (isTRUE(ndpost <= 0L)) {", "repl": "  if (FALSE) {", "note": "bart() drops the thinned-to-zero ndpost refusal", "files": ["test-bart-weights-parity.R"]}
{"id": "bg1", "file": "R/bart.R", "find": "  if (is.na(n.grow.sweeps) || n.grow.sweeps < 0L) {", "repl": "  if (FALSE) {", "note": "bart2 drops the non-negative n.grow.sweeps refusal", "files": ["test-bart2-grow-from-root.R"]}
{"id": "bg2", "file": "R/bart.R", "find": "  if (!is.null(warm.start) && n.grow.sweeps > 0L) {", "repl": "  if (FALSE) {", "note": "bart2 drops the warm.start/n.grow.sweeps conflict refusal", "files": ["test-bart2-grow-from-root.R"]}
{"id": "bg3", "file": "R/bart.R", "find": "    sampler$growFromRoot(n.grow.sweeps, updateState = FALSE)", "repl": "    sampler$growFromRoot(1L, updateState = FALSE)", "note": "bart2 grows one sweep regardless of n.grow.sweeps", "files": ["test-bart2-grow-from-root.R"]}
{"id": "bf1", "file": "R/generics.R", "find": "    result[,, j] <- (g %*% t(basis)) * fits[,, k]", "repl": "    result[,, j] <- (g %*% t(basis)) + fits[,, k]", "note": "extract forest contribution adds instead of multiplying", "files": ["test-bcf-forest-channel.R"]}
{"id": "bf2", "file": "R/bart.R", "find": "    result$n.forests <- numForests", "repl": "    result$n.forests <- 1L", "note": "packaged n.forests pinned at 1", "files": ["test-bcf-forest-channel.R"]}
{"id": "bf3", "file": "R/generics.R", "find": "  fits <- reshapeChainedChannel(object$forestFits, n.chains, TRUE, 2L)", "repl": "  fits <- reshapeChainedChannel(object$forestFits, n.chains, FALSE, 2L)", "note": "extractForest reshapes uncombined before slicing", "files": ["test-bcf-forest-channel.R"]}
{"id": "bz1", "file": "src/bartcore/combiner.hpp", "find": "      if (std::fabs(m) < zeroMultiplierTolerance) {\n        glue_.forestResponse[i] = 0.0;\n        glue_.forestWeights[i] = 0.0;\n        continue;\n      }", "repl": "      if (false) {\n        glue_.forestResponse[i] = 0.0;\n        glue_.forestWeights[i] = 0.0;\n        continue;\n      }", "note": "formForestResponse near-zero multiplier snap removed", "files": ["test-bcf-zero-multiplier.R"]}
{"id": "bz2", "file": "src/bartcore/combiner.hpp", "find": "      glue_.forestWeights[i] = std::fabs(m) < zeroMultiplierTolerance\n        ? 0.0\n        : (w == nullptr ? 1.0 : w[i]) * m * m;", "repl": "      glue_.forestWeights[i] = (w == nullptr ? 1.0 : w[i]) * m * m;", "note": "formForestVetoWeights near-zero snap removed (leg A finding 2)", "files": ["test-bcf-zero-multiplier.R"]}
{"id": "bh1", "file": "src/bartcore/model.hpp", "find": "    return k > maxDraw ? maxDraw : k;", "repl": "    return k;", "note": "sampled k is no longer capped at 1e6", "files": ["test-binaryResponse-hyperprior.R"]}
{"id": "bh2", "file": "src/bartcore/model.hpp", "find": "    double shape = 0.5 * (totalNumLeaves + degreesOfFreedom);", "repl": "    double shape = 0.5 * totalNumLeaves;", "note": "k posterior shape ignores the hyperprior degrees of freedom", "files": ["test-binaryResponse-hyperprior.R"]}
{"id": "bh3", "file": "src/bartcore/model.hpp", "find": "    if (std::fabs(scale) <= DBL_MAX) rate += 0.5 / (scale * scale);", "repl": "    rate += 0.5;", "note": "k posterior rate ignores the hyperprior scale (Inf treated as 1)", "files": ["test-binaryResponse-hyperprior.R"]}
{"id": "cp1", "file": "R/model.R", "find": "resolvePriorScale <- function(node.prior, node.hyperprior) {\n  if (is.na(node.prior@prior.sd)) {\n    return(node.prior@prior.scale)\n  }", "repl": "resolvePriorScale <- function(node.prior, node.hyperprior) {\n  if (is.na(node.prior@prior.sd)) {\n    return(NA_real_)\n  }", "note": "a named prior.scale is dropped (falls back to the response range)", "files": ["test-calibration-prior-draws.R"]}
{"id": "cp2", "file": "R/model.R", "find": "  node.prior@prior.sd * node.hyperprior@k", "repl": "  node.prior@prior.sd", "note": "named prior sd not converted to a scale by k", "files": ["test-calibration-prior-draws.R"]}
{"id":"bz3","file":"R/bartcore.R","find":"      b.prior.variance,\n      0,\n      update.b\n    ))","repl":"      b.prior.variance,\n      0,\n      TRUE\n    ))","note":"bcf glue ignores update.b (b is always resampled)","files":["test-bcf-zero-multiplier.R"]}
{"id":"cp3","file":"R/spec.R","find":"  if (!is.null(monotoneDirections)) {\n    attr(model, \"monotone\") <- monotoneDirections\n  }","repl":"  if (FALSE) {\n    attr(model, \"monotone\") <- monotoneDirections\n  }","note":"resolved monotone directions never reach the engine","files":["test-calibration-prior-draws.R"]}
{"id": "dca1", "file": "R/utility.R", "find": "      columnTypes[[j]] <- if (is.ordered(column)) {\n        ORDINAL_VARIABLE\n      } else {\n        CATEGORICAL_VARIABLE\n      }", "repl": "      columnTypes[[j]] <- CATEGORICAL_VARIABLE", "note": "ordered factors typed categorical instead of ordinal", "files": ["test-data-categorical.R"]}
{"id": "dca2", "file": "R/utility.R", "find": "    refactored <- factor(as.character(column), levels = factorLevels[[j]])", "repl": "    refactored <- factor(as.character(column))", "note": "test factors recoded on their own level order, not training's", "files": ["test-data-categorical.R"]}
{"id": "dca3", "file": "R/dbarts.R", "find": "      if (any(data@varTypes == CATEGORICAL_VARIABLE)) {\n        trees <- decodeCategoricalSplits(trees, data@x, data@varTypes)\n      }", "repl": "      if (FALSE) {\n        trees <- decodeCategoricalSplits(trees, data@x, data@varTypes)\n      }", "note": "getTrees skips the categorical direction decode", "files": ["test-data-categorical.R", "test-data-categorical-declared.R", "test-data-categorical-wide.R"]}
{"id": "dcd1", "file": "R/plotTree.R", "find": "    numLevels[j] <- if (!is.null(factorLevels) && !is.null(factorLevels[[j]])) {\n      length(factorLevels[[j]])\n    } else {\n      as.integer(max(x[, j])) + 1L\n    }", "repl": "    numLevels[j] <- as.integer(max(x[, j])) + 1L", "note": "decode pads to the observed max code, not the declared level count", "files": ["test-data-categorical-declared.R", "test-data-categorical-wide.R", "test-data-categorical.R"]}
{"id": "dcd2", "file": "R/plotTree.R", "find": "        paste(rep.int(\"L\", padding), collapse = \"\")", "repl": "        paste(rep.int(\"R\", padding), collapse = \"\")", "note": "unobserved trailing levels decode as R rather than L", "files": ["test-data-categorical-declared.R", "test-data-categorical-wide.R", "test-data-categorical.R"]}
{"id": "dcd3", "file": "src/R_interface_bartcore.cpp", "find": "    if (!store.categoricalValueIsValid(column, values[i]))\n      Rf_error(\"categorical predictor values must be existing category codes\");", "repl": "    if (false)\n      Rf_error(\"categorical predictor values must be existing category codes\");", "note": "setPredictor no longer bounds a categorical code", "files": ["test-data-categorical-declared.R"]}
{"id": "dcm1", "file": "R/data.R", "find": "  weights[subset]\n}", "repl": "  weights\n}", "note": "validateXYWeights ignores subset", "files": ["test-data-compatibility.R", "test-data-formula.R"]}
{"id": "dcm2", "file": "R/data.R", "find": "  list(offset = offset, offsetGivenAsScalar = offsetGivenAsScalar)\n}", "repl": "  list(offset = NULL, offsetGivenAsScalar = offsetGivenAsScalar)\n}", "note": "x/y interface silently discards the user offset", "files": ["test-data-compatibility.R", "test-data-formula.R"]}
{"id": "dfm1", "file": "R/data.R", "find": "        weights.test <- testFrame[[\"(weights)\"]]", "repl": "        weights.test <- NULL", "note": "formula interface drops test weights", "files": ["test-data-formula.R"]}
{"id": "dfm2", "file": "R/data.R", "find": "      if (!dataIsMissing && any(names(data) == testOffsetName)) {", "repl": "      if (FALSE) {", "note": "test offset named in 'data' no longer resolved there", "files": ["test-data-formula.R", "test-data-testOffset.R"]}
{"id": "dmm1", "file": "R/data.R", "find": "      completeCases <- if (missing == \"error\") {\n        stats::complete.cases(x, y)\n      } else {\n        rep_len(TRUE, length(y))\n      }", "repl": "      completeCases <- stats::complete.cases(x, y)", "note": "matrix branch complete-cases filters unconditionally again", "files": ["test-data-missing-matrix.R"]}
{"id": "dmm2", "file": "R/data.R", "find": "      if (missing == \"error\") {\n        if (anyNA(y)) {\n          stop(\"response contains missing values\")\n        }", "repl": "      if (FALSE) {\n        if (anyNA(y)) {\n          stop(\"response contains missing values\")\n        }", "note": "matrix branch drops its pre-filter missing checks", "files": ["test-data-missing-matrix.R"]}
{"id": "dm1", "file": "R/dbarts.R", "find": "        trees$missing <- c(\"L\", \"R\")[trees$missing + 1L]", "repl": "        trees$missing <- c(\"R\", \"L\")[trees$missing + 1L]", "note": "getTrees decodes the NA route with the sides swapped", "files": ["test-data-missing.R"]}
{"id": "dm2", "file": "R/data.R", "find": "    if (xHasNA && any(colSums(!is.na(x)) == 0L)) {\n      stop(\"predictor columns cannot be entirely missing\")\n    }", "repl": "    if (FALSE) {\n      stop(\"predictor columns cannot be entirely missing\")\n    }", "note": "all-NA predictor column refusal dropped", "files": ["test-data-missing.R"]}
{"id": "dto1", "file": "R/data.R", "find": "      return(list(\n        offset.test = if (offsetGivenAsScalar == TRUE) offset[1] else offset,\n        testUsesRegularOffset = TRUE\n      ))", "repl": "      return(list(\n        offset.test = if (offsetGivenAsScalar == TRUE) offset[1] else offset,\n        testUsesRegularOffset = FALSE\n      ))", "note": "offset.test = offset no longer flagged as reusing the regular offset", "files": ["test-data-testOffset.R"]}
{"id": "dto2", "file": "R/data.R", "find": "    testOffset <- subTermInLanguage(\n      matchedCall$offset.test,\n      quote(offset),\n      baseOffset\n    )", "repl": "    testOffset <- matchedCall$offset.test", "note": "offset.test expressions no longer substitute the regular offset", "files": ["test-data-testOffset.R"]}
{"id": "dto3", "file": "R/data.R", "find": "      return(list(offset.test = baseOffset, testUsesRegularOffset = TRUE))", "repl": "      return(list(offset.test = baseOffset, testUsesRegularOffset = FALSE))", "note": "quoted offset.test = offset loses the regular-offset flag", "files": ["test-data-testOffset.R"]}
{"id": "dh1", "file": "src/R_interface_bartcore.cpp", "find": "void refuseMutationOnView(const bartcore::SamplerBase& sampler,\n                          const char* caller) {\n  if (!sampler.data().hasRequantizeSource())", "repl": "void refuseMutationOnView(const bartcore::SamplerBase& sampler,\n                          const char* caller) {\n  if (false)", "note": "view samplers no longer refuse predictor mutation", "files": ["test-data-handle.R"]}
{"id": "ce1", "file": "R/A_class.R", "find": "    return(\"'n.thin' must be a positive integer\")", "repl": "    return(TRUE)", "note": "n.thin positivity check no longer refuses", "files": ["test-control-errors.R", "test-control-valuesAreUsed.R"]}
{"id": "ce2", "file": "R/A_class.R", "find": "    return(\"'n.burn' must be a non-negative integer\")", "repl": "    return(TRUE)", "note": "n.burn non-negativity check no longer refuses", "files": ["test-control-errors.R"]}
{"id": "ce3", "file": "R/A_class.R", "find": "    return(\"'n.threads' must be a positive integer\")", "repl": "    return(TRUE)", "note": "n.threads positivity check no longer refuses", "files": ["test-control-errors.R", "test-multithreaded.R"]}
{"id": "cvu1", "file": "R/bart.R", "find": "  control@n.burn <- control@n.burn %/% control@n.thin\n  control@printEvery <- control@printEvery %/% control@n.thin", "repl": "  control@printEvery <- control@printEvery %/% control@n.thin", "note": "bart() no longer thins n.burn", "files": ["test-control-valuesAreUsed.R"]}
{"id": "cvu2", "file": "R/bart.R", "find": "    n.thin = keepevery,", "repl": "    n.thin = 1L,", "note": "bart() ignores keepevery", "files": ["test-control-valuesAreUsed.R"]}
{"id": "cd1", "file": "R/diagnostics.R", "find": "    dim = c(dim(pieces[[1L]])[1L], n.chains, length(varNames)),", "repl": "    dim = c(n.chains, dim(pieces[[1L]])[1L], length(varNames)),", "note": "bartDrawsArray transposes the iteration and chain margins", "files": ["test-convergence-diagnostics.R"]}
{"id": "cd2", "file": "R/diagnostics.R", "find": "  } else if (any(x$stats$rhat > 1.01, na.rm = TRUE)) {", "repl": "  } else if (FALSE) {", "note": "print.summary.bart never notes a high R-hat", "files": ["test-convergence-diagnostics.R"]}
{"id": "cd3", "file": "R/diagnostics.R", "find": "  pooled <- matrix(arr, prod(dim(arr)[1L:2L]), dim(arr)[3L])", "repl": "  pooled <- matrix(arr, prod(dim(arr)[1L:2L]), 1L)", "note": "quantileSummary pools every variable into one column", "files": ["test-convergence-diagnostics.R"]}
{"id": "fa1", "file": "R/model.R", "find": "    logistic = pi * sqrt(3.0),", "repl": "    logistic = 3.0,", "note": "logistic default node scale loses the logistic latent sd", "files": ["test-family.R"]}
{"id": "fa2", "file": "R/spec.R", "find": "    if (all(data@weights == 1)) {\n      data@weights <- NULL\n    } else {\n      stop(\n        \"probit models do not support weights; fit integer count weights \",", "repl": "    if (TRUE) {\n      data@weights <- NULL\n    } else {\n      stop(\n        \"probit models do not support weights; fit integer count weights \",", "note": "probit silently discards weights instead of refusing", "files": ["test-family.R"]}
{"id": "fo1", "file": "R/dbarts.R", "find": "      .Call(C_dbarts_bartcore_getFitsWithoutOffset, ptr)", "repl": "      .Call(C_dbarts_bartcore_getFitsWithoutOffset, ptr) + 1", "note": "getFitsWithoutOffset returns a shifted location", "files": ["test-family-offset.R", "test-embedding-recipes.R", "test-augmentation.R"]}
{"id": "dca4", "file": "R/utility.R", "find": "      columnLevels[[j]] <- list(levels(column))\n      columnNames[[j]] <- name", "repl": "      columnLevels[[j]] <- list(rev(levels(column)))\n      columnNames[[j]] <- name", "note": "factor level table stored reversed", "files": ["test-data-categorical.R", "test-data-categorical-declared.R"]}
{"id": "dcm3", "file": "R/data.R", "find": "  weights <- as.double(weights)\n  if (length(weights) == 1L) {\n    weights <- rep_len(weights, initialNumObservations)\n  }", "repl": "  weights <- as.double(weights)\n  if (length(weights) == 1L) {\n    weights <- rep_len(weights, initialNumObservations)\n  }\n  weights <- rev(weights)", "note": "x/y weights installed in reverse row order", "files": ["test-data-compatibility.R", "test-data-formula.R"]}
{"id": "dfm3", "file": "R/data.R", "find": "      modelFrameCall$formula <- testFormula\n      modelFrameCall$data <- test", "repl": "      modelFrameCall$formula <- testFormula\n      modelFrameCall$data <- test[rev(seq_len(nrow(test))), , drop = FALSE]", "note": "formula test weights read from reversed test rows", "files": ["test-data-formula.R"]}
{"id": "mo1", "file": "R/model.R", "find": "  categorical <- which(result != 0L & data@varTypes == CATEGORICAL_VARIABLE)\n  if (length(categorical) > 0L) {", "repl": "  categorical <- which(result != 0L & data@varTypes == CATEGORICAL_VARIABLE)\n  if (FALSE) {", "note": "monotone accepts a categorical predictor", "files": ["test-monotone.R"]}
{"id": "mo2", "file": "R/model.R", "find": "    if (length(monotone) != numColumns) {", "repl": "    if (FALSE) {", "note": "unnamed monotone length check dropped", "files": ["test-monotone.R"]}
{"id": "mo3", "file": "R/model.R", "find": "      result[columns] <- direction", "repl": "      result[columns] <- -direction", "note": "named monotone directions resolve with the sign flipped", "files": ["test-monotone.R", "test-calibration-prior-draws.R"]}
{"id": "hu1", "file": "R/bart.R", "find": "  if (all(positive)) {\n    stop(", "repl": "  if (FALSE) {\n    stop(", "note": "hurdle no longer requires an exact zero", "files": ["test-hurdle-surface.R"]}
{"id": "hu2", "file": "R/bart.R", "find": "  if (anyNA(y) || any(!is.finite(y)) || any(y < 0)) {", "repl": "  if (FALSE) {", "note": "hurdle response non-negativity check dropped", "files": ["test-hurdle-surface.R"]}
{"id": "hz1", "file": "R/bart.R", "find": "  result$periods <- attr(fit$control, \"bartcore.hazard.periods\")", "repl": "  result$periods <- NULL", "note": "hazard fits lose the periods marker", "files": ["test-hazard.R"]}
{"id": "hz2", "file": "R/bart.R", "find": "    m <- sum(periods <= times[j])", "repl": "    m <- sum(periods < times[j])", "note": "hazard survival cumulates a period short", "files": ["test-hazard.R"]}
{"id": "it1", "file": "R/model.R", "find": "        stop(\"each interactions 'forbid' entry must name two or more columns\")", "repl": "        invisible(NULL)", "note": "interactions forbid arity refusal dropped", "files": ["test-interactions.R"]}
{"id": "it2", "file": "R/model.R", "find": "      stop(\"interactions 'max.order' must be a single integer >= 1\")", "repl": "      order <- 1L", "note": "interactions max.order validation replaced by a silent default", "files": ["test-interactions.R"]}
{"id": "it3", "file": "R/spec.R", "find": "    attr(model, \"interaction.max.order\") <- interactionSpec$max.order", "repl": "    attr(model, \"interaction.max.order\") <- 0L", "note": "the resolved max.order cap never reaches the engine", "files": ["test-interactions.R"]}
{"id": "pp1", "file": "R/generics.R", "find": "        ))\n      }\n      result <- ev + noise\n    }\n  } else {\n    if (responseIsBinary) {", "repl": "        ))\n      }\n      result <- ev\n    }\n  } else {\n    if (responseIsBinary) {", "note": "gaussian ppd adds no residual noise", "files": ["test-ppd-sigma-pairing.R", "test-generics-posteriorPredictiveDistribution.R"]}
{"id": "pp2", "file": "R/generics.R", "find": "  if (!responseIsBinary && is.null(dim(sigma))) {\n    sigma <- uncombineChains(as.vector(sigma), n.chains)\n  }", "repl": "  if (FALSE) {\n    sigma <- uncombineChains(as.vector(sigma), n.chains)\n  }", "note": "ppd does not split a combined sigma before pairing", "files": ["test-ppd-sigma-pairing.R", "test-generics-posteriorPredictiveDistribution.R"]}
{"id": "pl1", "file": "R/generics.R", "find": "      result <- dnorm(y, as.vector(ev), sd, log = TRUE)", "repl": "      result <- dnorm(y, as.vector(ev), sd, log = FALSE)", "note": "gaussian pointwise loglik returns the density, not its log", "files": ["test-pointwise-loglik.R"]}
{"id": "pl2", "file": "R/generics.R", "find": "      result[rep(weights, each = n.draws) == 0] <- NaN", "repl": "      result[rep(weights, each = n.draws) == 0] <- -Inf", "note": "zero-weight rows report -Inf instead of NaN", "files": ["test-pointwise-loglik.R"]}
{"id": "dt1", "file": "src/R_interface_bartcore.cpp", "find": "  results.splitProbabilities = shape.usesDart ? REAL(varprobsExpr) : NULL;", "repl": "  results.splitProbabilities = NULL;", "note": "DART split probabilities never recorded", "files": ["test-dart-mixed-columns.R"]}
{"id": "mp1", "file": "R/model.R", "find": "    tree.prior@update.delay <- as.numeric(control@n.burn %/% 2L)", "repl": "    tree.prior@update.delay <- as.numeric(control@n.burn)", "note": "unset DART update.delay resolves to the full burn-in", "files": ["test-model-priors.R"]}
{"id": "mp2", "file": "R/model.R", "find": "    stop(\"'\", name, \"' must be positive\")", "repl": "    invisible(NULL)", "note": "prior-constructor positivity refusal dropped", "files": ["test-model-priors.R"]}
{"id": "me1", "file": "R/model.R", "find": "resolveMonotone <- function(monotone, data) {", "repl": "resolveMonotone <- function(monotone, data) { # nolint", "note": "no-op control mutation", "files": ["test-model-errors.R"]}
{"id": "fr1", "file": "R/data.R", "find": "      type = if (is.ordered(y)) \"ordered factor\" else \"factor\",", "repl": "      type = \"factor\",", "note": "classifyResponse never reports an ordered factor", "files": ["test-factor-response.R", "test-monotone.R"]}
{"id": "fr2", "file": "R/data.R", "find": "    as.double(as.integer(if (is.character(y)) factor(y) else y) - 1L)", "repl": "    as.double(as.integer(if (is.character(y)) factor(y) else y))", "note": "factor response coded 1-based instead of 0-based", "files": ["test-factor-response.R"]}
{"id": "fr3", "file": "R/data.R", "find": "    if (!splitMultinomialMessage) {\n      stop(", "repl": "    if (FALSE) {\n      stop(", "note": "3+-level factor refusal dropped from the single-forest entries", "files": ["test-factor-response.R"]}
{"id": "fo2", "file": "R/bartcore.R", "find": "    if (anyNA(offset)) {\n      stop(\"'offset' contains missing values\")\n    }", "repl": "    if (FALSE) {\n      stop(\"'offset' contains missing values\")\n    }", "note": "setOffset drops its NA refusal", "files": ["test-family-offset.R"]}
{"id": "fo3", "file": "R/bartcore.R", "find": "    if (identical(sampler$data@testUsesRegularOffset, TRUE)) {\n      offset.test <- NULL\n    }", "repl": "    if (FALSE) {\n      offset.test <- NULL\n    }", "note": "clearing the offset leaves a synced test offset installed", "files": ["test-family-offset.R", "test-data-testOffset.R"]}
{"id": "fo4", "file": "R/dbarts.R", "find": "      bartcoreSamplerSetOffset(.self, offset, updateScale)", "repl": "      bartcoreSamplerSetOffset(.self, offset, updateScale)\n      selfEnv <- NULL", "note": "no-op probe on the setOffset conduit", "files": ["test-family-offset.R"]}
{"id": "gl1", "file": "R/model.R", "find": "        \"gp node prior 'lengthscale' must have length 1 or match the \",", "repl": "        \"gp node prior lengths disagree \",", "note": "gp lengthscale refusal text unpinned", "files": ["test-gp-leaves.R"]}
{"id": "gl2", "file": "R/model.R", "find": "    if (length(lengthscale) == 1L) {\n      lengthscale <- rep_len(lengthscale, length(columnIndices))\n    }", "repl": "    if (FALSE) {\n      lengthscale <- rep_len(lengthscale, length(columnIndices))\n    }", "note": "a scalar gp lengthscale is no longer recycled", "files": ["test-gp-leaves.R"]}
{"id": "ml1", "file": "R/model.R", "find": "        \"node prior columns must be resolved against data; \",", "repl": "        \"node prior columns unresolved; \",", "note": "unresolved node-prior designation refusal text unpinned", "files": ["test-linear-leaves.R", "test-gp-leaves.R", "test-model-errors.R"]}
{"id": "ms1", "file": "R/dbarts.R", "find": "      checkMissingPolicy(data, sourceAnyNA(x), \"predictors\")", "repl": "      invisible(NULL)", "note": "setPredictor drops the missing = error policy check", "files": ["test-data-missing.R", "test-mutate-sparse-valued.R"]}
{"id": "cw1", "file": "R/dbarts.R", "find": "      if (!is.null(trees$missing)) {\n        trees$missing <- c(\"L\", \"R\")[trees$missing + 1L]\n      }", "repl": "      if (FALSE) {\n        trees$missing <- c(\"L\", \"R\")[trees$missing + 1L]\n      }", "note": "getTrees leaves the NA route as raw integer codes", "files": ["test-data-categorical-wide.R", "test-data-missing.R"]}
{"id": "cvu3", "file": "R/dbarts.R", "find": "  if (length(control@call) == 1L && control@call == call(\"NA\")) {\n    control@call <- matchedCall\n  }", "repl": "  if (FALSE) {\n    control@call <- matchedCall\n  }", "note": "the sampler's control call slot is not propagated", "files": ["test-control-valuesAreUsed.R"]}
{"id": "mt1", "file": "R/bart.R", "find": "  control@n.burn <- control@n.burn %/% control@n.thin\n  control@n.samples <- control@n.samples %/% control@n.thin", "repl": "  control@n.samples <- control@n.samples %/% control@n.thin", "note": "bart2 no longer thins n.burn", "files": ["test-multithreaded.R", "test-control-valuesAreUsed.R"]}
{"id":"gs1","file":"R/bartcore.R","find":"  if (isTRUE(updateScale)) {\n    refuseAmplitudeMutation(\n      sampler,\n      \"setResponse(updateScale = TRUE)\",","repl":"  if (FALSE) {\n    refuseAmplitudeMutation(\n      sampler,\n      \"setResponse(updateScale = TRUE)\",","note":"setResponse(updateScale=TRUE) no longer refused under a grouped decorator","files":["test-grouped-swap.R"]}
{"id":"gs2","file":"R/bartcore.R","find":"  y <- as.double(y)\n  if (anyNA(y)) {\n    stop(\"response contains missing values\")\n  }","repl":"  y <- rev(as.double(y))\n  if (anyNA(y)) {\n    stop(\"response contains missing values\")\n  }","note":"setResponse installs the response in reverse row order","files":["test-grouped-swap.R","test-composition-sequences.R"]}
{"id":"mc1","file":"R/bartcore.R","find":"  sampler$data@y <- as.double(rowSums(counts))","repl":"  sampler$data@y <- as.double(counts[, 1L])","note":"setCounts derives trials from the first category only","files":["test-multinomial-counts-mutation.R"]}
{"id":"mc2","file":"R/bartcore.R","find":"  if (NCOL(counts) != ncol(current)) {\n    stop(\"'counts' must have \", ncol(current), \" categories\")\n  }","repl":"  if (FALSE) {\n    stop(\"'counts' must have \", ncol(current), \" categories\")\n  }","note":"setCounts drops its category-count check","files":["test-multinomial-counts-mutation.R"]}
{"id":"dh2","file":"src/R_interface_bartcore.cpp","find":"      if (row < 1 || static_cast<size_t>(row) > parent.numObservations)\n        Rf_error(\"train row out of range\");","repl":"      if (false)\n        Rf_error(\"train row out of range\");","note":"data-handle view drops its train-row range check","files":["test-data-handle.R"]}
{"id":"dcd5","file":"src/R_interface_bartcore.cpp","find":"    std::snprintf(blockError, sizeof blockError,\n                  \"bartcore state is missing required block '%s'\", name);","repl":"    std::snprintf(blockError, sizeof blockError,\n                  \"bartcore state lacks '%s'\", name);","note":"missing-required-block refusal text unpinned","files":["test-data-categorical-declared.R","test-mutate-then-serialize.R"]}
{"id":"ll1","file":"R/dbarts.R","find":"          names(trees)[slopeColumns] <- paste0(\"beta.\", covariateNames)","repl":"          invisible(NULL)","note":"linear-leaf slope columns keep their generic names","files":["test-linear-leaves.R"]}
{"id":"dto4","file":"R/data.R","find":"            \"vectored 'offset' cannot be directly applied to test data of unequal length\"","repl":"            \"offset length mismatch\"","note":"vectored-offset refusal text unpinned","files":["test-data-testOffset.R"]}
{"id":"hz3","file":"R/generics.R","find":"  if (identical(object[[\"family\"]], \"logistic\")) {\n    plogis(latents)\n  } else {\n    pnorm(latents)\n  }","repl":"  pnorm(latents)","note":"probabilityFromLatents always uses the probit link","files":["test-hazard.R","test-family.R"]}
{"id":"er2","file":"R/dbarts.R","find":"    setCalibration = function(\n      prior.scale,\n      prior.sd,\n      prior.mean,\n      forest = 1L,\n      updateState = NA\n    ) {","repl":"    setCalibration = function(\n      prior.scale,\n      prior.sd,\n      prior.mean,\n      forest = 1L,\n      updateState = NA\n    ) {\n      if (!missing(prior.scale)) prior.scale <- prior.scale * 2","note":"setCalibration doubles the requested prior scale","files":["test-embedding-recipes.R","test-augmentation.R"]}
{"id":"mts1","file":"src/R_interface_bartcore.cpp","find":"static const int stateFormatVersion = 3;","repl":"static const int stateFormatVersion = 4;","note":"state format version bumped without a reader change","files":["test-mutate-then-serialize.R","test-data-categorical-wide.R"]}
{"id":"mth1","file":"R/bart.R","find":"  control@n.samples <- control@n.samples %/% control@n.thin","repl":"  control@n.samples <- control@n.samples %/% control@n.thin\n  control@n.chains <- 1L","note":"bart2 collapses every request to a single chain","files":["test-multithreaded.R","test-generics-multithreaded.R"]}
{"id":"gfc1","file":"R/dbarts.R","find":"      if (!is.null(trees$missing)) {","repl":"      if (is.null(trees$missing)) {","note":"getTrees decodes the missing route only when it is absent","files":["test-grow-from-root-categorical.R","test-data-missing.R"]}
{"id":"me2","file":"R/model.R","find":"    stop(\"gp node prior 'lengthscale' must be positive\")","repl":"    invisible(NULL)","note":"gp lengthscale positivity refusal dropped","files":["test-model-errors.R","test-gp-leaves.R"]}
{"id":"mth2","file":"R/bart.R","find":"  yhat.test <- NULL\n  yhat.test.mean <- NULL\n  if (NROW(fit$data@x.test) > 0) {","repl":"  yhat.test <- NULL\n  yhat.test.mean <- NULL\n  if (TRUE) {","note":"bart() packages a test channel even with no test rows","files":["test-multithreaded.R","test-generics-multithreaded.R","test-control-valuesAreUsed.R"]}
{"id":"mth3","file":"R/bart.R","find":"      yhat.test.mean <- apply(yhat.test, length(dim(yhat.test)), mean)","repl":"      yhat.test.mean <- apply(yhat.test, 1L, mean)","note":"bart() averages the test channel over the wrong margin","files":["test-multithreaded.R","test-generics-multithreaded.R"]}
{"id":"fo5","file":"R/bartcore.R","find":"  sampler$data@offset <- offset","repl":"  sampler$data@offset <- sampler$data@offset","note":"setOffset does not mirror the new offset into data@offset","files":["test-family-offset.R"]}
{"id":"mp3","file":"R/model.R","find":"  prior@splitProbabilities <- split.probs\n  prior@splitProbabilitiesSpec <- NULL","repl":"  prior@splitProbabilities <- numeric()\n  prior@splitProbabilitiesSpec <- NULL","note":"named split probabilities resolve to nothing","files":["test-model-priors.R","test-dart-mixed-columns.R"]}
{"id":"me3","file":"R/model.R","find":"    stop(\"'\", name, \"' must be positive\")","repl":"    stop(\"'\", name, \"' is out of bounds\")","note":"prior positivity refusal text unpinned","files":["test-model-errors.R","test-model-priors.R"]}
{"id":"mc3","file":"R/bartcore.R","find":"  ptr <- sampler$getPointer()\n  .Call(C_dbarts_bartcore_setCounts, ptr, counts)\n  sampler$data@counts <- counts","repl":"  ptr <- sampler$getPointer()\n  sampler$data@counts <- counts","note":"setCounts never reaches the engine","files":["test-multinomial-counts-mutation.R"]}
{"id":"mts2","file":"src/R_interface_bartcore.cpp","find":"static const int stateFormatVersion = 3;","repl":"static const int stateFormatVersion = 3; // NOLINT","note":"no-op control on the state format","files":["test-mutate-then-serialize.R"]}
