# Provenance for the files in this directory.
#
# role:
#   current             - the baseline the local and CI equivalence/speed
#                          gates compare against.
#   historical          - bartcore-native, superseded by a later recording;
#                          the engine could still reproduce these.
#   historical-classic  - pre-cutover; the classic engine that produced
#                          these is deleted, so they can never be
#                          re-recorded. Permanent evidence of the
#                          classic-to-bartcore cutover (see
#                          docs/design/public-surface.md), not a gate.
#
# rule (P17, going forward): a re-record that changes any recorded draw (a
#   non-bitwise link) must name, in its MANIFEST row or the commit body, the
#   ORACLE establishing the new values are right - an exact-gate re-run with
#   its gap, an SBC arm with its result, an analytic identity, or a
#   cross-implementation check. "The deviation looks like the expected
#   class" is an adjudication, not an oracle; a row relying on it must say
#   so explicitly.
#
# build mode (docs/plans/engine-performance.md S1): these equivalence
#   baselines and the RNG-locked snapshot tinytests are reference-build
#   values; the shipped build reproduces them bitwise today only because no
#   vectorized draw-path kernel has landed (engine-performance S2 onward),
#   after which the shipped build owes them the statistical, not bitwise,
#   match.
#
# file                                role                 commit   machine/arch    scenarios
equivalence-abf88654.rds              current              abf88654 arm64 (macOS)   50 scenarios, same set as predecessor. DRAW-CHANGING re-record, and only on the two scenarios that call xbart (xbart, xbartmixed): xbart now distributes work over (replication, fold) UNITS instead of replication ranges, each replication drawing its data split from its own seed and each unit its fits from its own, both derived from the call seed, so every xbart stream moves. The other 48 scenarios call no xbart and are bitwise unchanged; this slice touches no src/ file. (The list-valued k grid and the binary default cell moving from fixed 2 to chi(1.5, 2) land in the same slice but move nothing recorded here: both xbart scenarios name k = c(1, 3).) ORACLE (P17): inst/tinytest/test-xbart-fold-oracle.R, which asserts, from outside the function, (a) that the fold partition at a fixed seed is EXACTLY the one reconstructed outside xbart from (seed, n, n.reps, fold count) through the documented seed derivation, and that the folds are pairwise disjoint and cover 1:n; (b) that each fold posterior mean on its held-out rows tracks the y it was scored against, above a permutation null recomputed in the file (a misordered gather of the test channel would decorrelate the two while leaving every shape intact); (c) that on a y independent of x the held-out rmse sits at or above sd(y) and at or above 1.75x the in-sample rmse at the same hyperparameters, so no fold trained on the rows it was scored on; and (d) that a labelled (n.trees, k) cell of the result array is the cell actually fit. It PASSES 13/13 at this tip on both the reference and the shipped build. ORACLE POISON, three mutations at this tip, each reverted and the file touched: adding 1 to a replication split seed fails arm (a) (1/13); training every fold on all rows fails arm (c) (2/13); and seeding once per chunk instead of per unit - the predecessor scheme this re-record replaces - fails inst/tinytest/test-xbart-reproducibility.R 6 of 24, and 2 of 14 under _R_CHECK_LIMIT_CORES_=TRUE, where that file holds its thread counts to the two workers a check run allows; that file also fails 3 of 24 when the dispatch is not run under the caller-stream restore. PARTITION vs fbff1989, from this tip and identically from both builds: 48 of 50 scenarios reproduce BITWISE ("identical draws (same RNG stream)"); the movers are EXACTLY xbart (8 summaries, max |z| = 2.31) and xbartmixed (8 summaries, max |z| = 1.86), neither near the |z| > 4 threshold, so the loss surfaces are statistically unmoved; 50 compared / 0 skipped. NEUTRALITY: at the same tip bcf-equivalence-fbff1989 is 12/12 with all 7 channels bitwise in every scenario and multinomial-equivalence-fbff1989 is 11/11 bitwise - neither harness carries an xbart scenario, and both keep their fbff1989 recordings. BUILD MODE: recorded from the REFERENCE build, which reproduces it 50/50 under --strict-coverage; the SHIPPED build reproduces it 50/50 bitwise as well, no vectorized draw-path kernel having landed. fbff1989 demoted to historical.
equivalence-fbff1989.rds              historical           fbff1989 arm64 (macOS)   50 scenarios, same set as predecessor. DRAW-CHANGING re-record: the swap tree proposal is DELETED from the kernel and the default mixture moves from birth_death 0.5 / swap 0.1 / change 0.4 to birth_death 0.6 / change 0.4, so a tree whose move-type uniform lands in [0.5, 0.6) takes birth/death where it took swap and the stream desynchronizes inside the first sweep at any usual tree count. ORACLE (P17), a BITWISE IDENTITY rather than an adjudication: with swapProbability = 0.0 the old dispatch's middle test `u < bd + 0.0` is `u < bd` exactly in IEEE and is already known false, so control reaches changeMove at the same stream position and swapMove draws nothing - the OLD kernel at the NEW mixture is bitwise the NEW kernel. A throwaway private library was built at the parent commit 39692087 with ONLY the mixture defaults moved (the five default R vectors, the seven C++ initializers and the two-forest refusal's literal to birth_death 0.6 / swap 0.0 / change 0.4; swapMove, its dispatch and the monotone birth/death-only rewrite left standing), all three equivalence baselines were recorded from it, and compare from this tip reports "identical draws (same RNG stream)" on EVERY scenario of all three: gaussian 50 compared / 0 skipped with zero "max |z|" lines, BCF 12 compared / 0 skipped every channel bitwise, multinomial 11 compared / 0 skipped every channel bitwise. The deletion therefore moved no draw the mixture change did not. ORACLE POISON: the same compare at the same tip against the predecessor baselines, recorded at swap 0.1, falls back to the statistical mode on every scenario of all three with zero identical streams - so the oracle tests the MIXTURE, not merely that two runs of one build agree. PARTITION vs 1e5f80b2: all 50 scenarios move, as a mixture change must, and every one falls to the statistical comparison - max |z| = 3.73 over 3687 summaries, 17 with |z| > 3, NONE at or above the |z| > 4 threshold, so the posteriors are statistically unmoved. This baseline reproduces itself 50/50 under --strict-coverage from a second --preclean install. 1e5f80b2 demoted to historical. Superseded by abf88654 (xbart distributed over (replication, fold) units, with a seed per unit), a DRAW-CHANGING re-record on its two xbart scenarios alone: the other 48 reproduce this file bitwise, and xbart and xbartmixed move at max |z| = 2.31 and 1.86 over 8 summaries each, both under 4.
equivalence-1e5f80b2.rds              historical           1e5f80b2 arm64 (macOS)   50 scenarios: the 49 survivors of d4bca4ce (its 51 less grouped and grouped_aft, deleted with the whole grouped random-intercept path) plus aft, a new standalone AFT survival scenario - bart2(family = "aft") on a (time, status) response, recording the test linear predictor, varcount, sigma and the survivalProbabilities surface at three horizons - which restores the equivalence coverage grouped_aft was carrying. ORACLE (P17), NEUTRAL: this is a SUBTRACTION plus an ADDITION, not a draw change - all 49 survivors reproduce equivalence-d4bca4ce BITWISE inside a non-strict compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 49 compared / 1 skipped (not in baseline): aft, with grouped and grouped_aft reported skipped (not produced this run)), so the deletion moved no recorded draw and no new-value oracle is owed; benchmarks/R/aft-exact.R is the exact oracle standing behind the added scenario and PASSES in quick mode at this tip. NEUTRALITY: at the same tip bcf-equivalence-3c81d6df is 12/12 bitwise on every gated channel and multinomial-equivalence-4d9a3337 is 11/11 - neither harness carries a grouped scenario. This baseline reproduces itself 50/50 under --strict-coverage. d4bca4ce demoted to historical. Superseded by fbff1989 (the swap removal), a DRAW-CHANGING re-record whose oracle is a bitwise identity: at the new mixture the swap-carrying kernel reproduces the new kernel exactly, and this file's own scenarios all move under the statistical comparison at max |z| = 3.73, none at |z| > 4.
equivalence-d4bca4ce.rds              historical           d4bca4ce arm64 (macOS)   51 scenarios: the 46 of 02d41365 plus testswap (a sampler's whole test container replaced mid-chain through both setTestPredictor and setTestPredictorAndOffset, on a design carrying a dense factor, an NA-bearing factor and a CSC-backed factor beside two numeric columns, each container recorded as its own leg), leaffactor and leaffactormixed (an ordered factor designated as a linear-leaf covariate, on an all-dense and on a mixed container), factorpartial (the per-observation update session over a dense factor column of a mixed store, with the install mask recorded) and xbartmixed (xbart's fold views over a mixed parent carrying both factor kinds). ORACLE (P17), NEUTRAL: this is an ADDITION, not a draw change - all 46 predecessor scenarios reproduce equivalence-02d41365 BITWISE inside a non-strict compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 46 compared / 5 skipped (not in baseline): testswap, leaffactor, leaffactormixed, factorpartial, xbartmixed), so the additions changed no existing draws and no new-value oracle is owed. NEUTRALITY: at the same tip bcf-equivalence-00cfa108 is 12/12 bitwise on every channel and multinomial-equivalence-4d9a3337 is 11/11 bitwise on every channel - neither harness gains a scenario. This baseline reproduces itself 51/51 under --strict-coverage. Superseded by 1e5f80b2 (the grouped-random-intercept retirement), whose 49 shared scenarios reproduce it bitwise.
equivalence-02d41365.rds              historical           02d41365 arm64 (macOS)   46 scenarios, same set as predecessor. DRAW-CHANGING re-record: an ordered-factor predictor's cut grid is now the K - 1 midpoints between consecutive DECLARED level codes instead of n.cuts uniform cuts over the observed code range, so its codes, its candidate count and its split prior all move; the cut cap is raised to K - 1 rather than the grid thinned, so n.cuts no longer applies to such a column. ORACLE (P17): benchmarks/R/categorical-exact.R's new ordered-factor arm, an exact brute-force posterior over the binary partitions of a 4-level ordered factor's level interval (15 trees, 1-D leaf quadrature) - the sampler matches it to max gap 0.0010 in FULL mode against a 0.005 tolerance, at a cut cap BOTH above (n.cuts = 100) and below (n.cuts = 2) the level count, the two arms agreeing to every reported digit, which is the grid-follows-the-level-table property the change asserts. The predecessor tip FAILS the same arm in the same FULL mode at BOTH caps - 0.0071 at n.cuts = 100 (the prior-multiplicity tilt, and a hard failure rather than a sub-tolerance observation) and 0.1454 at n.cuts = 2 (levels 0 and 1 merged to one code) - so the oracle both establishes the new values and fires on the old ones. (All four figures are FULL mode, 3 seeds x 50000 draws, tolerance 0.005; the quick arm's tolerance is 0.01 and its numbers are not comparable to these.) The categorical arm of the same script is unmoved (gap 0.0008). PARTITION vs ee5ffe74 inside a non-strict compare against it: 45 of 46 scenarios reproduce BITWISE ("identical draws (same RNG stream)"); the mover is EXACTLY ordfactor - 30 summaries, max |z| = 3.38, 3 with |z| > 3, all under the |z| > 4 threshold - and it is the only scenario in the corpus carrying an ordered-factor predictor. NEUTRALITY: at the same tip bcf-equivalence-00cfa108 is 12/12 bitwise on every channel and multinomial-equivalence-4d9a3337 is 11/11 bitwise on every channel - neither harness has an ordered-factor predictor. This baseline reproduces itself 46/46 under --strict-coverage. Superseded by d4bca4ce (the S4b gate-corpus extension), whose 46 shared scenarios reproduce it bitwise.
equivalence-ee5ffe74.rds              historical           ee5ffe74 arm64 (macOS)   46 scenarios: the 43 of 736bfb05 plus ordfactor (an ordered-factor predictor at K = 150 levels against the default n.cuts = 100 - the regime where adjacent levels share a code - with three interior levels absent from training and present in the test frame), nafactor (two NA-bearing factor columns, so MIA on a CATEGORICAL column has an anchor; the pre-existing missing scenario injects NAs into a numeric matrix only) and sparsefactor (a CSC-backed sparseFactor column, whose category count comes from the container's declared level table rather than a sweep of the values). The three shapes were absent from the corpus entirely, so the ordered-factor cut grid, a factor column's missing route and a CSC-backed factor's declared count moved no recorded draw. ORACLE (P17), NEUTRAL: this is an ADDITION, not a draw change - all 43 predecessors reproduce 736bfb05 BITWISE inside a non-strict compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 43 compared / 3 skipped (not in baseline): ordfactor, nafactor, sparsefactor), so the additions changed no existing draws and no new-value oracle is owed. NEUTRALITY: at the same tip bcf-equivalence-00cfa108 is 12/12 bitwise and multinomial-equivalence-4d9a3337 is 11/11 bitwise - neither harness gains a scenario. This baseline reproduces itself 46/46 under --strict-coverage. Superseded by 02d41365 (the ordered-factor midpoint grid), whose 45 non-ordfactor scenarios reproduce it bitwise.
equivalence-736bfb05.rds              historical           736bfb05 arm64 (macOS)   43 scenarios, same set as predecessor. Re-recorded when xbart()'s k-grid sort-before-sweep was restored, which moves the RNG stream for scenarios whose k grid was not already sorted. ORACLE (P17), adjudication: PARTITION vs predecessor under --strict-coverage - 42/43 scenarios bitwise, xbart alone moves (max |z| = 1.94 over 8 summaries, well under the |z| > 4 threshold); test-xbart-method.R's order-invariance pin confirms only the warm-start trajectory moved, not the posterior. Reproduces itself 43/43 under --strict-coverage.
equivalence-31e52644.rds              historical           31e52644 arm64 (macOS)   37 scenarios, the same set as its predecessor; re-recorded at the prior-initializer fix landing: Chain::sampleTreesFromPrior draws each tree by whole-tree rejection until Tree::bottomNodesHaveWeight accepts - the CGM prior CONDITIONED on the no-empty-leaf set, the law the moves price via logLikelihoodForBranch's veto (docs/design/empty-leaf-veto.md) - where it previously PROJECTED an unrestricted draw through collapseEmptyNodes, a different law that also admitted leaves holding only zero-weight rows. The initial forest's RNG stream moves in every fit that initializes trees from the prior; converged posteriors are unaffected (Chain::run never calls the initializer). ORACLE (P17): benchmarks/R/geweke-mc.R FULL exits 0 at this tip - the marginal-conditional oracle whose FIRING adjudicated the defect (pre-fix initializer P(single-leaf tree) 0.04993 vs theory 0.0500 where the sweep realizes 0.05492 vs its theory 0.0552, z = 17.61) - plus backfit-exact PASS and bcf-exact quick OK at gaps <= 0.0009. PARTITION vs its predecessor inside a compare against it (non-strict): 33 of 37 scenarios reproduce BITWISE ("identical draws (same RNG stream)"); the movers are EXACTLY the four prior-initializing fits - grouped max |z| 2.26, grouped_aft 1.24, hazard 2.30, hurdle 3.01, all under 4, posteriors statistically unmoved. NEUTRALITY: at the same tip bcf-equivalence-6e3b9fb8 is bitwise on every channel of all 12 scenarios and multinomial-equivalence-1027be5 (recording not retained) is 10/10 bitwise - no scenario in either harness initializes trees from the prior (bartcoreRun-driven, bare-root start), so neither re-records. This baseline reproduces itself 37/37 under --strict-coverage. Superseded by f009eff8 (the P16 xbart harness extension), whose 37 shared scenarios - the whole set - reproduce it bitwise
equivalence-f494156.rds               historical           f494156  arm64 (macOS)   26: the 25 of 1d9388d plus hazard (discrete-time survival, family = "hazard"; the survival discrete-time-hazard arc - per-draw survival probabilities on held-out subjects, varcount over the person-period design, and the posterior-mean survival surface). The hazard family is person-period ingestion sugar over the probit path - it adds NO engine code and remaps to probit before the sampler is built - so the engine binary is IDENTICAL to 1d9388d (this landing is R-only; state so plainly). NEUTRALITY: all 25 pre-existing scenarios reproduce 1d9388d bitwise inside a compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 25 compared / 1 skipped (not in baseline): hazard - the addition changed no existing draws), and this baseline reproduces itself 26/26 under --strict-coverage - superseded by 7903855 (recording not retained)
equivalence-1d9388d.rds               historical           1d9388d  arm64 (macOS)   25: the 24 of 227f46a plus nbinom (overdispersed counts from a nonlinear log-mean, family = "nbinom", estimated dispersion; the negative-binomial arc - latent test fits, varcount, per-draw dispersion r, and test mean counts) - superseded by f494156. Recorded at the C2 engine HEAD 1d9388d (C3 is R-only, so the engine binary is identical - the ordinal precedent). NEUTRALITY: all 24 pre-existing scenarios reproduce 227f46a bitwise inside a compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 24 compared / 1 skipped (not in baseline): nbinom - the addition changed no existing draws)
equivalence-227f46a.rds               historical           227f46a  arm64 (macOS)   24: the 23 of 31dc05a plus ordinal (ordered 4-level outcome cut from a latent continuous truth, family = "ordinal"; the ordinal-outcomes arc - latent test fits, varcount, cutpoint draws, and test category probabilities) - superseded by 1d9388d. Recorded at the C2 engine HEAD 227f46a (the C3 landing hash was unknowable at recording; C3 is R-only, so the engine binary is identical). NEUTRALITY: all 23 pre-existing scenarios reproduce 31dc05a bitwise inside a compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 23 compared / 1 skipped (not in baseline): ordinal - the addition changed no existing draws)
equivalence-31dc05a.rds               historical           31dc05a  arm64 (macOS)   23: the 22 of its predecessor plus student (contaminated normal, resid.dist = student() estimated-nu; the robust-errors arc) - superseded by 227f46a. NEUTRALITY: all 22 pre-existing scenarios reproduce its predecessor bitwise inside a compare against it (the student addition changed no existing draws)
equivalence-de67cbb.rds               historical           de67cbb  arm64 (macOS)   21: same as current minus grouped_aft; every shared scenario identical (the addition changed no existing draws)
equivalence-5430fdb.rds               historical-classic   5430fdb  arm64 (macOS)   9: same as b354f3a (recording not retained). Recorded 168665a8 (07-02) under the classic engine, which is deleted (b354f3ab, 07-03); the flip to bartcore is b33d40b3 (07-03). CORRECTED 2026-08-17: this is now the ONLY historical-classic file - equivalence-19c499a.rds and equivalence-fbd2168.rds (both recording not retained) were relabeled to historical, both having been recorded 07-05, after the deletion - so the permanent cross-engine cutover evidence is 9 scenarios, not 18. QUANTIFIED CROSS-ENGINE EVIDENCE (link 1 vs equivalence-b354f3a.rds (recording not retained), recomputed 2026-08-17): 9 scenarios, max abs z 3.83, over 329 summaries, no disjoint ranges. See the 2026-08-17 provenance audit.
bcf-equivalence-fbff1989.rds          current              fbff1989 arm64 (macOS)   12 scenarios, same set as predecessor. DRAW-CHANGING re-record, the swap removal's BCF arm. The two-forest path never reads a proposal probability off the model - ForestStructureSpec's own defaults supply every BCF fit's mixture - so those defaults moving from 0.5 / 0.1 / 0.4 to 0.6 / 0.4 is what moves every BCF draw. ORACLE (P17), a BITWISE IDENTITY rather than an adjudication: with swapProbability = 0.0 the old dispatch's middle test `u < bd + 0.0` is `u < bd` exactly in IEEE and is already known false, so control reaches changeMove at the same stream position and swapMove draws nothing - the OLD kernel at the NEW mixture is bitwise the NEW kernel. A throwaway private library was built at the parent commit 39692087 with ONLY the mixture defaults moved (the five default R vectors, the seven C++ initializers and the two-forest refusal's literal to birth_death 0.6 / swap 0.0 / change 0.4; swapMove, its dispatch and the monotone birth/death-only rewrite left standing), all three equivalence baselines were recorded from it, and compare from this tip reports "identical draws (same RNG stream)" on EVERY scenario of all three: gaussian 50 compared / 0 skipped with zero "max |z|" lines, BCF 12 compared / 0 skipped every channel bitwise, multinomial 11 compared / 0 skipped every channel bitwise. The deletion therefore moved no draw the mixture change did not. ORACLE POISON: the same compare at the same tip against the predecessor baselines, recorded at swap 0.1, falls back to the statistical mode on every scenario of all three with zero identical streams - so the oracle tests the MIXTURE, not merely that two runs of one build agree. PARTITION vs 00cfa108's successor 3c81d6df: all 12 scenarios move and every gated channel mismatches, as a kernel change must. The draws-axis fallback reports max |z| up to 20.82 over 209 summaries per scenario, which is NOT a calibrated statement about the posterior: this harness runs one chain at one fixed seed per scenario and its Welch z treats autocorrelated draws as independent, so it has no sampling distribution to read a shift against. The oracle for the new values is the bitwise identity above, not that fallback; the three BCF exact-posterior gates all PASS at this tip in quick mode - bcf-exact E[mu] gap 0.0005 and E[(b1-b0)tau] 0.0002, bcf-exact-weak E[tau] 0.0012 and P(|a| <= 0.25, 0.5, 1) 0.0185, bcf-exact-restricted E[mu] 0.0007 and E[tau] 0.0014. 3c81d6df demoted to historical.
bcf-equivalence-3c81d6df.rds          historical           3c81d6df arm64 (macOS)   12 scenarios, same set as predecessor. REFRESH re-record at the release-candidate engine tip: 43 src/ commits landed since 00cfa108 was recorded, and NEUTRALITY was verified before recording - `compare` against bcf-equivalence-00cfa108.rds reports "identical draws (same RNG stream)" for all 12 scenarios, every channel bitwise, so none of them moved a BCF draw. No P17 oracle owed (MANIFEST header rule: only a re-record that changes a recorded draw owes one). SELF-REPRODUCTION: a fresh --preclean install of the same tree reproduces this file 12/12 bitwise on every channel. CROSS-HOST CONFIRM (docs/plans/bcf-cross-host.md sec 7 step 5) not run this pass - the x86 validation box retired; the standing cross-host gate is exact-gates.yaml's --cross-host compare on every push, re-pinned to this file below. 00cfa108 demoted to historical. Superseded by fbff1989 (the swap removal), whose oracle is the same bitwise identity.
bcf-equivalence-00cfa108.rds          historical           00cfa108 arm64 (macOS)   12 scenarios, same set as predecessor. DRAW-SHIFTING re-record: drawShippedGlue - the specialized two-scalar amplitude path bcf's K = 2 shape shipped with, plus its shape predicate and its AmplitudeSpec flag - is DELETED, and the general q-variate conditional now draws every shape. The two made the same rng calls in the same order, so only floating-point accumulation order moves. ORACLE (P17): the three BCF exact-posterior scripts PASS at this tip in FULL mode - bcf-exact (E[mu]/E[tau] gaps 0.0001 in modes 1 and 2a, mode 2b E[mu] gap 0.0172 inside its 0.035 tolerance), bcf-exact-weak (E[a mu] gap 0.0070, E[tau] 0.0007, P(|a| <= 0.25, 0.5, 1) gap 0.0041), bcf-exact-restricted (gaps 0.0002) - so the recorded draws are the right posterior, not merely a different rounding. PARTITION vs 6e3b9fb8: ALL 12 scenarios move, as a BCF-only accumulation change must; the point-in-time snapshot channels (mu, tau, glue) mismatch with no statistical fallback, and the draws-axis fallback reports max |z| = 0.00 over 209 summaries in every scenario - posteriors statistically unmoved. 6e3b9fb8 is deleted rather than kept as historical: the engine that produced it no longer exists. This baseline reproduces itself 12/12 bitwise on every channel. NEUTRALITY: at the same tip equivalence-736bfb05 is 43/43 identical under --strict-coverage and multinomial-equivalence-4d9a3337 is 11/11 bitwise on every channel - neither harness builds a multi-forest amplitude model. Superseded by 3c81d6df (RC-tip refresh re-record; NEUTRAL, no draws moved, verified bitwise before recording).
multinomial-equivalence-fbff1989.rds  current              fbff1989 arm64 (macOS)   11 scenarios, same set as predecessor. DRAW-CHANGING re-record, the swap removal's multinomial arm: MultinomialForestSpec's own mixture defaults move with the rest, so every category forest's move-type stream shifts. ORACLE (P17), a BITWISE IDENTITY rather than an adjudication: with swapProbability = 0.0 the old dispatch's middle test `u < bd + 0.0` is `u < bd` exactly in IEEE and is already known false, so control reaches changeMove at the same stream position and swapMove draws nothing - the OLD kernel at the NEW mixture is bitwise the NEW kernel. A throwaway private library was built at the parent commit 39692087 with ONLY the mixture defaults moved (the five default R vectors, the seven C++ initializers and the two-forest refusal's literal to birth_death 0.6 / swap 0.0 / change 0.4; swapMove, its dispatch and the monotone birth/death-only rewrite left standing), all three equivalence baselines were recorded from it, and compare from this tip reports "identical draws (same RNG stream)" on EVERY scenario of all three: gaussian 50 compared / 0 skipped with zero "max |z|" lines, BCF 12 compared / 0 skipped every channel bitwise, multinomial 11 compared / 0 skipped every channel bitwise. The deletion therefore moved no draw the mixture change did not. ORACLE POISON: the same compare at the same tip against the predecessor baselines, recorded at swap 0.1, falls back to the statistical mode on every scenario of all three with zero identical streams - so the oracle tests the MIXTURE, not merely that two runs of one build agree. PARTITION vs 4d9a3337: all 11 scenarios move; the draws-axis fallback reports max |z| 4.69 to 9.31 over 458 to 687 summaries per scenario, which - one chain, one seed, autocorrelated draws - is not a calibrated posterior comparison for the same reason the BCF row gives. The oracle is the bitwise identity above; multinomial-exact.R PASSES at this tip in quick mode on all arms, its identified-probability arms and the arm-7 analytic level identity included (worst reported gap 0.0757 against its 0.095 tolerance). 4d9a3337 demoted to historical.
multinomial-equivalence-4d9a3337.rds  historical           4d9a3337 arm64 (macOS)   11 scenarios (predecessor's 10 plus k3countsswap: a mid-chain response swap through bartcoreSetCounts on an n x K grouped-count matrix). DRAW-SHIFTING re-record: MultinomialForestCombiner::afterCombine divided leaf.scale by sqrt(numTrees) a second time, so the level's sd was sqrt(m) too small (8.66x at n.trees = 75); every multinomial draw moves. ORACLE (P17), two: (a) benchmarks/R/multinomial-exact.R's six identified-probability arms, unmoved post-fix (the level cannot move an identified probability); (b) that script's new arm 7, an analytic identity - the grand level's sample sd now matches tau/sqrt(K) (gap 0.0018 against a 0.043 band), vs 8.66x too small pre-fix. tests/cpp's testMultinomialCombinerSeam pins the same closed form at a hand-built fixture. k3countsswap's non-vacuity (moves train and forestFits, shifts tree structure vs. the swap omitted) and the neutrality of the gaussian and bcf equivalence baselines at the same tip were also checked; reproduces itself, all 11 scenarios x their channels. Superseded by fbff1989 (the swap removal), whose oracle is the same bitwise identity.
bench-sampler-127f04ee.csv           current              127f04ee arm64 (macOS)   speed baseline, recorded 2026-09-08 on a quiet-machine grant (1-min loadavg 1.1-2.2) at the level-auto engine tip; harness rev 090a0a6c. Adjudicated first by a same-machine A/B against a rebuild of ab1dc52 (6 alternating rounds, per-round minima): no arm past 1.05, five of seven faster (run-n10000-p10-t75 0.985, setPredictor accept 0.971), setPredictor reject 1.007 (the recorded 0.136 had drifted: the rebuilt predecessor reads 0.139 on this host), embedded-offset gibbs step 1.032 - a real, deterministic 3.2 percent cost on the offset-mutate-then-draw-one loop, under the gate, from somewhere in the 126 engine commits since the predecessor's current-history anchor 95a0b83f (ab1dc52 predates the branch rewrite and is not an ancestor). Recorded as the median of three same-window record runs (setPredictor reject spans 0.136-0.144 ms across single runs on this host, a 3 percent grain on a 0.14 ms metric, so a compare that flags only that arm at 1.05 is re-run before it counts); two same-window compares reproduced every arm within 2 percent. Predecessor bench-sampler-ab1dc52.csv pruned
