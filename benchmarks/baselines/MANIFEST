# Provenance for the files in this directory.
#
# role:
#   current             - the baseline the local and CI equivalence/speed
#                          gates compare against.
#   historical          - bartcore-native, superseded by a later recording;
#                          the engine could still reproduce these.
#   historical-classic  - pre-cutover; the classic engine that produced
#                          these is deleted, so they can never be
#                          re-recorded. Permanent evidence of the
#                          classic-to-bartcore cutover (see
#                          docs/design/public-surface.md), not a gate.
#
# rule (P17, going forward): a re-record that changes any recorded draw (a
#   non-bitwise link) must name, in its MANIFEST row or the commit body, the
#   ORACLE establishing the new values are right - an exact-gate re-run with
#   its gap, an SBC arm with its result, an analytic identity, or a
#   cross-implementation check. "The deviation looks like the expected
#   class" is an adjudication, not an oracle; a row relying on it must say
#   so explicitly.
#
# pin site note: inst/tinytest/test-rbart-loop-callback.R carries 15
#   RNG-locked literals (tolerance 1e-8) that tools/regenerate-snapshots.R
#   does NOT cover; it has been hand-regenerated twice (31a4c017, c8f661af).
#
# file                                role                 commit   machine/arch    scenarios
equivalence-736bfb05.rds              current              736bfb05 arm64 (macOS)   43 scenarios, same set as predecessor. Re-recorded when xbart()'s k-grid sort-before-sweep was restored, which moves the RNG stream for scenarios whose k grid was not already sorted. ORACLE (P17), adjudication: PARTITION vs predecessor under --strict-coverage - 42/43 scenarios bitwise, xbart alone moves (max |z| = 1.94 over 8 summaries, well under the |z| > 4 threshold); test-xbart-method.R's order-invariance pin confirms only the warm-start trajectory moved, not the posterior. Reproduces itself 43/43 under --strict-coverage.
equivalence-31e52644.rds              historical           31e52644 arm64 (macOS)   37 scenarios, the same set as its predecessor; re-recorded at the prior-initializer fix landing: Chain::sampleTreesFromPrior draws each tree by whole-tree rejection until Tree::bottomNodesHaveWeight accepts - the CGM prior CONDITIONED on the no-empty-leaf set, the law the moves price via logLikelihoodForBranch's veto (docs/design/empty-leaf-veto.md) - where it previously PROJECTED an unrestricted draw through collapseEmptyNodes, a different law that also admitted leaves holding only zero-weight rows. The initial forest's RNG stream moves in every fit that initializes trees from the prior; converged posteriors are unaffected (Chain::run never calls the initializer). ORACLE (P17): benchmarks/R/geweke-mc.R FULL exits 0 at this tip - the marginal-conditional oracle whose FIRING adjudicated the defect (pre-fix initializer P(single-leaf tree) 0.04993 vs theory 0.0500 where the sweep realizes 0.05492 vs its theory 0.0552, z = 17.61) - plus backfit-exact PASS and bcf-exact quick OK at gaps <= 0.0009. PARTITION vs its predecessor inside a compare against it (non-strict): 33 of 37 scenarios reproduce BITWISE ("identical draws (same RNG stream)"); the movers are EXACTLY the four prior-initializing fits - grouped max |z| 2.26, grouped_aft 1.24, hazard 2.30, hurdle 3.01, all under 4, posteriors statistically unmoved. NEUTRALITY: at the same tip bcf-equivalence-6e3b9fb8 is bitwise on every channel of all 12 scenarios and multinomial-equivalence-1027be5 (recording not retained) is 10/10 bitwise - no scenario in either harness initializes trees from the prior (bartcoreRun-driven, bare-root start), so neither re-records. This baseline reproduces itself 37/37 under --strict-coverage. Superseded by f009eff8 (the P16 xbart harness extension), whose 37 shared scenarios - the whole set - reproduce it bitwise
equivalence-f494156.rds               historical           f494156  arm64 (macOS)   26: the 25 of 1d9388d plus hazard (discrete-time survival, family = "hazard"; the survival discrete-time-hazard arc - per-draw survival probabilities on held-out subjects, varcount over the person-period design, and the posterior-mean survival surface). The hazard family is person-period ingestion sugar over the probit path - it adds NO engine code and remaps to probit before the sampler is built - so the engine binary is IDENTICAL to 1d9388d (this landing is R-only; state so plainly). NEUTRALITY: all 25 pre-existing scenarios reproduce 1d9388d bitwise inside a compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 25 compared / 1 skipped (not in baseline): hazard - the addition changed no existing draws), and this baseline reproduces itself 26/26 under --strict-coverage - superseded by 7903855 (recording not retained)
equivalence-1d9388d.rds               historical           1d9388d  arm64 (macOS)   25: the 24 of 227f46a plus nbinom (overdispersed counts from a nonlinear log-mean, family = "nbinom", estimated dispersion; the negative-binomial arc - latent test fits, varcount, per-draw dispersion r, and test mean counts) - superseded by f494156. Recorded at the C2 engine HEAD 1d9388d (C3 is R-only, so the engine binary is identical - the ordinal precedent). NEUTRALITY: all 24 pre-existing scenarios reproduce 227f46a bitwise inside a compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 24 compared / 1 skipped (not in baseline): nbinom - the addition changed no existing draws)
equivalence-227f46a.rds               historical           227f46a  arm64 (macOS)   24: the 23 of 31dc05a plus ordinal (ordered 4-level outcome cut from a latent continuous truth, family = "ordinal"; the ordinal-outcomes arc - latent test fits, varcount, cutpoint draws, and test category probabilities) - superseded by 1d9388d. Recorded at the C2 engine HEAD 227f46a (the C3 landing hash was unknowable at recording; C3 is R-only, so the engine binary is identical). NEUTRALITY: all 23 pre-existing scenarios reproduce 31dc05a bitwise inside a compare against it ("identical draws (same RNG stream)" for every scenario; coverage: 23 compared / 1 skipped (not in baseline): ordinal - the addition changed no existing draws)
equivalence-31dc05a.rds               historical           31dc05a  arm64 (macOS)   23: the 22 of its predecessor plus student (contaminated normal, resid.dist = student() estimated-nu; the robust-errors arc) - superseded by 227f46a. NEUTRALITY: all 22 pre-existing scenarios reproduce its predecessor bitwise inside a compare against it (the student addition changed no existing draws)
equivalence-de67cbb.rds               historical           de67cbb  arm64 (macOS)   21: same as current minus grouped_aft; every shared scenario identical (the addition changed no existing draws)
equivalence-5430fdb.rds               historical-classic   5430fdb  arm64 (macOS)   9: same as b354f3a (recording not retained). Recorded 168665a8 (07-02) under the classic engine, which is deleted (b354f3ab, 07-03); the flip to bartcore is b33d40b3 (07-03). CORRECTED 2026-08-17: this is now the ONLY historical-classic file - equivalence-19c499a.rds and equivalence-fbd2168.rds (both recording not retained) were relabeled to historical, both having been recorded 07-05, after the deletion - so the permanent cross-engine cutover evidence is 9 scenarios, not 18. QUANTIFIED CROSS-ENGINE EVIDENCE (link 1 vs equivalence-b354f3a.rds (recording not retained), recomputed 2026-08-17): 9 scenarios, max abs z 3.83, over 329 summaries, no disjoint ranges. See the 2026-08-17 provenance audit.
bcf-equivalence-6e3b9fb8.rds          current              6e3b9fb8 arm64 (macOS)   12 scenarios, same set as predecessor. SHAPE-ONLY re-record: run()$varcount's forest axis widened from a 4x40 matrix to a 4x2x40 array (single-chain scenarios, so 2-D collapses on the surviving chain axis). ORACLE: every other draw channel (mu, tau, glue, sigma, train, varcount.tau, installed masks) reproduces predecessor bitwise; varcount's new slab 1 is identical() to the old matrix element-for-element, and slab 2's last draw matches a live bartcoreForestVariableCounts(1) read. NEUTRALITY: at the same tip, the gaussian and multinomial equivalence baselines stay bitwise/unrecorded (neither harness can build a multi-forest amplitude model).
multinomial-equivalence-4d9a3337.rds  current              4d9a3337 arm64 (macOS)   11 scenarios (predecessor's 10 plus k3countsswap: a mid-chain response swap through bartcoreSetCounts on an n x K grouped-count matrix). DRAW-SHIFTING re-record: MultinomialForestCombiner::afterCombine divided leaf.scale by sqrt(numTrees) a second time, so the level's sd was sqrt(m) too small (8.66x at n.trees = 75); every multinomial draw moves. ORACLE (P17), two: (a) benchmarks/R/multinomial-exact.R's six identified-probability arms, unmoved post-fix (the level cannot move an identified probability); (b) that script's new arm 7, an analytic identity - the grand level's sample sd now matches tau/sqrt(K) (gap 0.0018 against a 0.043 band), vs 8.66x too small pre-fix. tests/cpp's testMultinomialCombinerSeam pins the same closed form at a hand-built fixture. k3countsswap's non-vacuity (moves train and forestFits, shifts tree structure vs. the swap omitted) and the neutrality of the gaussian and bcf equivalence baselines at the same tip were also checked; reproduces itself, all 11 scenarios x their channels.
bench-sampler-ab1dc52.csv             current              ab1dc52  arm64 (macOS)   speed baseline (hardened harness), recorded cold 2026-08-08 on the post-fused-suffstat quiet-machine grant (1-min loadavg 2.6-2.8 throughout; a same-window print re-run reproduced every arm within 2 percent). vs its predecessor: run-n10000-p10-t75 1.810 -> 1.524 ms/iteration (0.842 - the fused four-bank suffstat pass at the grid's largest n), setPredictor accept/reject 0.965/0.971 (the gather-door fused walk unroll landed in between), every other arm within +/- 1.5 percent, no arm slower
